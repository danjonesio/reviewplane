// Code generated by packages/protocol/tools/generate.ts from
// schemas/platform/v1.schema.json. DO NOT EDIT.

/**
 * Canonical encoders. Output is deterministic: properties appear in schema order, absent
 * optional properties are omitted, and numbers and strings are formatted identically to
 * the Go encoder so that both languages produce byte-identical frames.
 */

import {
  jsonBoolean,
  jsonInteger,
  jsonNumber,
  jsonString,
} from "../../../canonical.ts";
import type {
  Actor,
  ApiError,
  ApiErrorDetails,
  ApiErrorResponse,
  ApiMeta,
  AuthenticationLoginFailedPayload,
  AuthenticationLoginSucceededPayload,
  Correlation,
  CursorClaims,
  Envelope,
  HumanSession,
  JobEnqueuedPayload,
  JobFailedPayload,
  JobSucceededPayload,
  Organisation,
  OrganisationCreatedPayload,
  Project,
  ProjectArchivedPayload,
  ProjectCreatedPayload,
  ProjectRepositoryChangedPayload,
  ProjectSettings,
  ProjectUpdatedPayload,
  RepositoryIdentity,
  SessionRevokedPayload,
  StreamError,
  StreamHeartbeat,
  StreamRefreshRequired,
  StreamSubscribe,
  StreamSubscribed,
  User,
  UserCredentialsSetPayload,
  UserInvitedPayload,
  ValidationViewport,
} from "./types.ts";

/**
 * Canonically encodes a RepositoryIdentity.
 */
export function encodeRepositoryIdentity(value: RepositoryIdentity): string {
  const fields: string[] = [];
  fields.push(`"canonical":${jsonString(value.canonical)}`);
  if (value.clone_urls !== undefined) {
    fields.push(`"clone_urls":${`[${value.clone_urls.map((item) => jsonString(item)).join(",")}]`}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ValidationViewport.
 */
export function encodeValidationViewport(value: ValidationViewport): string {
  const fields: string[] = [];
  fields.push(`"width":${jsonInteger(value.width)}`);
  fields.push(`"height":${jsonInteger(value.height)}`);
  if (value.device_scale_factor !== undefined) {
    fields.push(`"device_scale_factor":${jsonNumber(value.device_scale_factor)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectSettings.
 */
export function encodeProjectSettings(value: ProjectSettings): string {
  const fields: string[] = [];
  fields.push(`"default_validation_viewports":${`[${value.default_validation_viewports.map((item) => encodeValidationViewport(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Organisation.
 */
export function encodeOrganisation(value: Organisation): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"name":${jsonString(value.name)}`);
  fields.push(`"slug":${jsonString(value.slug)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"created_at":${jsonString(value.created_at)}`);
  fields.push(`"updated_at":${jsonString(value.updated_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a User.
 */
export function encodeUser(value: User): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"organisation_id":${jsonString(value.organisation_id)}`);
  fields.push(`"email":${jsonString(value.email)}`);
  fields.push(`"display_name":${jsonString(value.display_name)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.local_credential_set !== undefined) {
    fields.push(`"local_credential_set":${jsonBoolean(value.local_credential_set)}`);
  }
  fields.push(`"created_at":${jsonString(value.created_at)}`);
  fields.push(`"updated_at":${jsonString(value.updated_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Project.
 */
export function encodeProject(value: Project): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"organisation_id":${jsonString(value.organisation_id)}`);
  fields.push(`"name":${jsonString(value.name)}`);
  fields.push(`"slug":${jsonString(value.slug)}`);
  if (value.repository_identity !== undefined) {
    fields.push(`"repository_identity":${encodeRepositoryIdentity(value.repository_identity)}`);
  }
  fields.push(`"default_branch":${jsonString(value.default_branch)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"settings":${encodeProjectSettings(value.settings)}`);
  fields.push(`"version":${jsonInteger(value.version)}`);
  fields.push(`"created_at":${jsonString(value.created_at)}`);
  fields.push(`"updated_at":${jsonString(value.updated_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a HumanSession.
 */
export function encodeHumanSession(value: HumanSession): string {
  const fields: string[] = [];
  fields.push(`"session_id":${jsonString(value.session_id)}`);
  if (value.user_id !== undefined) {
    fields.push(`"user_id":${jsonString(value.user_id)}`);
  }
  if (value.organisation_id !== undefined) {
    fields.push(`"organisation_id":${jsonString(value.organisation_id)}`);
  }
  if (value.email !== undefined) {
    fields.push(`"email":${jsonString(value.email)}`);
  }
  fields.push(`"display":${jsonString(value.display)}`);
  if (value.project_ids !== undefined) {
    fields.push(`"project_ids":${`[${value.project_ids.map((item) => jsonString(item)).join(",")}]`}`);
  }
  fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Actor.
 */
export function encodeActor(value: Actor): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  if (value.id !== undefined) {
    fields.push(`"id":${jsonString(value.id)}`);
  }
  if (value.display !== undefined) {
    fields.push(`"display":${jsonString(value.display)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Correlation.
 */
export function encodeCorrelation(value: Correlation): string {
  const fields: string[] = [];
  if (value.request_id !== undefined) {
    fields.push(`"request_id":${jsonString(value.request_id)}`);
  }
  if (value.causation_event_id !== undefined) {
    fields.push(`"causation_event_id":${jsonString(value.causation_event_id)}`);
  }
  if (value.connector_id !== undefined) {
    fields.push(`"connector_id":${jsonString(value.connector_id)}`);
  }
  if (value.environment_id !== undefined) {
    fields.push(`"environment_id":${jsonString(value.environment_id)}`);
  }
  if (value.workspace_id !== undefined) {
    fields.push(`"workspace_id":${jsonString(value.workspace_id)}`);
  }
  if (value.published_service_id !== undefined) {
    fields.push(`"published_service_id":${jsonString(value.published_service_id)}`);
  }
  if (value.agent_session_id !== undefined) {
    fields.push(`"agent_session_id":${jsonString(value.agent_session_id)}`);
  }
  if (value.browser_session_id !== undefined) {
    fields.push(`"browser_session_id":${jsonString(value.browser_session_id)}`);
  }
  if (value.worker_id !== undefined) {
    fields.push(`"worker_id":${jsonString(value.worker_id)}`);
  }
  if (value.artefact_id !== undefined) {
    fields.push(`"artefact_id":${jsonString(value.artefact_id)}`);
  }
  if (value.review_id !== undefined) {
    fields.push(`"review_id":${jsonString(value.review_id)}`);
  }
  if (value.finding_id !== undefined) {
    fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  }
  if (value.annotation_id !== undefined) {
    fields.push(`"annotation_id":${jsonString(value.annotation_id)}`);
  }
  if (value.job_id !== undefined) {
    fields.push(`"job_id":${jsonString(value.job_id)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Envelope.
 */
export function encodeEnvelope(value: Envelope, payloadJson: string): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"schema_version":${jsonInteger(value.schema_version)}`);
  fields.push(`"sequence":${jsonInteger(value.sequence)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"occurred_at":${jsonString(value.occurred_at)}`);
  fields.push(`"recorded_at":${jsonString(value.recorded_at)}`);
  fields.push(`"organisation_id":${jsonString(value.organisation_id)}`);
  if (value.project_id !== undefined) {
    fields.push(`"project_id":${jsonString(value.project_id)}`);
  }
  fields.push(`"actor":${encodeActor(value.actor)}`);
  if (value.correlation !== undefined) {
    fields.push(`"correlation":${encodeCorrelation(value.correlation)}`);
  }
  fields.push(`"payload":${payloadJson}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a OrganisationCreatedPayload.
 */
export function encodeOrganisationCreatedPayload(value: OrganisationCreatedPayload): string {
  const fields: string[] = [];
  fields.push(`"slug":${jsonString(value.slug)}`);
  fields.push(`"name":${jsonString(value.name)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectCreatedPayload.
 */
export function encodeProjectCreatedPayload(value: ProjectCreatedPayload): string {
  const fields: string[] = [];
  fields.push(`"slug":${jsonString(value.slug)}`);
  fields.push(`"name":${jsonString(value.name)}`);
  if (value.default_branch !== undefined) {
    fields.push(`"default_branch":${jsonString(value.default_branch)}`);
  }
  if (value.repository_canonical !== undefined) {
    fields.push(`"repository_canonical":${jsonString(value.repository_canonical)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectRepositoryChangedPayload.
 */
export function encodeProjectRepositoryChangedPayload(value: ProjectRepositoryChangedPayload): string {
  const fields: string[] = [];
  if (value.previous_canonical !== undefined) {
    fields.push(`"previous_canonical":${jsonString(value.previous_canonical)}`);
  }
  fields.push(`"new_canonical":${jsonString(value.new_canonical)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a UserInvitedPayload.
 */
export function encodeUserInvitedPayload(value: UserInvitedPayload): string {
  const fields: string[] = [];
  fields.push(`"user_id":${jsonString(value.user_id)}`);
  fields.push(`"method":${jsonString(value.method)}`);
  fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a UserCredentialsSetPayload.
 */
export function encodeUserCredentialsSetPayload(value: UserCredentialsSetPayload): string {
  const fields: string[] = [];
  fields.push(`"user_id":${jsonString(value.user_id)}`);
  fields.push(`"method":${jsonString(value.method)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a AuthenticationLoginSucceededPayload.
 */
export function encodeAuthenticationLoginSucceededPayload(value: AuthenticationLoginSucceededPayload): string {
  const fields: string[] = [];
  fields.push(`"session_id":${jsonString(value.session_id)}`);
  if (value.user_id !== undefined) {
    fields.push(`"user_id":${jsonString(value.user_id)}`);
  }
  fields.push(`"method":${jsonString(value.method)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a AuthenticationLoginFailedPayload.
 */
export function encodeAuthenticationLoginFailedPayload(value: AuthenticationLoginFailedPayload): string {
  const fields: string[] = [];
  fields.push(`"reason":${jsonString(value.reason)}`);
  fields.push(`"method":${jsonString(value.method)}`);
  if (value.user_id !== undefined) {
    fields.push(`"user_id":${jsonString(value.user_id)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionRevokedPayload.
 */
export function encodeSessionRevokedPayload(value: SessionRevokedPayload): string {
  const fields: string[] = [];
  fields.push(`"session_id":${jsonString(value.session_id)}`);
  if (value.user_id !== undefined) {
    fields.push(`"user_id":${jsonString(value.user_id)}`);
  }
  fields.push(`"reason":${jsonString(value.reason)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectUpdatedPayload.
 */
export function encodeProjectUpdatedPayload(value: ProjectUpdatedPayload): string {
  const fields: string[] = [];
  fields.push(`"changed_fields":${`[${value.changed_fields.map((item) => jsonString(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectArchivedPayload.
 */
export function encodeProjectArchivedPayload(value: ProjectArchivedPayload): string {
  const fields: string[] = [];
  fields.push(`"previous_status":${jsonString(value.previous_status)}`);
  fields.push(`"new_status":${jsonString(value.new_status)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a JobEnqueuedPayload.
 */
export function encodeJobEnqueuedPayload(value: JobEnqueuedPayload): string {
  const fields: string[] = [];
  fields.push(`"job_id":${jsonString(value.job_id)}`);
  fields.push(`"kind":${jsonString(value.kind)}`);
  fields.push(`"run_after":${jsonString(value.run_after)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a JobSucceededPayload.
 */
export function encodeJobSucceededPayload(value: JobSucceededPayload): string {
  const fields: string[] = [];
  fields.push(`"job_id":${jsonString(value.job_id)}`);
  fields.push(`"kind":${jsonString(value.kind)}`);
  fields.push(`"attempts":${jsonInteger(value.attempts)}`);
  fields.push(`"duration_ms":${jsonInteger(value.duration_ms)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a JobFailedPayload.
 */
export function encodeJobFailedPayload(value: JobFailedPayload): string {
  const fields: string[] = [];
  fields.push(`"job_id":${jsonString(value.job_id)}`);
  fields.push(`"kind":${jsonString(value.kind)}`);
  fields.push(`"attempts":${jsonInteger(value.attempts)}`);
  fields.push(`"reason":${jsonString(value.reason)}`);
  fields.push(`"retrying":${jsonBoolean(value.retrying)}`);
  if (value.next_attempt_at !== undefined) {
    fields.push(`"next_attempt_at":${jsonString(value.next_attempt_at)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamSubscribe.
 */
export function encodeStreamSubscribe(value: StreamSubscribe): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"last_sequence":${jsonInteger(value.last_sequence)}`);
  if (value.max_replay !== undefined) {
    fields.push(`"max_replay":${jsonInteger(value.max_replay)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamSubscribed.
 */
export function encodeStreamSubscribed(value: StreamSubscribed): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"project_id":${jsonString(value.project_id)}`);
  fields.push(`"current_sequence":${jsonInteger(value.current_sequence)}`);
  fields.push(`"earliest_available_sequence":${jsonInteger(value.earliest_available_sequence)}`);
  fields.push(`"replaying":${jsonBoolean(value.replaying)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamRefreshRequired.
 */
export function encodeStreamRefreshRequired(value: StreamRefreshRequired): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"reason":${jsonString(value.reason)}`);
  fields.push(`"current_sequence":${jsonInteger(value.current_sequence)}`);
  fields.push(`"earliest_available_sequence":${jsonInteger(value.earliest_available_sequence)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamHeartbeat.
 */
export function encodeStreamHeartbeat(value: StreamHeartbeat): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"current_sequence":${jsonInteger(value.current_sequence)}`);
  fields.push(`"sent_at":${jsonString(value.sent_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamError.
 */
export function encodeStreamError(value: StreamError): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"code":${jsonString(value.code)}`);
  fields.push(`"message":${jsonString(value.message)}`);
  fields.push(`"retryable":${jsonBoolean(value.retryable)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ApiErrorDetails.
 */
export function encodeApiErrorDetails(value: ApiErrorDetails): string {
  const fields: string[] = [];
  if (value.current_version !== undefined) {
    fields.push(`"current_version":${jsonInteger(value.current_version)}`);
  }
  if (value.expected_version !== undefined) {
    fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  }
  if (value.current_epoch !== undefined) {
    fields.push(`"current_epoch":${jsonInteger(value.current_epoch)}`);
  }
  if (value.candidates !== undefined) {
    fields.push(`"candidates":${`[${value.candidates.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.allowed_transitions !== undefined) {
    fields.push(`"allowed_transitions":${`[${value.allowed_transitions.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.required_evidence !== undefined) {
    fields.push(`"required_evidence":${`[${value.required_evidence.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.missing_context !== undefined) {
    fields.push(`"missing_context":${`[${value.missing_context.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.retry_after_ms !== undefined) {
    fields.push(`"retry_after_ms":${jsonInteger(value.retry_after_ms)}`);
  }
  if (value.field !== undefined) {
    fields.push(`"field":${jsonString(value.field)}`);
  }
  if (value.reason !== undefined) {
    fields.push(`"reason":${jsonString(value.reason)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ApiError.
 */
export function encodeApiError(value: ApiError): string {
  const fields: string[] = [];
  fields.push(`"code":${jsonString(value.code)}`);
  fields.push(`"message":${jsonString(value.message)}`);
  if (value.details !== undefined) {
    fields.push(`"details":${encodeApiErrorDetails(value.details)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ApiMeta.
 */
export function encodeApiMeta(value: ApiMeta): string {
  const fields: string[] = [];
  fields.push(`"request_id":${jsonString(value.request_id)}`);
  if (value.next_cursor !== undefined) {
    fields.push(`"next_cursor":${jsonString(value.next_cursor)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ApiErrorResponse.
 */
export function encodeApiErrorResponse(value: ApiErrorResponse): string {
  const fields: string[] = [];
  fields.push(`"error":${encodeApiError(value.error)}`);
  fields.push(`"meta":${encodeApiMeta(value.meta)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a CursorClaims.
 */
export function encodeCursorClaims(value: CursorClaims): string {
  const fields: string[] = [];
  fields.push(`"version":${jsonInteger(value.version)}`);
  fields.push(`"sort_key":${jsonString(value.sort_key)}`);
  fields.push(`"id":${jsonString(value.id)}`);
  return `{${fields.join(",")}}`;
}
