// Code generated by packages/protocol/tools/generate.ts from
// schemas/platform/v1.schema.json. DO NOT EDIT.

import type { SchemaViolation } from "./types.ts";
import {
  checkArray,
  checkBoolean,
  checkInteger,
  checkObject,
  checkPlainObject,
  checkString,
} from "../../../validate-runtime.ts";

const PATTERN_0 = new RegExp("^[A-Za-z0-9_-]+$", "u");
const PATTERN_1 = new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$", "u");
const PATTERN_2 = new RegExp("^[^\\x00-\\x1f]+$", "u");
const PATTERN_3 = new RegExp("^[a-z0-9][a-z0-9-]*$", "u");
const PATTERN_4 = new RegExp("^[a-z][a-z0-9_]*$", "u");
const PATTERN_5 = new RegExp("^[^\\x00]+$", "u");
const PATTERN_6 = new RegExp("^[A-Za-z0-9_.-]+$", "u");
const PATTERN_7 = new RegExp("^[a-z][a-z0-9_.]*$", "u");
const PATTERN_8 = new RegExp("^[a-z][a-z0-9_.\\[\\]]*$", "u");

/**
 * Opaque durable identifier (docs/DOMAIN_MODEL.md section 3). Consumers MUST treat the
 * value as opaque: the schema bounds only its length and character class and never its
 * documented prefix, and the value encodes no tenant, no timestamp and no database
 * sequence.
 */
export function validateIdentifier(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_0 });
}

/**
 * RFC 3339 timestamp in UTC, with a mandatory trailing Z.
 */
export function validateTimestamp(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 20, maxLength: 30, pattern: PATTERN_1 });
}

/**
 * Position in a project's event stream. Monotonically increasing within a project and
 * never reused. Global ordering across projects is not guaranteed, and the value is not a
 * database row identifier a consumer may depend on.
 */
export function validateSequenceNumber(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 9007199254740991 });
}

/**
 * Opaque pagination cursor (docs/API.md section 6). It is base64url text without padding;
 * its contents are the server's business and a client MUST NOT parse, construct or modify
 * one.
 */
export function validateCursor(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 512, pattern: PATTERN_0 });
}

/**
 * The event types this source owns. Unknown types are rejected by this codec, never
 * ignored. This enumeration MUST equal the keys of x-protocol.messages, and is a subset of
 * the event_types vocabulary: an event type defined in another schema source is decoded by
 * that source.
 */
export function validateMessageType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["organisation.created","project.created","project.updated","project.archived","job.enqueued","job.succeeded","job.failed"] });
}

/**
 * Stable API error code (docs/API.md section 5, docs/MCP_SPEC.md section 12). This
 * enumeration MUST equal x-protocol.error_classes. Adding a code is additive within a
 * protocol version, and clients MUST tolerate a code they do not recognise.
 */
export function validateErrorClass(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["AUTHENTICATION_REQUIRED","AUTHORISATION_DENIED","PROJECT_CONTEXT_AMBIGUOUS","PROJECT_CONTEXT_MISMATCH","RESOURCE_NOT_FOUND","RESOURCE_STALE","VERSION_CONFLICT","IDEMPOTENCY_CONFLICT","VALIDATION_FAILED","CONNECTOR_OFFLINE","PUBLISHED_SERVICE_UNAVAILABLE","BROWSER_CAPACITY_EXHAUSTED","BROWSER_SESSION_NOT_ACTIVE","BROWSER_COMMAND_TIMEOUT","CONTROL_NOT_OWNED","CONTROL_EPOCH_STALE","POLICY_DENIED","APPROVAL_REQUIRED","EVIDENCE_REQUIRED","ARTEFACT_UPLOAD_INCOMPLETE","UNSUPPORTED_CAPABILITY","RATE_LIMITED","INTERNAL_ERROR"] });
}

/**
 * Kind of principal an event is attributed to (docs/EVENTS.md section 5). Actor identity
 * MUST never be inferred from display text.
 */
export function validateActorType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["human_user","agent_session","connector","browser_worker","system","integration"] });
}

/**
 * Durable background work the runner understands (docs/ARCHITECTURE.md section 4.8). It is
 * an enumeration because an operator reading a failed job needs a name with a fixed
 * meaning, not free text a caller chose.
 */
export function validateJobKind(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["event_outbox_dispatch","idempotency_key_expiry","published_service_expiry"] });
}

/**
 * Why a job attempt failed. Stable, so a dead-lettered job can be diagnosed from the audit
 * trail alone.
 */
export function validateJobFailureReason(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["handler_error","attempt_timeout","attempts_exhausted","handler_unknown"] });
}

/**
 * Project lifecycle status (docs/DOMAIN_MODEL.md section 5).
 */
export function validateProjectStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["active","archived"] });
}

/**
 * Human-readable label for a timeline. It is description and never an authorisation input.
 */
export function validateActorDisplay(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 200, pattern: PATTERN_2 });
}

/**
 * The principal an event is attributed to (docs/EVENTS.md section 2).
 */
export function validateActor(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "id", "display"], ["type"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateActorType(source["type"], `${path}.type`, out);
  }
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["display"] !== undefined) {
    validateActorDisplay(source["display"], `${path}.display`, out);
  }
}

/**
 * Correlation identifiers (docs/EVENTS.md section 2, docs/ARCHITECTURE.md section 15).
 * Every member is an opaque identifier: correlation never carries a header, a body or a
 * secret.
 */
export function validateCorrelation(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["request_id", "causation_event_id", "connector_id", "environment_id", "workspace_id", "published_service_id", "agent_session_id", "browser_session_id", "worker_id", "artefact_id", "review_id", "finding_id", "annotation_id", "job_id"], []);
  if (source === null) return;
  if (source["request_id"] !== undefined) {
    validateIdentifier(source["request_id"], `${path}.request_id`, out);
  }
  if (source["causation_event_id"] !== undefined) {
    validateIdentifier(source["causation_event_id"], `${path}.causation_event_id`, out);
  }
  if (source["connector_id"] !== undefined) {
    validateIdentifier(source["connector_id"], `${path}.connector_id`, out);
  }
  if (source["environment_id"] !== undefined) {
    validateIdentifier(source["environment_id"], `${path}.environment_id`, out);
  }
  if (source["workspace_id"] !== undefined) {
    validateIdentifier(source["workspace_id"], `${path}.workspace_id`, out);
  }
  if (source["published_service_id"] !== undefined) {
    validateIdentifier(source["published_service_id"], `${path}.published_service_id`, out);
  }
  if (source["agent_session_id"] !== undefined) {
    validateIdentifier(source["agent_session_id"], `${path}.agent_session_id`, out);
  }
  if (source["browser_session_id"] !== undefined) {
    validateIdentifier(source["browser_session_id"], `${path}.browser_session_id`, out);
  }
  if (source["worker_id"] !== undefined) {
    validateIdentifier(source["worker_id"], `${path}.worker_id`, out);
  }
  if (source["artefact_id"] !== undefined) {
    validateIdentifier(source["artefact_id"], `${path}.artefact_id`, out);
  }
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["annotation_id"] !== undefined) {
    validateIdentifier(source["annotation_id"], `${path}.annotation_id`, out);
  }
  if (source["job_id"] !== undefined) {
    validateIdentifier(source["job_id"], `${path}.job_id`, out);
  }
}

/**
 * Envelope schema version. Stage 1 accepts 1 only; any other value is rejected rather than
 * best-effort parsed.
 */
export function validateEnvelopeSchemaVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1 });
}

/**
 * Event payload. Its schema is selected by type. Payloads exclude raw secrets, cookies and
 * authorisation headers (docs/EVENTS.md section 8); the event is bounded by
 * x-protocol.limits.max_event_bytes and each payload by its own x-max-bytes.
 */
export function validateEnvelopePayload(value: unknown, path: string, out: SchemaViolation[]): void {
  checkPlainObject(value, path, out);
}

/**
 * The docs/EVENTS.md section 2 event envelope. Every event names the organisation it
 * belongs to, its position in a stream, and the actor that caused it; the payload schema
 * is selected by type. project_id is optional because an occurrence can precede any
 * project association — a connector enrolment does — and every project-scoped read filters
 * on it.
 */
export function validateEnvelope(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "schema_version", "sequence", "type", "occurred_at", "recorded_at", "organisation_id", "project_id", "actor", "correlation", "payload"], ["id", "schema_version", "sequence", "type", "occurred_at", "recorded_at", "organisation_id", "actor", "payload"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["schema_version"] !== undefined) {
    validateEnvelopeSchemaVersion(source["schema_version"], `${path}.schema_version`, out);
  }
  if (source["sequence"] !== undefined) {
    validateSequenceNumber(source["sequence"], `${path}.sequence`, out);
  }
  if (source["type"] !== undefined) {
    validateMessageType(source["type"], `${path}.type`, out);
  }
  if (source["occurred_at"] !== undefined) {
    validateTimestamp(source["occurred_at"], `${path}.occurred_at`, out);
  }
  if (source["recorded_at"] !== undefined) {
    validateTimestamp(source["recorded_at"], `${path}.recorded_at`, out);
  }
  if (source["organisation_id"] !== undefined) {
    validateIdentifier(source["organisation_id"], `${path}.organisation_id`, out);
  }
  if (source["project_id"] !== undefined) {
    validateIdentifier(source["project_id"], `${path}.project_id`, out);
  }
  if (source["actor"] !== undefined) {
    validateActor(source["actor"], `${path}.actor`, out);
  }
  if (source["correlation"] !== undefined) {
    validateCorrelation(source["correlation"], `${path}.correlation`, out);
  }
  if (source["payload"] !== undefined) {
    validateEnvelopePayload(source["payload"], `${path}.payload`, out);
  }
}

/**
 * Mutable human-friendly alias. The identifier is authoritative; this is not.
 */
export function validateOrganisationCreatedPayloadSlug(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 63, pattern: PATTERN_3 });
}

/**
 * Display name.
 */
export function validateOrganisationCreatedPayloadName(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 200, pattern: PATTERN_2 });
}

/**
 * Payload of organisation.created.
 */
export function validateOrganisationCreatedPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["slug", "name"], ["slug", "name"]);
  if (source === null) return;
  if (source["slug"] !== undefined) {
    validateOrganisationCreatedPayloadSlug(source["slug"], `${path}.slug`, out);
  }
  if (source["name"] !== undefined) {
    validateOrganisationCreatedPayloadName(source["name"], `${path}.name`, out);
  }
}

/**
 * Project-scoped alias. Mutable, and never a substitute for the identifier.
 */
export function validateProjectCreatedPayloadSlug(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 63, pattern: PATTERN_3 });
}

/**
 * Display name.
 */
export function validateProjectCreatedPayloadName(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 200, pattern: PATTERN_2 });
}

/**
 * Payload of project.created.
 */
export function validateProjectCreatedPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["slug", "name"], ["slug", "name"]);
  if (source === null) return;
  if (source["slug"] !== undefined) {
    validateProjectCreatedPayloadSlug(source["slug"], `${path}.slug`, out);
  }
  if (source["name"] !== undefined) {
    validateProjectCreatedPayloadName(source["name"], `${path}.name`, out);
  }
}

export function validateProjectUpdatedPayloadChangedFieldsItem(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_4 });
}

/**
 * Attribute names that changed, in schema order.
 */
export function validateProjectUpdatedPayloadChangedFields(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateProjectUpdatedPayloadChangedFieldsItem(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of project.updated. It names the fields that changed rather than restating the
 * record: current state is queryable, and the audit question is what moved.
 */
export function validateProjectUpdatedPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["changed_fields"], ["changed_fields"]);
  if (source === null) return;
  if (source["changed_fields"] !== undefined) {
    validateProjectUpdatedPayloadChangedFields(source["changed_fields"], `${path}.changed_fields`, out);
  }
}

/**
 * Payload of project.archived. It carries previous and new state, which docs/EVENTS.md
 * section 8 requires of a state change.
 */
export function validateProjectArchivedPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["previous_status", "new_status"], ["previous_status", "new_status"]);
  if (source === null) return;
  if (source["previous_status"] !== undefined) {
    validateProjectStatus(source["previous_status"], `${path}.previous_status`, out);
  }
  if (source["new_status"] !== undefined) {
    validateProjectStatus(source["new_status"], `${path}.new_status`, out);
  }
}

/**
 * Payload of job.enqueued.
 */
export function validateJobEnqueuedPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["job_id", "kind", "run_after"], ["job_id", "kind", "run_after"]);
  if (source === null) return;
  if (source["job_id"] !== undefined) {
    validateIdentifier(source["job_id"], `${path}.job_id`, out);
  }
  if (source["kind"] !== undefined) {
    validateJobKind(source["kind"], `${path}.kind`, out);
  }
  if (source["run_after"] !== undefined) {
    validateTimestamp(source["run_after"], `${path}.run_after`, out);
  }
}

/**
 * Attempts made, including the one that succeeded.
 */
export function validateJobSucceededPayloadAttempts(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1000 });
}

/**
 * Wall-clock duration of the successful attempt.
 */
export function validateJobSucceededPayloadDurationMs(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 86400000 });
}

/**
 * Payload of job.succeeded.
 */
export function validateJobSucceededPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["job_id", "kind", "attempts", "duration_ms"], ["job_id", "kind", "attempts", "duration_ms"]);
  if (source === null) return;
  if (source["job_id"] !== undefined) {
    validateIdentifier(source["job_id"], `${path}.job_id`, out);
  }
  if (source["kind"] !== undefined) {
    validateJobKind(source["kind"], `${path}.kind`, out);
  }
  if (source["attempts"] !== undefined) {
    validateJobSucceededPayloadAttempts(source["attempts"], `${path}.attempts`, out);
  }
  if (source["duration_ms"] !== undefined) {
    validateJobSucceededPayloadDurationMs(source["duration_ms"], `${path}.duration_ms`, out);
  }
}

/**
 * Attempts made, including the one that failed.
 */
export function validateJobFailedPayloadAttempts(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1000 });
}

/**
 * Whether another attempt is scheduled. Stated rather than inferred, so an operator can
 * tell a retry from a dead job without reading the runner's source.
 */
export function validateJobFailedPayloadRetrying(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Payload of job.failed.
 */
export function validateJobFailedPayload(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["job_id", "kind", "attempts", "reason", "retrying", "next_attempt_at"], ["job_id", "kind", "attempts", "reason", "retrying"]);
  if (source === null) return;
  if (source["job_id"] !== undefined) {
    validateIdentifier(source["job_id"], `${path}.job_id`, out);
  }
  if (source["kind"] !== undefined) {
    validateJobKind(source["kind"], `${path}.kind`, out);
  }
  if (source["attempts"] !== undefined) {
    validateJobFailedPayloadAttempts(source["attempts"], `${path}.attempts`, out);
  }
  if (source["reason"] !== undefined) {
    validateJobFailureReason(source["reason"], `${path}.reason`, out);
  }
  if (source["retrying"] !== undefined) {
    validateJobFailedPayloadRetrying(source["retrying"], `${path}.retrying`, out);
  }
  if (source["next_attempt_at"] !== undefined) {
    validateTimestamp(source["next_attempt_at"], `${path}.next_attempt_at`, out);
  }
}

/**
 * Message discriminator.
 */
export function validateStreamSubscribeType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["stream.subscribe"] });
}

/**
 * The highest project sequence the client has already applied. Zero means the client holds
 * nothing and wants the stream from its start; a client that has just loaded current state
 * sends the sequence that state was read at.
 */
export function validateStreamSubscribeLastSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Largest replay the client will accept. A gap larger than this is answered with a refresh
 * instruction rather than a long replay, which is cheaper for both sides than paging
 * through history the client is about to refetch anyway.
 */
export function validateStreamSubscribeMaxReplay(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 10000 });
}

/**
 * Subscription request (docs/API.md section 18.1).
 */
export function validateStreamSubscribe(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "last_sequence", "max_replay"], ["type", "last_sequence"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateStreamSubscribeType(source["type"], `${path}.type`, out);
  }
  if (source["last_sequence"] !== undefined) {
    validateStreamSubscribeLastSequence(source["last_sequence"], `${path}.last_sequence`, out);
  }
  if (source["max_replay"] !== undefined) {
    validateStreamSubscribeMaxReplay(source["max_replay"], `${path}.max_replay`, out);
  }
}

/**
 * Message discriminator.
 */
export function validateStreamSubscribedType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["stream.subscribed"] });
}

/**
 * Sequence the stream is at now. Zero when the project has no events yet.
 */
export function validateStreamSubscribedCurrentSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Oldest sequence still replayable. A client below it receives a refresh instruction
 * instead of a replay.
 */
export function validateStreamSubscribedEarliestAvailableSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Whether events between the client's position and current_sequence follow before live
 * delivery begins.
 */
export function validateStreamSubscribedReplaying(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Subscription accepted.
 */
export function validateStreamSubscribed(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "project_id", "current_sequence", "earliest_available_sequence", "replaying"], ["type", "project_id", "current_sequence", "earliest_available_sequence", "replaying"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateStreamSubscribedType(source["type"], `${path}.type`, out);
  }
  if (source["project_id"] !== undefined) {
    validateIdentifier(source["project_id"], `${path}.project_id`, out);
  }
  if (source["current_sequence"] !== undefined) {
    validateStreamSubscribedCurrentSequence(source["current_sequence"], `${path}.current_sequence`, out);
  }
  if (source["earliest_available_sequence"] !== undefined) {
    validateStreamSubscribedEarliestAvailableSequence(source["earliest_available_sequence"], `${path}.earliest_available_sequence`, out);
  }
  if (source["replaying"] !== undefined) {
    validateStreamSubscribedReplaying(source["replaying"], `${path}.replaying`, out);
  }
}

/**
 * Message discriminator.
 */
export function validateStreamRefreshRequiredType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["stream.refresh_required"] });
}

/**
 * Why a replay was not possible.
 */
export function validateStreamRefreshRequiredReason(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["replay_window_exceeded","sequence_ahead_of_stream","replay_limit_exceeded"] });
}

/**
 * Sequence the stream is at now. A client refetches state and resumes from it.
 */
export function validateStreamRefreshRequiredCurrentSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Oldest sequence still replayable.
 */
export function validateStreamRefreshRequiredEarliestAvailableSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * The client's position cannot be served from the retained stream (docs/EVENTS.md section
 * 10).
 */
export function validateStreamRefreshRequired(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "reason", "current_sequence", "earliest_available_sequence"], ["type", "reason", "current_sequence", "earliest_available_sequence"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateStreamRefreshRequiredType(source["type"], `${path}.type`, out);
  }
  if (source["reason"] !== undefined) {
    validateStreamRefreshRequiredReason(source["reason"], `${path}.reason`, out);
  }
  if (source["current_sequence"] !== undefined) {
    validateStreamRefreshRequiredCurrentSequence(source["current_sequence"], `${path}.current_sequence`, out);
  }
  if (source["earliest_available_sequence"] !== undefined) {
    validateStreamRefreshRequiredEarliestAvailableSequence(source["earliest_available_sequence"], `${path}.earliest_available_sequence`, out);
  }
}

/**
 * Message discriminator.
 */
export function validateStreamHeartbeatType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["stream.heartbeat"] });
}

/**
 * Sequence the stream is at now.
 */
export function validateStreamHeartbeatCurrentSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Liveness on a quiet stream.
 */
export function validateStreamHeartbeat(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "current_sequence", "sent_at"], ["type", "current_sequence", "sent_at"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateStreamHeartbeatType(source["type"], `${path}.type`, out);
  }
  if (source["current_sequence"] !== undefined) {
    validateStreamHeartbeatCurrentSequence(source["current_sequence"], `${path}.current_sequence`, out);
  }
  if (source["sent_at"] !== undefined) {
    validateTimestamp(source["sent_at"], `${path}.sent_at`, out);
  }
}

/**
 * Message discriminator.
 */
export function validateStreamErrorType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["stream.error"] });
}

/**
 * Human-readable explanation. It never carries request data, a credential or a stack
 * trace.
 */
export function validateStreamErrorMessage(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 1024, pattern: PATTERN_5 });
}

/**
 * Whether repeating the subscription verbatim can succeed. Stated rather than inferred
 * from the code, because the answer is not derivable from the code's shape.
 */
export function validateStreamErrorRetryable(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * A refusal on an open subscription.
 */
export function validateStreamError(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "code", "message", "retryable"], ["type", "code", "message", "retryable"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateStreamErrorType(source["type"], `${path}.type`, out);
  }
  if (source["code"] !== undefined) {
    validateErrorClass(source["code"], `${path}.code`, out);
  }
  if (source["message"] !== undefined) {
    validateStreamErrorMessage(source["message"], `${path}.message`, out);
  }
  if (source["retryable"] !== undefined) {
    validateStreamErrorRetryable(source["retryable"], `${path}.retryable`, out);
  }
}

/**
 * The version the record actually holds. Carried by VERSION_CONFLICT so a caller can
 * re-read and retry rather than guess.
 */
export function validateApiErrorDetailsCurrentVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * The version the caller supplied. Carried beside current_version so a log line records
 * both halves of the conflict without the caller having to quote its own request back.
 */
export function validateApiErrorDetailsExpectedVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * The control epoch that is current. Carried by CONTROL_EPOCH_STALE.
 */
export function validateApiErrorDetailsCurrentEpoch(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Identifiers the caller may name to resolve the ambiguity. Carried by
 * PROJECT_CONTEXT_AMBIGUOUS.
 */
export function validateApiErrorDetailsCandidates(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 50, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateIdentifier(value[index], `${path}[${index}]`, out);
  }
}

export function validateApiErrorDetailsAllowedTransitionsItem(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_6 });
}

/**
 * Transitions that are legal from the record's current state. Carried by a refused
 * transition.
 */
export function validateApiErrorDetailsAllowedTransitions(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 50, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateApiErrorDetailsAllowedTransitionsItem(value[index], `${path}[${index}]`, out);
  }
}

export function validateApiErrorDetailsRequiredEvidenceItem(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_4 });
}

/**
 * Evidence the operation needs before it can succeed. Carried by EVIDENCE_REQUIRED.
 */
export function validateApiErrorDetailsRequiredEvidence(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 50, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateApiErrorDetailsRequiredEvidenceItem(value[index], `${path}[${index}]`, out);
  }
}

export function validateApiErrorDetailsMissingContextItem(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_7 });
}

/**
 * Captured context the request omitted. Carried by a refusal to store a finding that could
 * not be reproduced later.
 */
export function validateApiErrorDetailsMissingContext(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 50, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateApiErrorDetailsMissingContextItem(value[index], `${path}[${index}]`, out);
  }
}

/**
 * How long to wait before repeating the request. Carried by RATE_LIMITED and by a
 * duplicate that arrived while the first call was still in flight.
 */
export function validateApiErrorDetailsRetryAfterMs(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 86400000 });
}

/**
 * The request member the refusal is about.
 */
export function validateApiErrorDetailsField(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 128, pattern: PATTERN_8 });
}

/**
 * Stable sub-reason where one code covers several causes.
 */
export function validateApiErrorDetailsReason(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_4 });
}

/**
 * Code-specific refusal detail (docs/MCP_SPEC.md section 12). Every member is optional and
 * the object refuses unknown members, so the detail a caller may rely on is a fixed
 * vocabulary rather than whatever a handler happened to attach.
 */
export function validateApiErrorDetails(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["current_version", "expected_version", "current_epoch", "candidates", "allowed_transitions", "required_evidence", "missing_context", "retry_after_ms", "field", "reason"], []);
  if (source === null) return;
  if (source["current_version"] !== undefined) {
    validateApiErrorDetailsCurrentVersion(source["current_version"], `${path}.current_version`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateApiErrorDetailsExpectedVersion(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["current_epoch"] !== undefined) {
    validateApiErrorDetailsCurrentEpoch(source["current_epoch"], `${path}.current_epoch`, out);
  }
  if (source["candidates"] !== undefined) {
    validateApiErrorDetailsCandidates(source["candidates"], `${path}.candidates`, out);
  }
  if (source["allowed_transitions"] !== undefined) {
    validateApiErrorDetailsAllowedTransitions(source["allowed_transitions"], `${path}.allowed_transitions`, out);
  }
  if (source["required_evidence"] !== undefined) {
    validateApiErrorDetailsRequiredEvidence(source["required_evidence"], `${path}.required_evidence`, out);
  }
  if (source["missing_context"] !== undefined) {
    validateApiErrorDetailsMissingContext(source["missing_context"], `${path}.missing_context`, out);
  }
  if (source["retry_after_ms"] !== undefined) {
    validateApiErrorDetailsRetryAfterMs(source["retry_after_ms"], `${path}.retry_after_ms`, out);
  }
  if (source["field"] !== undefined) {
    validateApiErrorDetailsField(source["field"], `${path}.field`, out);
  }
  if (source["reason"] !== undefined) {
    validateApiErrorDetailsReason(source["reason"], `${path}.reason`, out);
  }
}

/**
 * Human-readable explanation. It never carries a credential, request data or a stack trace
 * (docs/SECURITY.md section 18).
 */
export function validateApiErrorMessage(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 1024, pattern: PATTERN_5 });
}

/**
 * The refusal itself (docs/API.md section 5).
 */
export function validateApiError(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["code", "message", "details"], ["code", "message"]);
  if (source === null) return;
  if (source["code"] !== undefined) {
    validateErrorClass(source["code"], `${path}.code`, out);
  }
  if (source["message"] !== undefined) {
    validateApiErrorMessage(source["message"], `${path}.message`, out);
  }
  if (source["details"] !== undefined) {
    validateApiErrorDetails(source["details"], `${path}.details`, out);
  }
}

/**
 * Response metadata carried by every API response (docs/API.md sections 5 and 6).
 */
export function validateApiMeta(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["request_id", "next_cursor"], ["request_id"]);
  if (source === null) return;
  if (source["request_id"] !== undefined) {
    validateIdentifier(source["request_id"], `${path}.request_id`, out);
  }
  if (source["next_cursor"] !== undefined) {
    validateCursor(source["next_cursor"], `${path}.next_cursor`, out);
  }
}

/**
 * The complete refusal body (docs/API.md section 5). A success body is {"data": ...,
 * "meta": ...} whose data is endpoint-specific and therefore defined by the endpoint's own
 * schema; meta is api_meta in both cases.
 */
export function validateApiErrorResponse(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["error", "meta"], ["error", "meta"]);
  if (source === null) return;
  if (source["error"] !== undefined) {
    validateApiError(source["error"], `${path}.error`, out);
  }
  if (source["meta"] !== undefined) {
    validateApiMeta(source["meta"], `${path}.meta`, out);
  }
}

/**
 * Cursor format version. A cursor of another version is refused rather than guessed at.
 */
export function validateCursorClaimsVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1 });
}

/**
 * Value of the collection's sort column at the last row of the previous page.
 */
export function validateCursorClaimsSortKey(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 128, pattern: PATTERN_5 });
}

/**
 * What an opaque pagination cursor encodes. It is defined here so both languages encode
 * and decode the same bytes, and never to invite a client to build one: the encoded form
 * is base64url text and the API refuses a cursor whose shape it did not produce.
 */
export function validateCursorClaims(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["version", "sort_key", "id"], ["version", "sort_key", "id"]);
  if (source === null) return;
  if (source["version"] !== undefined) {
    validateCursorClaimsVersion(source["version"], `${path}.version`, out);
  }
  if (source["sort_key"] !== undefined) {
    validateCursorClaimsSortKey(source["sort_key"], `${path}.sort_key`, out);
  }
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
}
