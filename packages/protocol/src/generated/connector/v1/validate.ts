// Code generated by packages/protocol/tools/generate.ts from
// schemas/connector/v1.schema.json. DO NOT EDIT.

import type { SchemaViolation } from "./types.ts";
import {
  checkArray,
  checkInteger,
  checkNumber,
  checkObject,
  checkPlainObject,
  checkString,
  forbidProperty,
  matchesCondition,
  requireProperty,
} from "../../../validate-runtime.ts";

const PATTERN_0 = new RegExp("^[A-Za-z0-9_-]+$", "u");
const PATTERN_1 = new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$", "u");
const PATTERN_2 = new RegExp("^wss://[!-~]+$", "u");
const PATTERN_3 = new RegExp("^[0-9A-Za-z][0-9A-Za-z.+-]*$", "u");
const PATTERN_4 = new RegExp("^[A-Za-z0-9:._-]+$", "u");
const PATTERN_5 = new RegExp("^[A-Za-z0-9._~+/=-]+$", "u");
const PATTERN_6 = new RegExp("^[A-Za-z0-9+/]+={0,2}$", "u");
const PATTERN_7 = new RegExp("^[a-z0-9][a-z0-9._-]*$", "u");
const PATTERN_8 = new RegExp("^[a-z][a-z0-9-]*$", "u");
const PATTERN_9 = new RegExp("^[^\\x00-\\x1f\\x7f]+$", "u");
const PATTERN_10 = new RegExp("^[a-z0-9]+$", "u");
const PATTERN_11 = new RegExp("^[a-z0-9_]+$", "u");
const PATTERN_12 = new RegExp("^[A-Za-z0-9.:_-]+$", "u");
const PATTERN_13 = new RegExp("^[A-Za-z0-9.:_\\[\\]-]+:[0-9]{1,5}$", "u");

/**
 * Opaque durable identifier (docs/DOMAIN_MODEL.md section 3). Consumers MUST treat the
 * value as opaque: the schema bounds only its length and character class and never its
 * documented prefix.
 */
export function validateIdentifier(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_0 });
}

/**
 * RFC 3339 timestamp in UTC, with a mandatory trailing Z.
 */
export function validateTimestamp(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 20, maxLength: 30, pattern: PATTERN_1 });
}

/**
 * Outbound WebSocket endpoint. TLS is mandatory, so only the wss scheme is accepted
 * (docs/SECURITY.md section 15).
 */
export function validateWebsocketUrl(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 8, maxLength: 2048, pattern: PATTERN_2 });
}

/**
 * Connector release version, for example 0.1.0.
 */
export function validateSemanticVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 32, pattern: PATTERN_3 });
}

/**
 * Opaque digest or fingerprint. Bounded in length and character class only.
 */
export function validateDigest(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 8, maxLength: 128, pattern: PATTERN_4 });
}

/**
 * One-time enrolment token (docs/SECURITY.md section 6.2). Sensitive: generated models
 * wrap this value so that log, debug and default JSON representations redact it. The wire
 * value is produced only by the generated canonical encoder.
 */
export function validateEnrolmentToken(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 16, maxLength: 256, pattern: PATTERN_5 });
}

/**
 * Session-scoped route capability presented when opening a data stream (docs/SECURITY.md
 * section 9). Sensitive: it is a bearer credential and is redacted in log, debug and
 * default JSON representations.
 */
export function validateSessionCapability(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 16, maxLength: 512, pattern: PATTERN_5 });
}

/**
 * Base64-encoded SubjectPublicKeyInfo for the connector device key. The matching private
 * key never leaves the development environment and this protocol has no field capable of
 * carrying it (docs/CONNECTOR_PROTOCOL.md section 4.2).
 */
export function validatePublicKey(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 32, maxLength: 2048, pattern: PATTERN_6 });
}

/**
 * Base64-encoded signed device identity issued by the control plane.
 */
export function validateCertificate(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 32, maxLength: 8192, pattern: PATTERN_6 });
}

/**
 * Operator-assigned environment label.
 */
export function validateEnvironmentLabel(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_7 });
}

/**
 * Capability advertised by the connector build. Known version 1 values are listed in
 * x-protocol.known_capabilities; unknown values are accepted so that a newer connector is
 * classified by docs/CONNECTOR_PROTOCOL.md section 19 rather than rejected outright.
 */
export function validateConnectorCapability(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_8 });
}

/**
 * Application protocol spoken to the local development service. HTTP upgrade to WebSocket
 * is carried inside http and https (docs/CONNECTOR_PROTOCOL.md section 13).
 */
export function validateDestinationProtocol(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["http","https"] });
}

/**
 * Version 1 message type. Unknown types are rejected, never ignored. This enumeration MUST
 * equal the keys of x-protocol.messages.
 */
export function validateMessageType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["connector.registration.request","connector.registration.response","heartbeat","route.publish","route.publish.ack"] });
}

/**
 * Stable connector error class (docs/CONNECTOR_PROTOCOL.md section 21). This enumeration
 * MUST equal x-protocol.error_classes.
 */
export function validateErrorClass(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["ENROLMENT_TOKEN_INVALID","IDENTITY_REVOKED","PROTOCOL_UNSUPPORTED","PROJECT_NOT_AUTHORISED","WORKSPACE_NOT_FOUND","DESTINATION_NOT_ALLOWED","PORT_NOT_LISTENING","ROUTE_LIMIT_EXCEEDED","ROUTE_EXPIRED","STREAM_LIMIT_EXCEEDED","CONTROL_PLANE_UNAVAILABLE","UPGRADE_REQUIRED"] });
}

/**
 * Protocol version. Stage 0 accepts 1 only; any other value is rejected with
 * PROTOCOL_UNSUPPORTED and is never best-effort parsed.
 */
export function validateEnvelopeProtocolVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1 });
}

/**
 * Message payload. Its schema is selected by type. Large payloads MUST NOT travel on the
 * control channel; the frame is bounded by x-protocol.limits.max_control_frame_bytes and
 * each payload by its own x-max-bytes.
 */
export function validateEnvelopePayload(value: unknown, path: string, out: SchemaViolation[]): void {
  checkPlainObject(value, path, out);
}

/**
 * Control-channel message envelope (docs/CONNECTOR_PROTOCOL.md section 7).
 */
export function validateEnvelope(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["protocol_version", "message_id", "type", "sent_at", "connector_id", "correlation_id", "payload"], ["protocol_version", "message_id", "type", "sent_at", "payload"]);
  if (source === null) return;
  if (source["protocol_version"] !== undefined) {
    validateEnvelopeProtocolVersion(source["protocol_version"], `${path}.protocol_version`, out);
  }
  if (source["message_id"] !== undefined) {
    validateIdentifier(source["message_id"], `${path}.message_id`, out);
  }
  if (source["type"] !== undefined) {
    validateMessageType(source["type"], `${path}.type`, out);
  }
  if (source["sent_at"] !== undefined) {
    validateTimestamp(source["sent_at"], `${path}.sent_at`, out);
  }
  if (source["connector_id"] !== undefined) {
    validateIdentifier(source["connector_id"], `${path}.connector_id`, out);
  }
  if (source["correlation_id"] !== undefined) {
    validateIdentifier(source["correlation_id"], `${path}.correlation_id`, out);
  }
  if (source["payload"] !== undefined) {
    validateEnvelopePayload(source["payload"], `${path}.payload`, out);
  }
  if (matchesCondition(source["type"], ["connector.registration.request", "connector.registration.response"])) {
    forbidProperty(source, path, "connector_id", "the registration exchange precedes connector identity assignment", out);
  }
  if (matchesCondition(source["type"], ["heartbeat", "route.publish", "route.publish.ack"])) {
    requireProperty(source, path, "connector_id", "every post-enrolment message is attributed to a connector identity", out);
  }
}

/**
 * Operator-visible environment name.
 */
export function validateEnvironmentDescriptorName(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 128, pattern: PATTERN_9 });
}

/**
 * Operating system. Known version 1 values are in x-protocol.known_platforms.
 */
export function validateEnvironmentDescriptorPlatform(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 32, pattern: PATTERN_10 });
}

/**
 * CPU architecture. Known version 1 values are in x-protocol.known_architectures.
 */
export function validateEnvironmentDescriptorArchitecture(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 32, pattern: PATTERN_11 });
}

/**
 * Optional operator-assigned labels.
 */
export function validateEnvironmentDescriptorLabels(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 16, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateEnvironmentLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Development environment self-description (docs/CONNECTOR_PROTOCOL.md section 4.3,
 * docs/DOMAIN_MODEL.md section 7).
 */
export function validateEnvironmentDescriptor(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["name", "platform", "architecture", "labels"], ["name", "platform", "architecture"]);
  if (source === null) return;
  if (source["name"] !== undefined) {
    validateEnvironmentDescriptorName(source["name"], `${path}.name`, out);
  }
  if (source["platform"] !== undefined) {
    validateEnvironmentDescriptorPlatform(source["platform"], `${path}.platform`, out);
  }
  if (source["architecture"] !== undefined) {
    validateEnvironmentDescriptorArchitecture(source["architecture"], `${path}.architecture`, out);
  }
  if (source["labels"] !== undefined) {
    validateEnvironmentDescriptorLabels(source["labels"], `${path}.labels`, out);
  }
}

/**
 * Capabilities this connector build supports.
 */
export function validateConnectorDescriptorCapabilities(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 32, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateConnectorCapability(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Connector build self-description (docs/CONNECTOR_PROTOCOL.md section 4.3).
 */
export function validateConnectorDescriptor(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["version", "capabilities"], ["version", "capabilities"]);
  if (source === null) return;
  if (source["version"] !== undefined) {
    validateSemanticVersion(source["version"], `${path}.version`, out);
  }
  if (source["capabilities"] !== undefined) {
    validateConnectorDescriptorCapabilities(source["capabilities"], `${path}.capabilities`, out);
  }
}

/**
 * Registration request (docs/CONNECTOR_PROTOCOL.md section 4.3). protocol_version is
 * carried by the envelope and is not repeated here.
 */
export function validateRegistrationRequest(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["enrolment_token", "public_key", "environment", "connector"], ["enrolment_token", "public_key", "environment", "connector"]);
  if (source === null) return;
  if (source["enrolment_token"] !== undefined) {
    validateEnrolmentToken(source["enrolment_token"], `${path}.enrolment_token`, out);
  }
  if (source["public_key"] !== undefined) {
    validatePublicKey(source["public_key"], `${path}.public_key`, out);
  }
  if (source["environment"] !== undefined) {
    validateEnvironmentDescriptor(source["environment"], `${path}.environment`, out);
  }
  if (source["connector"] !== undefined) {
    validateConnectorDescriptor(source["connector"], `${path}.connector`, out);
  }
}

/**
 * Signed device identity issued in exchange for the enrolment token (docs/SECURITY.md
 * section 6.2).
 */
export function validateSignedIdentity(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["certificate", "certificate_fingerprint", "expires_at"], ["certificate", "certificate_fingerprint", "expires_at"]);
  if (source === null) return;
  if (source["certificate"] !== undefined) {
    validateCertificate(source["certificate"], `${path}.certificate`, out);
  }
  if (source["certificate_fingerprint"] !== undefined) {
    validateDigest(source["certificate_fingerprint"], `${path}.certificate_fingerprint`, out);
  }
  if (source["expires_at"] !== undefined) {
    validateTimestamp(source["expires_at"], `${path}.expires_at`, out);
  }
}

/**
 * Endpoints the connector dials outbound after enrolment (docs/CONNECTOR_PROTOCOL.md
 * section 5).
 */
export function validateControlPlaneEndpoints(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["control_url", "data_url"], ["control_url", "data_url"]);
  if (source === null) return;
  if (source["control_url"] !== undefined) {
    validateWebsocketUrl(source["control_url"], `${path}.control_url`, out);
  }
  if (source["data_url"] !== undefined) {
    validateWebsocketUrl(source["data_url"], `${path}.data_url`, out);
  }
}

/**
 * Registration response (docs/CONNECTOR_PROTOCOL.md section 4.3): connector ID, signed
 * identity, control-plane endpoints and organisation policy digest.
 */
export function validateRegistrationResponse(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["connector_id", "signed_identity", "control_plane_endpoints", "policy_digest"], ["connector_id", "signed_identity", "control_plane_endpoints", "policy_digest"]);
  if (source === null) return;
  if (source["connector_id"] !== undefined) {
    validateIdentifier(source["connector_id"], `${path}.connector_id`, out);
  }
  if (source["signed_identity"] !== undefined) {
    validateSignedIdentity(source["signed_identity"], `${path}.signed_identity`, out);
  }
  if (source["control_plane_endpoints"] !== undefined) {
    validateControlPlaneEndpoints(source["control_plane_endpoints"], `${path}.control_plane_endpoints`, out);
  }
  if (source["policy_digest"] !== undefined) {
    validateDigest(source["policy_digest"], `${path}.policy_digest`, out);
  }
}

/**
 * Normalised system load average.
 */
export function validateResourceSummaryLoad(value: unknown, path: string, out: SchemaViolation[]): void {
  checkNumber(value, path, out, { minimum: 0, maximum: 1024 });
}

/**
 * Available memory in bytes.
 */
export function validateResourceSummaryMemoryAvailableBytes(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 9007199254740991 });
}

/**
 * Optional coarse resource report. additionalProperties is false so that no sensitive
 * process detail can be attached (docs/CONNECTOR_PROTOCOL.md section 8).
 */
export function validateResourceSummary(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["load", "memory_available_bytes"], []);
  if (source === null) return;
  if (source["load"] !== undefined) {
    validateResourceSummaryLoad(source["load"], `${path}.load`, out);
  }
  if (source["memory_available_bytes"] !== undefined) {
    validateResourceSummaryMemoryAvailableBytes(source["memory_available_bytes"], `${path}.memory_available_bytes`, out);
  }
}

/**
 * Connector self-reported health. Delayed and disconnected are control-plane conclusions,
 * not self-reports.
 */
export function validateHeartbeatStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["healthy","degraded"] });
}

/**
 * Seconds since connector start.
 */
export function validateHeartbeatUptimeSeconds(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 3153600000 });
}

/**
 * Currently published routes.
 */
export function validateHeartbeatActiveRoutes(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 1024 });
}

/**
 * Currently open data streams.
 */
export function validateHeartbeatActiveStreams(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 65535 });
}

/**
 * Heartbeat payload (docs/CONNECTOR_PROTOCOL.md section 8).
 */
export function validateHeartbeat(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["status", "uptime_seconds", "version", "active_routes", "active_streams", "resource_summary"], ["status", "uptime_seconds", "version", "active_routes", "active_streams"]);
  if (source === null) return;
  if (source["status"] !== undefined) {
    validateHeartbeatStatus(source["status"], `${path}.status`, out);
  }
  if (source["uptime_seconds"] !== undefined) {
    validateHeartbeatUptimeSeconds(source["uptime_seconds"], `${path}.uptime_seconds`, out);
  }
  if (source["version"] !== undefined) {
    validateSemanticVersion(source["version"], `${path}.version`, out);
  }
  if (source["active_routes"] !== undefined) {
    validateHeartbeatActiveRoutes(source["active_routes"], `${path}.active_routes`, out);
  }
  if (source["active_streams"] !== undefined) {
    validateHeartbeatActiveStreams(source["active_streams"], `${path}.active_streams`, out);
  }
  if (source["resource_summary"] !== undefined) {
    validateResourceSummary(source["resource_summary"], `${path}.resource_summary`, out);
  }
}

/**
 * Local destination host. The connector still applies its own allow-list; schema
 * acceptance is not authorisation.
 */
export function validateRoutePublishLocalHost(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 255, pattern: PATTERN_12 });
}

/**
 * Local destination port.
 */
export function validateRoutePublishLocalPort(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 65535 });
}

/**
 * Browser sessions authorised to use the route, conventionally prefixed brs_.
 */
export function validateRoutePublishAllowedBrowserSessionIds(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 32, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateIdentifier(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Route publication request from the control plane (docs/CONNECTOR_PROTOCOL.md section
 * 11).
 */
export function validateRoutePublish(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["route_id", "project_id", "workspace_id", "local_host", "local_port", "protocol", "expires_at", "allowed_browser_session_ids"], ["route_id", "project_id", "workspace_id", "local_host", "local_port", "protocol", "expires_at", "allowed_browser_session_ids"]);
  if (source === null) return;
  if (source["route_id"] !== undefined) {
    validateIdentifier(source["route_id"], `${path}.route_id`, out);
  }
  if (source["project_id"] !== undefined) {
    validateIdentifier(source["project_id"], `${path}.project_id`, out);
  }
  if (source["workspace_id"] !== undefined) {
    validateIdentifier(source["workspace_id"], `${path}.workspace_id`, out);
  }
  if (source["local_host"] !== undefined) {
    validateRoutePublishLocalHost(source["local_host"], `${path}.local_host`, out);
  }
  if (source["local_port"] !== undefined) {
    validateRoutePublishLocalPort(source["local_port"], `${path}.local_port`, out);
  }
  if (source["protocol"] !== undefined) {
    validateDestinationProtocol(source["protocol"], `${path}.protocol`, out);
  }
  if (source["expires_at"] !== undefined) {
    validateTimestamp(source["expires_at"], `${path}.expires_at`, out);
  }
  if (source["allowed_browser_session_ids"] !== undefined) {
    validateRoutePublishAllowedBrowserSessionIds(source["allowed_browser_session_ids"], `${path}.allowed_browser_session_ids`, out);
  }
}

/**
 * Outcome of connector validation.
 */
export function validateRoutePublishAckStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["ready","rejected"] });
}

/**
 * Destination the connector actually opened, as host:port.
 */
export function validateRoutePublishAckObservedDestination(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 3, maxLength: 262, pattern: PATTERN_13 });
}

/**
 * Connector acknowledgement of a route publication request (docs/CONNECTOR_PROTOCOL.md
 * section 11). Rejection carries a stable error class and no free text, per
 * docs/SECURITY.md section 18.
 */
export function validateRoutePublishAck(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["route_id", "status", "observed_destination", "error_class"], ["route_id", "status"]);
  if (source === null) return;
  if (source["route_id"] !== undefined) {
    validateIdentifier(source["route_id"], `${path}.route_id`, out);
  }
  if (source["status"] !== undefined) {
    validateRoutePublishAckStatus(source["status"], `${path}.status`, out);
  }
  if (source["observed_destination"] !== undefined) {
    validateRoutePublishAckObservedDestination(source["observed_destination"], `${path}.observed_destination`, out);
  }
  if (source["error_class"] !== undefined) {
    validateErrorClass(source["error_class"], `${path}.error_class`, out);
  }
  if (matchesCondition(source["status"], ["ready"])) {
    requireProperty(source, path, "observed_destination", "a ready route reports the destination it opened and carries no error class", out);
    forbidProperty(source, path, "error_class", "a ready route reports the destination it opened and carries no error class", out);
  }
  if (matchesCondition(source["status"], ["rejected"])) {
    requireProperty(source, path, "error_class", "a rejected route reports a stable error class and opens no destination", out);
    forbidProperty(source, path, "observed_destination", "a rejected route reports a stable error class and opens no destination", out);
  }
}

/**
 * Header opening one tunnelled connection on the data channel (docs/CONNECTOR_PROTOCOL.md
 * section 12). The connector opens only the pre-authorised destination bound to route_id;
 * this header carries no host or port.
 */
export function validateDataStreamHeader(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["route_id", "browser_session_id", "session_capability", "stream_id", "destination_protocol", "deadline"], ["route_id", "browser_session_id", "session_capability", "stream_id", "destination_protocol", "deadline"]);
  if (source === null) return;
  if (source["route_id"] !== undefined) {
    validateIdentifier(source["route_id"], `${path}.route_id`, out);
  }
  if (source["browser_session_id"] !== undefined) {
    validateIdentifier(source["browser_session_id"], `${path}.browser_session_id`, out);
  }
  if (source["session_capability"] !== undefined) {
    validateSessionCapability(source["session_capability"], `${path}.session_capability`, out);
  }
  if (source["stream_id"] !== undefined) {
    validateIdentifier(source["stream_id"], `${path}.stream_id`, out);
  }
  if (source["destination_protocol"] !== undefined) {
    validateDestinationProtocol(source["destination_protocol"], `${path}.destination_protocol`, out);
  }
  if (source["deadline"] !== undefined) {
    validateTimestamp(source["deadline"], `${path}.deadline`, out);
  }
}
