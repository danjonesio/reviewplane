// Code generated by packages/protocol/tools/generate.ts from
// schemas/connector/v1.schema.json. DO NOT EDIT.

/**
 * Canonical encoders. Output is deterministic: properties appear in schema order, absent
 * optional properties are omitted, and numbers and strings are formatted identically to
 * the Go encoder so that both languages produce byte-identical frames.
 */

import {
  jsonInteger,
  jsonNumber,
  jsonString,
} from "../../../canonical.ts";
import type {
  ConnectorDescriptor,
  ControlPlaneEndpoints,
  DataStreamHeader,
  Envelope,
  EnvironmentDescriptor,
  Heartbeat,
  RegistrationRequest,
  RegistrationResponse,
  ResourceSummary,
  RoutePublish,
  RoutePublishAck,
  SignedIdentity,
} from "./types.ts";

/**
 * Canonically encodes a Envelope.
 */
export function encodeEnvelope(value: Envelope, payloadJson: string): string {
  const fields: string[] = [];
  fields.push(`"protocol_version":${jsonInteger(value.protocol_version)}`);
  fields.push(`"message_id":${jsonString(value.message_id)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"sent_at":${jsonString(value.sent_at)}`);
  if (value.connector_id !== undefined) {
    fields.push(`"connector_id":${jsonString(value.connector_id)}`);
  }
  if (value.correlation_id !== undefined) {
    fields.push(`"correlation_id":${jsonString(value.correlation_id)}`);
  }
  fields.push(`"payload":${payloadJson}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a EnvironmentDescriptor.
 */
export function encodeEnvironmentDescriptor(value: EnvironmentDescriptor): string {
  const fields: string[] = [];
  fields.push(`"name":${jsonString(value.name)}`);
  fields.push(`"platform":${jsonString(value.platform)}`);
  fields.push(`"architecture":${jsonString(value.architecture)}`);
  if (value.labels !== undefined) {
    fields.push(`"labels":${`[${value.labels.map((item) => jsonString(item)).join(",")}]`}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ConnectorDescriptor.
 */
export function encodeConnectorDescriptor(value: ConnectorDescriptor): string {
  const fields: string[] = [];
  fields.push(`"version":${jsonString(value.version)}`);
  fields.push(`"capabilities":${`[${value.capabilities.map((item) => jsonString(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a RegistrationRequest.
 */
export function encodeRegistrationRequest(value: RegistrationRequest): string {
  const fields: string[] = [];
  fields.push(`"enrolment_token":${jsonString(value.enrolment_token.reveal())}`);
  fields.push(`"public_key":${jsonString(value.public_key)}`);
  fields.push(`"environment":${encodeEnvironmentDescriptor(value.environment)}`);
  fields.push(`"connector":${encodeConnectorDescriptor(value.connector)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SignedIdentity.
 */
export function encodeSignedIdentity(value: SignedIdentity): string {
  const fields: string[] = [];
  fields.push(`"certificate":${jsonString(value.certificate)}`);
  fields.push(`"certificate_fingerprint":${jsonString(value.certificate_fingerprint)}`);
  fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ControlPlaneEndpoints.
 */
export function encodeControlPlaneEndpoints(value: ControlPlaneEndpoints): string {
  const fields: string[] = [];
  fields.push(`"control_url":${jsonString(value.control_url)}`);
  fields.push(`"data_url":${jsonString(value.data_url)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a RegistrationResponse.
 */
export function encodeRegistrationResponse(value: RegistrationResponse): string {
  const fields: string[] = [];
  fields.push(`"connector_id":${jsonString(value.connector_id)}`);
  fields.push(`"signed_identity":${encodeSignedIdentity(value.signed_identity)}`);
  fields.push(`"control_plane_endpoints":${encodeControlPlaneEndpoints(value.control_plane_endpoints)}`);
  fields.push(`"policy_digest":${jsonString(value.policy_digest)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ResourceSummary.
 */
export function encodeResourceSummary(value: ResourceSummary): string {
  const fields: string[] = [];
  if (value.load !== undefined) {
    fields.push(`"load":${jsonNumber(value.load)}`);
  }
  if (value.memory_available_bytes !== undefined) {
    fields.push(`"memory_available_bytes":${jsonInteger(value.memory_available_bytes)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Heartbeat.
 */
export function encodeHeartbeat(value: Heartbeat): string {
  const fields: string[] = [];
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"uptime_seconds":${jsonInteger(value.uptime_seconds)}`);
  fields.push(`"version":${jsonString(value.version)}`);
  fields.push(`"active_routes":${jsonInteger(value.active_routes)}`);
  fields.push(`"active_streams":${jsonInteger(value.active_streams)}`);
  if (value.resource_summary !== undefined) {
    fields.push(`"resource_summary":${encodeResourceSummary(value.resource_summary)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a RoutePublish.
 */
export function encodeRoutePublish(value: RoutePublish): string {
  const fields: string[] = [];
  fields.push(`"route_id":${jsonString(value.route_id)}`);
  fields.push(`"project_id":${jsonString(value.project_id)}`);
  fields.push(`"workspace_id":${jsonString(value.workspace_id)}`);
  fields.push(`"local_host":${jsonString(value.local_host)}`);
  fields.push(`"local_port":${jsonInteger(value.local_port)}`);
  fields.push(`"protocol":${jsonString(value.protocol)}`);
  fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  fields.push(`"allowed_browser_session_ids":${`[${value.allowed_browser_session_ids.map((item) => jsonString(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a RoutePublishAck.
 */
export function encodeRoutePublishAck(value: RoutePublishAck): string {
  const fields: string[] = [];
  fields.push(`"route_id":${jsonString(value.route_id)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.observed_destination !== undefined) {
    fields.push(`"observed_destination":${jsonString(value.observed_destination)}`);
  }
  if (value.error_class !== undefined) {
    fields.push(`"error_class":${jsonString(value.error_class)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a DataStreamHeader.
 */
export function encodeDataStreamHeader(value: DataStreamHeader): string {
  const fields: string[] = [];
  fields.push(`"route_id":${jsonString(value.route_id)}`);
  fields.push(`"browser_session_id":${jsonString(value.browser_session_id)}`);
  fields.push(`"session_capability":${jsonString(value.session_capability.reveal())}`);
  fields.push(`"stream_id":${jsonString(value.stream_id)}`);
  fields.push(`"destination_protocol":${jsonString(value.destination_protocol)}`);
  fields.push(`"deadline":${jsonString(value.deadline)}`);
  return `{${fields.join(",")}}`;
}
