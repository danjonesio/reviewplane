// Code generated by packages/protocol/tools/generate.ts from
// schemas/browser/v1.schema.json. DO NOT EDIT.

/**
 * Canonical encoders. Output is deterministic: properties appear in schema order, absent
 * optional properties are omitted, and numbers and strings are formatted identically to
 * the Go encoder so that both languages produce byte-identical frames.
 */

import {
  jsonBoolean,
  jsonInteger,
  jsonNumber,
  jsonString,
} from "../../../canonical.ts";
import type {
  BrowserCommand,
  BrowserCommandResult,
  ClickParams,
  CommandError,
  ControllerIdentity,
  ElementDescriptor,
  Envelope,
  NavigateParams,
  NavigationResult,
  ResizeParams,
  ScreenshotParams,
  ScreenshotResult,
  SessionAllocate,
  SessionAllocated,
  SessionLimits,
  SessionStatusReport,
  SessionTerminate,
  SnapshotParams,
  SnapshotResult,
  TypeTextParams,
  Viewport,
  WaitParams,
  WorkerHeartbeat,
  WorkerRegistration,
  WorkerRegistrationAck,
} from "./types.ts";

/**
 * Canonically encodes a Envelope.
 */
export function encodeEnvelope(value: Envelope, payloadJson: string): string {
  const fields: string[] = [];
  fields.push(`"protocol_version":${jsonInteger(value.protocol_version)}`);
  fields.push(`"message_id":${jsonString(value.message_id)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"sent_at":${jsonString(value.sent_at)}`);
  if (value.worker_id !== undefined) {
    fields.push(`"worker_id":${jsonString(value.worker_id)}`);
  }
  if (value.browser_session_id !== undefined) {
    fields.push(`"browser_session_id":${jsonString(value.browser_session_id)}`);
  }
  if (value.controller !== undefined) {
    fields.push(`"controller":${encodeControllerIdentity(value.controller)}`);
  }
  if (value.control_epoch !== undefined) {
    fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  }
  if (value.sequence !== undefined) {
    fields.push(`"sequence":${jsonInteger(value.sequence)}`);
  }
  if (value.correlation_id !== undefined) {
    fields.push(`"correlation_id":${jsonString(value.correlation_id)}`);
  }
  fields.push(`"payload":${payloadJson}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ControllerIdentity.
 */
export function encodeControllerIdentity(value: ControllerIdentity): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"id":${jsonString(value.id)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Viewport.
 */
export function encodeViewport(value: Viewport): string {
  const fields: string[] = [];
  fields.push(`"width":${jsonInteger(value.width)}`);
  fields.push(`"height":${jsonInteger(value.height)}`);
  fields.push(`"device_scale_factor":${jsonNumber(value.device_scale_factor)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionLimits.
 */
export function encodeSessionLimits(value: SessionLimits): string {
  const fields: string[] = [];
  fields.push(`"max_duration_seconds":${jsonInteger(value.max_duration_seconds)}`);
  fields.push(`"default_timeout_ms":${jsonInteger(value.default_timeout_ms)}`);
  fields.push(`"max_command_timeout_ms":${jsonInteger(value.max_command_timeout_ms)}`);
  fields.push(`"screenshot_max_bytes":${jsonInteger(value.screenshot_max_bytes)}`);
  fields.push(`"snapshot_max_nodes":${jsonInteger(value.snapshot_max_nodes)}`);
  fields.push(`"snapshot_max_bytes":${jsonInteger(value.snapshot_max_bytes)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a WorkerRegistration.
 */
export function encodeWorkerRegistration(value: WorkerRegistration): string {
  const fields: string[] = [];
  fields.push(`"worker_name":${jsonString(value.worker_name)}`);
  fields.push(`"worker_version":${jsonString(value.worker_version)}`);
  fields.push(`"browser_type":${jsonString(value.browser_type)}`);
  fields.push(`"browser_version":${jsonString(value.browser_version)}`);
  fields.push(`"capacity":${jsonInteger(value.capacity)}`);
  fields.push(`"labels":${`[${value.labels.map((item) => jsonString(item)).join(",")}]`}`);
  fields.push(`"sandbox_enabled":${jsonBoolean(value.sandbox_enabled)}`);
  fields.push(`"started_at":${jsonString(value.started_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a WorkerRegistrationAck.
 */
export function encodeWorkerRegistrationAck(value: WorkerRegistrationAck): string {
  const fields: string[] = [];
  fields.push(`"accepted":${jsonBoolean(value.accepted)}`);
  fields.push(`"assigned_projects":${`[${value.assigned_projects.map((item) => jsonString(item)).join(",")}]`}`);
  fields.push(`"session_limits":${encodeSessionLimits(value.session_limits)}`);
  fields.push(`"heartbeat_interval_seconds":${jsonInteger(value.heartbeat_interval_seconds)}`);
  if (value.error !== undefined) {
    fields.push(`"error":${encodeCommandError(value.error)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a WorkerHeartbeat.
 */
export function encodeWorkerHeartbeat(value: WorkerHeartbeat): string {
  const fields: string[] = [];
  fields.push(`"active_sessions":${jsonInteger(value.active_sessions)}`);
  fields.push(`"capacity":${jsonInteger(value.capacity)}`);
  if (value.resident_memory_mb !== undefined) {
    fields.push(`"resident_memory_mb":${jsonInteger(value.resident_memory_mb)}`);
  }
  fields.push(`"observed_at":${jsonString(value.observed_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionAllocate.
 */
export function encodeSessionAllocate(value: SessionAllocate): string {
  const fields: string[] = [];
  fields.push(`"organisation_id":${jsonString(value.organisation_id)}`);
  fields.push(`"project_id":${jsonString(value.project_id)}`);
  if (value.agent_session_id !== undefined) {
    fields.push(`"agent_session_id":${jsonString(value.agent_session_id)}`);
  }
  if (value.published_service_id !== undefined) {
    fields.push(`"published_service_id":${jsonString(value.published_service_id)}`);
  }
  if (value.service_origin !== undefined) {
    fields.push(`"service_origin":${jsonString(value.service_origin)}`);
  }
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  fields.push(`"controller":${encodeControllerIdentity(value.controller)}`);
  fields.push(`"limits":${encodeSessionLimits(value.limits)}`);
  fields.push(`"retention_class":${jsonString(value.retention_class)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionAllocated.
 */
export function encodeSessionAllocated(value: SessionAllocated): string {
  const fields: string[] = [];
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"browser_type":${jsonString(value.browser_type)}`);
  fields.push(`"browser_version":${jsonString(value.browser_version)}`);
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"allocated_at":${jsonString(value.allocated_at)}`);
  if (value.error !== undefined) {
    fields.push(`"error":${encodeCommandError(value.error)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionStatusReport.
 */
export function encodeSessionStatusReport(value: SessionStatusReport): string {
  const fields: string[] = [];
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.previous_status !== undefined) {
    fields.push(`"previous_status":${jsonString(value.previous_status)}`);
  }
  if (value.reason !== undefined) {
    fields.push(`"reason":${jsonString(value.reason)}`);
  }
  if (value.control_epoch !== undefined) {
    fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  }
  if (value.current_controller !== undefined) {
    fields.push(`"current_controller":${encodeControllerIdentity(value.current_controller)}`);
  }
  fields.push(`"occurred_at":${jsonString(value.occurred_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionTerminate.
 */
export function encodeSessionTerminate(value: SessionTerminate): string {
  const fields: string[] = [];
  fields.push(`"reason":${jsonString(value.reason)}`);
  if (value.detail !== undefined) {
    fields.push(`"detail":${jsonString(value.detail)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a NavigateParams.
 */
export function encodeNavigateParams(value: NavigateParams): string {
  const fields: string[] = [];
  fields.push(`"url":${jsonString(value.url)}`);
  fields.push(`"wait_until":${jsonString(value.wait_until)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ClickParams.
 */
export function encodeClickParams(value: ClickParams): string {
  const fields: string[] = [];
  fields.push(`"snapshot_id":${jsonString(value.snapshot_id)}`);
  fields.push(`"ref":${jsonString(value.ref)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a TypeTextParams.
 */
export function encodeTypeTextParams(value: TypeTextParams): string {
  const fields: string[] = [];
  fields.push(`"snapshot_id":${jsonString(value.snapshot_id)}`);
  fields.push(`"ref":${jsonString(value.ref)}`);
  fields.push(`"text":${jsonString(value.text)}`);
  if (value.submit !== undefined) {
    fields.push(`"submit":${jsonBoolean(value.submit)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ResizeParams.
 */
export function encodeResizeParams(value: ResizeParams): string {
  const fields: string[] = [];
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a WaitParams.
 */
export function encodeWaitParams(value: WaitParams): string {
  const fields: string[] = [];
  fields.push(`"condition":${jsonString(value.condition)}`);
  if (value.url_pattern !== undefined) {
    fields.push(`"url_pattern":${jsonString(value.url_pattern)}`);
  }
  if (value.selector !== undefined) {
    fields.push(`"selector":${jsonString(value.selector)}`);
  }
  if (value.text !== undefined) {
    fields.push(`"text":${jsonString(value.text)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SnapshotParams.
 */
export function encodeSnapshotParams(value: SnapshotParams): string {
  const fields: string[] = [];
  if (value.max_nodes !== undefined) {
    fields.push(`"max_nodes":${jsonInteger(value.max_nodes)}`);
  }
  if (value.max_bytes !== undefined) {
    fields.push(`"max_bytes":${jsonInteger(value.max_bytes)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ScreenshotParams.
 */
export function encodeScreenshotParams(value: ScreenshotParams): string {
  const fields: string[] = [];
  fields.push(`"full_page":${jsonBoolean(value.full_page)}`);
  fields.push(`"persist":${jsonBoolean(value.persist)}`);
  fields.push(`"purpose":${jsonString(value.purpose)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a BrowserCommand.
 */
export function encodeBrowserCommand(value: BrowserCommand): string {
  const fields: string[] = [];
  fields.push(`"command":${jsonString(value.command)}`);
  fields.push(`"timeout_ms":${jsonInteger(value.timeout_ms)}`);
  if (value.navigate !== undefined) {
    fields.push(`"navigate":${encodeNavigateParams(value.navigate)}`);
  }
  if (value.click !== undefined) {
    fields.push(`"click":${encodeClickParams(value.click)}`);
  }
  if (value.type_text !== undefined) {
    fields.push(`"type_text":${encodeTypeTextParams(value.type_text)}`);
  }
  if (value.resize !== undefined) {
    fields.push(`"resize":${encodeResizeParams(value.resize)}`);
  }
  if (value.wait !== undefined) {
    fields.push(`"wait":${encodeWaitParams(value.wait)}`);
  }
  if (value.snapshot !== undefined) {
    fields.push(`"snapshot":${encodeSnapshotParams(value.snapshot)}`);
  }
  if (value.take_screenshot !== undefined) {
    fields.push(`"take_screenshot":${encodeScreenshotParams(value.take_screenshot)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a CommandError.
 */
export function encodeCommandError(value: CommandError): string {
  const fields: string[] = [];
  fields.push(`"code":${jsonString(value.code)}`);
  fields.push(`"message":${jsonString(value.message)}`);
  fields.push(`"retryable":${jsonBoolean(value.retryable)}`);
  if (value.current_epoch !== undefined) {
    fields.push(`"current_epoch":${jsonInteger(value.current_epoch)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a NavigationResult.
 */
export function encodeNavigationResult(value: NavigationResult): string {
  const fields: string[] = [];
  fields.push(`"url":${jsonString(value.url)}`);
  if (value.http_status !== undefined) {
    fields.push(`"http_status":${jsonInteger(value.http_status)}`);
  }
  fields.push(`"redirected":${jsonBoolean(value.redirected)}`);
  fields.push(`"title":${jsonString(value.title)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ElementDescriptor.
 */
export function encodeElementDescriptor(value: ElementDescriptor): string {
  const fields: string[] = [];
  fields.push(`"ref":${jsonString(value.ref)}`);
  fields.push(`"role":${jsonString(value.role)}`);
  if (value.name !== undefined) {
    fields.push(`"name":${jsonString(value.name)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SnapshotResult.
 */
export function encodeSnapshotResult(value: SnapshotResult): string {
  const fields: string[] = [];
  fields.push(`"snapshot_id":${jsonString(value.snapshot_id)}`);
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"node_count":${jsonInteger(value.node_count)}`);
  fields.push(`"truncated":${jsonBoolean(value.truncated)}`);
  fields.push(`"text":${jsonString(value.text)}`);
  fields.push(`"elements":${`[${value.elements.map((item) => encodeElementDescriptor(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ScreenshotResult.
 */
export function encodeScreenshotResult(value: ScreenshotResult): string {
  const fields: string[] = [];
  fields.push(`"artefact_id":${jsonString(value.artefact_id)}`);
  fields.push(`"sha256":${jsonString(value.sha256)}`);
  fields.push(`"size_bytes":${jsonInteger(value.size_bytes)}`);
  fields.push(`"content_type":${jsonString(value.content_type)}`);
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"full_page":${jsonBoolean(value.full_page)}`);
  fields.push(`"captured_at":${jsonString(value.captured_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a BrowserCommandResult.
 */
export function encodeBrowserCommandResult(value: BrowserCommandResult): string {
  const fields: string[] = [];
  fields.push(`"ok":${jsonBoolean(value.ok)}`);
  fields.push(`"command":${jsonString(value.command)}`);
  fields.push(`"sequence":${jsonInteger(value.sequence)}`);
  fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  fields.push(`"duration_ms":${jsonInteger(value.duration_ms)}`);
  fields.push(`"trust":${jsonString(value.trust)}`);
  fields.push(`"instruction_policy":${jsonString(value.instruction_policy)}`);
  if (value.error !== undefined) {
    fields.push(`"error":${encodeCommandError(value.error)}`);
  }
  if (value.navigation !== undefined) {
    fields.push(`"navigation":${encodeNavigationResult(value.navigation)}`);
  }
  if (value.snapshot !== undefined) {
    fields.push(`"snapshot":${encodeSnapshotResult(value.snapshot)}`);
  }
  if (value.screenshot !== undefined) {
    fields.push(`"screenshot":${encodeScreenshotResult(value.screenshot)}`);
  }
  if (value.viewport !== undefined) {
    fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  }
  return `{${fields.join(",")}}`;
}
