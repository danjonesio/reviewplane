// Code generated by packages/protocol/tools/generate.ts from
// schemas/mcp/v1.schema.json. DO NOT EDIT.

/**
 * Decoders build typed values from an already validated JSON tree. Calling a decoder on a
 * value that has not passed its validator is a programming error; the frame entry points
 * in frame.ts always validate first.
 */

import type {
  ActorReference,
  ActorType,
  AgentCapability,
  AgentFindingStatus,
  AgentReviewStatus,
  AgentSessionState,
  AgentSessionStatusInput,
  AgentSessionStatusResult,
  AnnotationGeometry,
  AnnotationType,
  AnnotationView,
  ArtefactKind,
  ArtefactLink,
  BrowserSessionView,
  BrowserTakeScreenshotInput,
  BrowserTakeScreenshotResult,
  ClientCapabilities,
  ClientIdentity,
  CommentView,
  ContentRectangle,
  Envelope,
  ErrorClass,
  ErrorDetails,
  FindingAddCommentInput,
  FindingAddCommentResult,
  FindingClaimInput,
  FindingGetInput,
  FindingGetResult,
  FindingInclude,
  FindingMutationResult,
  FindingSeverity,
  FindingSource,
  FindingStatus,
  FindingSubmitVerificationInput,
  FindingSubmitVerificationResult,
  FindingUpdateStatusInput,
  FindingView,
  InstructionPolicy,
  MediaType,
  MessageType,
  PolicySummary,
  ProjectCurrentInput,
  ProjectCurrentResult,
  ProjectReference,
  ReviewClaimInput,
  ReviewGetInput,
  ReviewGetResult,
  ReviewInclude,
  ReviewMutationResult,
  ReviewStatus,
  ReviewUpdateStatusInput,
  ReviewView,
  ScreenshotPurpose,
  ScrollPosition,
  ServerCapabilities,
  SessionInclude,
  SessionInitialisationRequest,
  SessionInitialisationResult,
  ToolError,
  ToolRefusal,
  TrustLabel,
  VerificationChecks,
  VerificationStatus,
  VerificationView,
  Viewport,
  Warning,
  WarningCode,
  WorkspaceReference,
} from "./types.ts";

/**
 * Decodes a validated ActorReference.
 */
export function decodeActorReference(value: unknown): ActorReference {
  const source = value as Record<string, unknown>;
  return {
    type: source["type"] as ActorType,
    ...(source["id"] === undefined ? {} : { id: source["id"] as string }),
    ...(source["display"] === undefined ? {} : { display: source["display"] as string }),
  };
}

/**
 * Decodes a validated Viewport.
 */
export function decodeViewport(value: unknown): Viewport {
  const source = value as Record<string, unknown>;
  return {
    width: source["width"] as number,
    height: source["height"] as number,
    device_scale_factor: source["device_scale_factor"] as number,
  };
}

/**
 * Decodes a validated ScrollPosition.
 */
export function decodeScrollPosition(value: unknown): ScrollPosition {
  const source = value as Record<string, unknown>;
  return {
    x: source["x"] as number,
    y: source["y"] as number,
  };
}

/**
 * Decodes a validated ContentRectangle.
 */
export function decodeContentRectangle(value: unknown): ContentRectangle {
  const source = value as Record<string, unknown>;
  return {
    width_px: source["width_px"] as number,
    height_px: source["height_px"] as number,
  };
}

/**
 * Decodes a validated AnnotationGeometry.
 */
export function decodeAnnotationGeometry(value: unknown): AnnotationGeometry {
  const source = value as Record<string, unknown>;
  return {
    x: source["x"] as number,
    y: source["y"] as number,
    ...(source["width"] === undefined ? {} : { width: source["width"] as number }),
    ...(source["height"] === undefined ? {} : { height: source["height"] as number }),
    ...(source["x2"] === undefined ? {} : { x2: source["x2"] as number }),
    ...(source["y2"] === undefined ? {} : { y2: source["y2"] as number }),
  };
}

/**
 * Decodes a validated ProjectReference.
 */
export function decodeProjectReference(value: unknown): ProjectReference {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    slug: source["slug"] as string,
    ...(source["name"] === undefined ? {} : { name: source["name"] as string }),
  };
}

/**
 * Decodes a validated WorkspaceReference.
 */
export function decodeWorkspaceReference(value: unknown): WorkspaceReference {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    ...(source["root_path"] === undefined ? {} : { root_path: source["root_path"] as string }),
    branch: source["branch"] as string,
    head_commit: source["head_commit"] as string,
    ...(source["dirty"] === undefined ? {} : { dirty: source["dirty"] as boolean }),
  };
}

/**
 * Decodes a validated PolicySummary.
 */
export function decodePolicySummary(value: unknown): PolicySummary {
  const source = value as Record<string, unknown>;
  return {
    agent_may_accept_findings: source["agent_may_accept_findings"] as boolean,
    verification_required: source["verification_required"] as boolean,
    secret_tools_available: source["secret_tools_available"] as boolean,
    required_viewports: (source["required_viewports"] as unknown[]).map((item) => item as string),
  };
}

/**
 * Decodes a validated ServerCapabilities.
 */
export function decodeServerCapabilities(value: unknown): ServerCapabilities {
  const source = value as Record<string, unknown>;
  return {
    browser_live: source["browser_live"] as boolean,
    image_resources: source["image_resources"] as boolean,
    human_takeover: source["human_takeover"] as boolean,
    review_inbox: source["review_inbox"] as boolean,
    managed_messages: source["managed_messages"] as boolean,
    session_resume: source["session_resume"] as boolean,
  };
}

/**
 * Decodes a validated ClientCapabilities.
 */
export function decodeClientCapabilities(value: unknown): ClientCapabilities {
  const source = value as Record<string, unknown>;
  return {
    ...(source["resources"] === undefined ? {} : { resources: source["resources"] as boolean }),
    ...(source["image_content"] === undefined ? {} : { image_content: source["image_content"] as boolean }),
    ...(source["managed_messages"] === undefined ? {} : { managed_messages: source["managed_messages"] as boolean }),
    ...(source["session_resume"] === undefined ? {} : { session_resume: source["session_resume"] as boolean }),
  };
}

/**
 * Decodes a validated Warning.
 */
export function decodeWarning(value: unknown): Warning {
  const source = value as Record<string, unknown>;
  return {
    code: source["code"] as WarningCode,
    message: source["message"] as string,
    ...(source["detail"] === undefined ? {} : { detail: source["detail"] as string }),
  };
}

/**
 * Decodes a validated ErrorDetails.
 */
export function decodeErrorDetails(value: unknown): ErrorDetails {
  const source = value as Record<string, unknown>;
  return {
    ...(source["current_version"] === undefined ? {} : { current_version: source["current_version"] as number }),
    ...(source["expected_version"] === undefined ? {} : { expected_version: source["expected_version"] as number }),
    ...(source["current_epoch"] === undefined ? {} : { current_epoch: source["current_epoch"] as number }),
    ...(source["field"] === undefined ? {} : { field: source["field"] as string }),
    ...(source["candidates"] === undefined ? {} : { candidates: (source["candidates"] as unknown[]).map((item) => decodeProjectReference(item)) }),
    ...(source["required_evidence"] === undefined ? {} : { required_evidence: (source["required_evidence"] as unknown[]).map((item) => item as string) }),
    ...(source["allowed_transitions"] === undefined ? {} : { allowed_transitions: (source["allowed_transitions"] as unknown[]).map((item) => item as string) }),
    ...(source["retry_after_ms"] === undefined ? {} : { retry_after_ms: source["retry_after_ms"] as number }),
  };
}

/**
 * Decodes a validated ToolError.
 */
export function decodeToolError(value: unknown): ToolError {
  const source = value as Record<string, unknown>;
  return {
    code: source["code"] as ErrorClass,
    message: source["message"] as string,
    retryable: source["retryable"] as boolean,
    ...(source["details"] === undefined ? {} : { details: decodeErrorDetails(source["details"]) }),
  };
}

/**
 * Decodes a validated ArtefactLink.
 */
export function decodeArtefactLink(value: unknown): ArtefactLink {
  const source = value as Record<string, unknown>;
  return {
    artefact_id: source["artefact_id"] as string,
    kind: source["kind"] as ArtefactKind,
    ...(source["role"] === undefined ? {} : { role: source["role"] as string }),
    resource_uri: source["resource_uri"] as string,
    ...(source["content_path"] === undefined ? {} : { content_path: source["content_path"] as string }),
    ...(source["expires_at"] === undefined ? {} : { expires_at: source["expires_at"] as string }),
    content_type: source["content_type"] as MediaType,
    ...(source["sha256"] === undefined ? {} : { sha256: source["sha256"] as string }),
    ...(source["size_bytes"] === undefined ? {} : { size_bytes: source["size_bytes"] as number }),
    ...(source["content_rectangle"] === undefined ? {} : { content_rectangle: decodeContentRectangle(source["content_rectangle"]) }),
    trust: source["trust"] as TrustLabel,
    instruction_policy: source["instruction_policy"] as InstructionPolicy,
  };
}

/**
 * Decodes a validated AnnotationView.
 */
export function decodeAnnotationView(value: unknown): AnnotationView {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    artefact_id: source["artefact_id"] as string,
    type: source["type"] as AnnotationType,
    geometry: decodeAnnotationGeometry(source["geometry"]),
    label: source["label"] as string,
    ...(source["marker_number"] === undefined ? {} : { marker_number: source["marker_number"] as number }),
  };
}

/**
 * Decodes a validated CommentView.
 */
export function decodeCommentView(value: unknown): CommentView {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    finding_id: source["finding_id"] as string,
    body: source["body"] as string,
    author: decodeActorReference(source["author"]),
    created_at: source["created_at"] as string,
  };
}

/**
 * Decodes a validated VerificationChecks.
 */
export function decodeVerificationChecks(value: unknown): VerificationChecks {
  const source = value as Record<string, unknown>;
  return {
    reproduced_before: source["reproduced_before"] as boolean,
    console_errors_reviewed: source["console_errors_reviewed"] as boolean,
    network_failures_reviewed: source["network_failures_reviewed"] as boolean,
    ...(source["accessibility_checked"] === undefined ? {} : { accessibility_checked: source["accessibility_checked"] as boolean }),
  };
}

/**
 * Decodes a validated VerificationView.
 */
export function decodeVerificationView(value: unknown): VerificationView {
  const source = value as Record<string, unknown>;
  return {
    verification_id: source["verification_id"] as string,
    finding_id: source["finding_id"] as string,
    status: source["status"] as VerificationStatus,
    summary: source["summary"] as string,
    branch: source["branch"] as string,
    commit: source["commit"] as string,
    tested_viewports: (source["tested_viewports"] as unknown[]).map((item) => decodeViewport(item)),
    checks: decodeVerificationChecks(source["checks"]),
    ...(source["artefacts"] === undefined ? {} : { artefacts: (source["artefacts"] as unknown[]).map((item) => decodeArtefactLink(item)) }),
    submitted_by: decodeActorReference(source["submitted_by"]),
    submitted_at: source["submitted_at"] as string,
  };
}

/**
 * Decodes a validated ReviewView.
 */
export function decodeReviewView(value: unknown): ReviewView {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    project_id: source["project_id"] as string,
    slug: source["slug"] as string,
    title: source["title"] as string,
    ...(source["description"] === undefined ? {} : { description: source["description"] as string }),
    status: source["status"] as ReviewStatus,
    version: source["version"] as number,
    captured_branch: source["captured_branch"] as string,
    captured_commit: source["captured_commit"] as string,
    captured_workspace_id: source["captured_workspace_id"] as string,
    ...(source["source_browser_session_id"] === undefined ? {} : { source_browser_session_id: source["source_browser_session_id"] as string }),
    ...(source["claimed_by"] === undefined ? {} : { claimed_by: decodeActorReference(source["claimed_by"]) }),
    finding_count: source["finding_count"] as number,
    resource_uri: source["resource_uri"] as string,
    created_at: source["created_at"] as string,
    updated_at: source["updated_at"] as string,
  };
}

/**
 * Decodes a validated FindingView.
 */
export function decodeFindingView(value: unknown): FindingView {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    review_id: source["review_id"] as string,
    title: source["title"] as string,
    ...(source["description"] === undefined ? {} : { description: source["description"] as string }),
    severity: source["severity"] as FindingSeverity,
    status: source["status"] as FindingStatus,
    source: source["source"] as FindingSource,
    version: source["version"] as number,
    url: source["url"] as string,
    viewport: decodeViewport(source["viewport"]),
    scroll_position: decodeScrollPosition(source["scroll_position"]),
    captured_commit: source["captured_commit"] as string,
    screenshot_artefact_id: source["screenshot_artefact_id"] as string,
    ...(source["acceptance_criteria"] === undefined ? {} : { acceptance_criteria: source["acceptance_criteria"] as string }),
    ...(source["claimed_by"] === undefined ? {} : { claimed_by: decodeActorReference(source["claimed_by"]) }),
    ...(source["annotation_count"] === undefined ? {} : { annotation_count: source["annotation_count"] as number }),
    resource_uri: source["resource_uri"] as string,
    untrusted_fields: (source["untrusted_fields"] as unknown[]).map((item) => item as string),
  };
}

/**
 * Decodes a validated BrowserSessionView.
 */
export function decodeBrowserSessionView(value: unknown): BrowserSessionView {
  const source = value as Record<string, unknown>;
  return {
    id: source["id"] as string,
    status: source["status"] as string,
    control_epoch: source["control_epoch"] as number,
    ...(source["controller"] === undefined ? {} : { controller: decodeActorReference(source["controller"]) }),
    viewport: decodeViewport(source["viewport"]),
    ...(source["service_origin"] === undefined ? {} : { service_origin: source["service_origin"] as string }),
  };
}

/**
 * Decodes a validated SessionInitialisationRequest.
 */
export function decodeSessionInitialisationRequest(value: unknown): SessionInitialisationRequest {
  const source = value as Record<string, unknown>;
  return {
    client: decodeClientIdentity(source["client"]),
    ...(source["project_hint"] === undefined ? {} : { project_hint: source["project_hint"] as string }),
    ...(source["workspace_hint"] === undefined ? {} : { workspace_hint: source["workspace_hint"] as string }),
    ...(source["capabilities"] === undefined ? {} : { capabilities: decodeClientCapabilities(source["capabilities"]) }),
  };
}

/**
 * Decodes a validated ClientIdentity.
 */
export function decodeClientIdentity(value: unknown): ClientIdentity {
  const source = value as Record<string, unknown>;
  return {
    name: source["name"] as string,
    version: source["version"] as string,
  };
}

/**
 * Decodes a validated SessionInitialisationResult.
 */
export function decodeSessionInitialisationResult(value: unknown): SessionInitialisationResult {
  const source = value as Record<string, unknown>;
  return {
    agent_session_id: source["agent_session_id"] as string,
    project: decodeProjectReference(source["project"]),
    ...(source["workspace"] === undefined ? {} : { workspace: decodeWorkspaceReference(source["workspace"]) }),
    capabilities: decodeServerCapabilities(source["capabilities"]),
    granted_capabilities: (source["granted_capabilities"] as unknown[]).map((item) => item as AgentCapability),
    ...(source["expires_at"] === undefined ? {} : { expires_at: source["expires_at"] as string }),
  };
}

/**
 * Decodes a validated ProjectCurrentInput.
 */
export function decodeProjectCurrentInput(value: unknown): ProjectCurrentInput {
  const source = value as Record<string, unknown>;
  return {
    ...(source["refresh_source_state"] === undefined ? {} : { refresh_source_state: source["refresh_source_state"] as boolean }),
  };
}

/**
 * Decodes a validated AgentSessionStatusInput.
 */
export function decodeAgentSessionStatusInput(value: unknown): AgentSessionStatusInput {
  const source = value as Record<string, unknown>;
  return {
    ...(source["include"] === undefined ? {} : { include: (source["include"] as unknown[]).map((item) => item as SessionInclude) }),
  };
}

/**
 * Decodes a validated ReviewGetInput.
 */
export function decodeReviewGetInput(value: unknown): ReviewGetInput {
  const source = value as Record<string, unknown>;
  return {
    review: source["review"] as string,
    ...(source["include"] === undefined ? {} : { include: (source["include"] as unknown[]).map((item) => item as ReviewInclude) }),
    ...(source["findings_limit"] === undefined ? {} : { findings_limit: source["findings_limit"] as number }),
    ...(source["findings_cursor"] === undefined ? {} : { findings_cursor: source["findings_cursor"] as string }),
  };
}

/**
 * Decodes a validated ReviewClaimInput.
 */
export function decodeReviewClaimInput(value: unknown): ReviewClaimInput {
  const source = value as Record<string, unknown>;
  return {
    review_id: source["review_id"] as string,
    expected_version: source["expected_version"] as number,
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated ReviewUpdateStatusInput.
 */
export function decodeReviewUpdateStatusInput(value: unknown): ReviewUpdateStatusInput {
  const source = value as Record<string, unknown>;
  return {
    review_id: source["review_id"] as string,
    expected_version: source["expected_version"] as number,
    status: source["status"] as AgentReviewStatus,
    ...(source["reason"] === undefined ? {} : { reason: source["reason"] as string }),
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated FindingGetInput.
 */
export function decodeFindingGetInput(value: unknown): FindingGetInput {
  const source = value as Record<string, unknown>;
  return {
    finding_id: source["finding_id"] as string,
    ...(source["include"] === undefined ? {} : { include: (source["include"] as unknown[]).map((item) => item as FindingInclude) }),
  };
}

/**
 * Decodes a validated FindingClaimInput.
 */
export function decodeFindingClaimInput(value: unknown): FindingClaimInput {
  const source = value as Record<string, unknown>;
  return {
    finding_id: source["finding_id"] as string,
    expected_version: source["expected_version"] as number,
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated FindingUpdateStatusInput.
 */
export function decodeFindingUpdateStatusInput(value: unknown): FindingUpdateStatusInput {
  const source = value as Record<string, unknown>;
  return {
    finding_id: source["finding_id"] as string,
    expected_version: source["expected_version"] as number,
    status: source["status"] as AgentFindingStatus,
    ...(source["resolution_note"] === undefined ? {} : { resolution_note: source["resolution_note"] as string }),
    ...(source["reason"] === undefined ? {} : { reason: source["reason"] as string }),
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated FindingAddCommentInput.
 */
export function decodeFindingAddCommentInput(value: unknown): FindingAddCommentInput {
  const source = value as Record<string, unknown>;
  return {
    finding_id: source["finding_id"] as string,
    body: source["body"] as string,
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated FindingSubmitVerificationInput.
 */
export function decodeFindingSubmitVerificationInput(value: unknown): FindingSubmitVerificationInput {
  const source = value as Record<string, unknown>;
  return {
    finding_id: source["finding_id"] as string,
    ...(source["expected_version"] === undefined ? {} : { expected_version: source["expected_version"] as number }),
    summary: source["summary"] as string,
    branch: source["branch"] as string,
    commit: source["commit"] as string,
    tested_viewports: (source["tested_viewports"] as unknown[]).map((item) => decodeViewport(item)),
    checks: decodeVerificationChecks(source["checks"]),
    artefact_ids: (source["artefact_ids"] as unknown[]).map((item) => item as string),
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated BrowserTakeScreenshotInput.
 */
export function decodeBrowserTakeScreenshotInput(value: unknown): BrowserTakeScreenshotInput {
  const source = value as Record<string, unknown>;
  return {
    browser_session_id: source["browser_session_id"] as string,
    control_epoch: source["control_epoch"] as number,
    ...(source["full_page"] === undefined ? {} : { full_page: source["full_page"] as boolean }),
    purpose: source["purpose"] as ScreenshotPurpose,
    idempotency_key: source["idempotency_key"] as string,
  };
}

/**
 * Decodes a validated ProjectCurrentResult.
 */
export function decodeProjectCurrentResult(value: unknown): ProjectCurrentResult {
  const source = value as Record<string, unknown>;
  return {
    project: decodeProjectReference(source["project"]),
    ...(source["workspace"] === undefined ? {} : { workspace: decodeWorkspaceReference(source["workspace"]) }),
    policy: decodePolicySummary(source["policy"]),
  };
}

/**
 * Decodes a validated AgentSessionStatusResult.
 */
export function decodeAgentSessionStatusResult(value: unknown): AgentSessionStatusResult {
  const source = value as Record<string, unknown>;
  return {
    agent_session_id: source["agent_session_id"] as string,
    status: source["status"] as AgentSessionState,
    ...(source["client"] === undefined ? {} : { client: decodeClientIdentity(source["client"]) }),
    project: decodeProjectReference(source["project"]),
    ...(source["workspace"] === undefined ? {} : { workspace: decodeWorkspaceReference(source["workspace"]) }),
    capabilities: decodeServerCapabilities(source["capabilities"]),
    granted_capabilities: (source["granted_capabilities"] as unknown[]).map((item) => item as AgentCapability),
    ...(source["browser_sessions"] === undefined ? {} : { browser_sessions: (source["browser_sessions"] as unknown[]).map((item) => decodeBrowserSessionView(item)) }),
    ...(source["expires_at"] === undefined ? {} : { expires_at: source["expires_at"] as string }),
  };
}

/**
 * Decodes a validated ReviewGetResult.
 */
export function decodeReviewGetResult(value: unknown): ReviewGetResult {
  const source = value as Record<string, unknown>;
  return {
    review: decodeReviewView(source["review"]),
    ...(source["findings"] === undefined ? {} : { findings: (source["findings"] as unknown[]).map((item) => decodeFindingView(item)) }),
    ...(source["findings_next_cursor"] === undefined ? {} : { findings_next_cursor: source["findings_next_cursor"] as string }),
    ...(source["artefact_links"] === undefined ? {} : { artefact_links: (source["artefact_links"] as unknown[]).map((item) => decodeArtefactLink(item)) }),
  };
}

/**
 * Decodes a validated ReviewMutationResult.
 */
export function decodeReviewMutationResult(value: unknown): ReviewMutationResult {
  const source = value as Record<string, unknown>;
  return {
    review: decodeReviewView(source["review"]),
    ...(source["previous_status"] === undefined ? {} : { previous_status: source["previous_status"] as ReviewStatus }),
  };
}

/**
 * Decodes a validated FindingGetResult.
 */
export function decodeFindingGetResult(value: unknown): FindingGetResult {
  const source = value as Record<string, unknown>;
  return {
    finding: decodeFindingView(source["finding"]),
    ...(source["annotations"] === undefined ? {} : { annotations: (source["annotations"] as unknown[]).map((item) => decodeAnnotationView(item)) }),
    ...(source["comments"] === undefined ? {} : { comments: (source["comments"] as unknown[]).map((item) => decodeCommentView(item)) }),
    ...(source["artefact_links"] === undefined ? {} : { artefact_links: (source["artefact_links"] as unknown[]).map((item) => decodeArtefactLink(item)) }),
    ...(source["latest_verification"] === undefined ? {} : { latest_verification: decodeVerificationView(source["latest_verification"]) }),
  };
}

/**
 * Decodes a validated FindingMutationResult.
 */
export function decodeFindingMutationResult(value: unknown): FindingMutationResult {
  const source = value as Record<string, unknown>;
  return {
    finding: decodeFindingView(source["finding"]),
    ...(source["previous_status"] === undefined ? {} : { previous_status: source["previous_status"] as FindingStatus }),
  };
}

/**
 * Decodes a validated FindingAddCommentResult.
 */
export function decodeFindingAddCommentResult(value: unknown): FindingAddCommentResult {
  const source = value as Record<string, unknown>;
  return {
    comment: decodeCommentView(source["comment"]),
  };
}

/**
 * Decodes a validated FindingSubmitVerificationResult.
 */
export function decodeFindingSubmitVerificationResult(value: unknown): FindingSubmitVerificationResult {
  const source = value as Record<string, unknown>;
  return {
    verification: decodeVerificationView(source["verification"]),
    finding: decodeFindingView(source["finding"]),
  };
}

/**
 * Decodes a validated BrowserTakeScreenshotResult.
 */
export function decodeBrowserTakeScreenshotResult(value: unknown): BrowserTakeScreenshotResult {
  const source = value as Record<string, unknown>;
  return {
    artefact: decodeArtefactLink(source["artefact"]),
    browser_session_id: source["browser_session_id"] as string,
    captured_at: source["captured_at"] as string,
    viewport: decodeViewport(source["viewport"]),
    full_page: source["full_page"] as boolean,
    ...(source["url"] === undefined ? {} : { url: source["url"] as string }),
  };
}

/**
 * Decodes a validated ToolRefusal.
 */
export function decodeToolRefusal(value: unknown): ToolRefusal {
  const source = value as Record<string, unknown>;
  return {
    protocol_version: source["protocol_version"] as number,
    ok: source["ok"] as boolean,
    request_id: source["request_id"] as string,
    type: source["type"] as MessageType,
    error: decodeToolError(source["error"]),
    trust: source["trust"] as TrustLabel,
    instruction_policy: source["instruction_policy"] as InstructionPolicy,
    ...(source["warnings"] === undefined ? {} : { warnings: (source["warnings"] as unknown[]).map((item) => decodeWarning(item)) }),
  };
}

/**
 * Decodes a validated Envelope.
 */
export function decodeEnvelope(value: unknown): Envelope {
  const source = value as Record<string, unknown>;
  return {
    protocol_version: source["protocol_version"] as number,
    ok: source["ok"] as boolean,
    request_id: source["request_id"] as string,
    type: source["type"] as MessageType,
    trust: source["trust"] as TrustLabel,
    instruction_policy: source["instruction_policy"] as InstructionPolicy,
    ...(source["warnings"] === undefined ? {} : { warnings: (source["warnings"] as unknown[]).map((item) => decodeWarning(item)) }),
  };
}
