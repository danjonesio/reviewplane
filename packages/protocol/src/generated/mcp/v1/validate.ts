// Code generated by packages/protocol/tools/generate.ts from
// schemas/mcp/v1.schema.json. DO NOT EDIT.

import type { SchemaViolation } from "./types.ts";
import {
  checkArray,
  checkBoolean,
  checkInteger,
  checkNumber,
  checkObject,
  checkPlainObject,
  checkString,
  matchesCondition,
  requireProperty,
} from "../../../validate-runtime.ts";

const PATTERN_0 = new RegExp("^[A-Za-z0-9_-]+$", "u");
const PATTERN_1 = new RegExp("^[A-Za-z0-9._:-]+$", "u");
const PATTERN_2 = new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$", "u");
const PATTERN_3 = new RegExp("^[a-z0-9][a-z0-9-]{0,62}[a-z0-9]$|^[a-z0-9]$", "u");
const PATTERN_4 = new RegExp("^[A-Za-z0-9._-]+$", "u");
const PATTERN_5 = new RegExp("^[^\\u0000-\\u001f\\u007f]+$", "u");
const PATTERN_6 = new RegExp("^[A-Za-z0-9 ._-]+$", "u");
const PATTERN_7 = new RegExp("^[A-Za-z0-9 ._+-]+$", "u");
const PATTERN_8 = new RegExp("^[^\\u0000-\\u0008\\u000b\\u000c\\u000e-\\u001f\\u007f]+$", "u");
const PATTERN_9 = new RegExp("^[!-~]+$", "u");
const PATTERN_10 = new RegExp("^(review|finding|artefact|screenshot)://[A-Za-z0-9._~/-]{1,300}$", "u");
const PATTERN_11 = new RegExp("^/[!-~]*$", "u");
const PATTERN_12 = new RegExp("^[0-9a-f]{7,64}$", "u");
const PATTERN_13 = new RegExp("^[0-9a-f]{64}$", "u");
const PATTERN_14 = new RegExp("^[A-Za-z0-9_.\\[\\]-]+$", "u");
const PATTERN_15 = new RegExp("^[0-9A-Fa-f:.]{1,45}$", "u");
const PATTERN_16 = new RegExp("^[a-zA-Z0-9][a-zA-Z0-9 ()@x.,:_-]{0,119}$", "u");
const PATTERN_17 = new RegExp("^https://[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?\\.[a-z0-9.-]{1,180}/$", "u");
const PATTERN_18 = new RegExp("^(\\[[0-9A-Fa-f:.]{1,45}\\]|[0-9A-Fa-f:.]{1,45}):[0-9]{1,5}$", "u");

/**
 * Opaque durable identifier (docs/DOMAIN_MODEL.md section 3). Consumers MUST treat the
 * value as opaque: the schema bounds only its length and character class and never its
 * documented prefix.
 */
export function validateIdentifier(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_0 });
}

/**
 * Identifier of the tool call, echoed in every envelope so a refusal in a transcript can
 * be joined to the audit record it produced (docs/MCP_SPEC.md section 5).
 */
export function validateRequestId(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_0 });
}

/**
 * Caller-chosen key for a state-changing tool (docs/MCP_SPEC.md section 10). It is scoped
 * to actor, tool and project: reuse with the same arguments returns the first result and
 * creates no second record, and reuse with different arguments is refused with
 * IDEMPOTENCY_CONFLICT.
 */
export function validateIdempotencyKey(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 8, maxLength: 128, pattern: PATTERN_1 });
}

/**
 * RFC 3339 timestamp in UTC, with a mandatory trailing Z.
 */
export function validateTimestamp(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 20, maxLength: 30, pattern: PATTERN_2 });
}

/**
 * Project-scoped human alias, for example bugs-on-homepage. It is unique among active
 * reviews of one project and not across projects, so the same slug in another project MUST
 * resolve as not found rather than as somebody else's review (docs/MCP_SPEC.md section
 * 7.6).
 */
export function validateSlug(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_3 });
}

/**
 * How an agent names a review: an immutable identifier or a project-scoped slug. Both
 * resolve inside the current project only, and no cross-project search is performed.
 */
export function validateReviewSelector(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_4 });
}

/**
 * Project the client believes it is working in, by slug or identifier. It narrows the
 * credential's binding and never widens it: a hint naming a project the credential is not
 * bound to is refused.
 */
export function validateProjectHint(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_4 });
}

/**
 * Filesystem root of the checkout the agent is working in, used to pick a workspace when
 * the project has more than one.
 */
export function validateWorkspaceHint(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 512, pattern: PATTERN_5 });
}

/**
 * Agent client name as it identifies itself, for example claude-code. It is descriptive
 * and never an authorisation input.
 */
export function validateClientName(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_6 });
}

/**
 * Agent client version as reported. unknown is accepted, because several clients do not
 * report one.
 */
export function validateClientVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_7 });
}

/**
 * Human-authored title. Rendered as text and never as markup, and never followed as an
 * instruction when it reached the record from a page.
 */
export function validateTitleText(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 200, pattern: PATTERN_5 });
}

/**
 * Human-authored or agent-authored prose: a description, instruction, comment, resolution
 * note or verification summary.
 */
export function validateBodyText(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 4000, pattern: PATTERN_8 });
}

/**
 * Short operator-facing explanation. Never carries credentials (docs/SECURITY.md section
 * 18).
 */
export function validateReasonText(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 512, pattern: PATTERN_5 });
}

/**
 * Absolute URL a finding or capture was taken at. It is page-derived and therefore
 * untrusted (ADR-0010): bounded in length and character class, displayed as text, never
 * executed and never followed as an instruction.
 */
export function validateCapturedUrl(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 2048, pattern: PATTERN_9 });
}

/**
 * One of the docs/MCP_SPEC.md section 8 resource URI forms. A resource enforces the same
 * authorisation as the tool that returned the link.
 */
export function validateResourceUri(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 3, maxLength: 320, pattern: PATTERN_10 });
}

/**
 * Path that serves artefact bytes for a short-lived, subject-bound grant (ADR-0019). It is
 * not a credential: the caller must still authenticate as the grant's subject, which is
 * why the identifier may appear in a URL at all.
 */
export function validateContentPath(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 256, pattern: PATTERN_11 });
}

/**
 * Version-control branch. Recorded on a verification so a human can see which branch the
 * claim was made from.
 */
export function validateBranchName(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 255, pattern: PATTERN_9 });
}

/**
 * Version-control commit. A verification commit MUST differ from the commit the finding
 * was captured at: a fix cannot exist at the revision the defect was recorded from.
 */
export function validateCommitSha(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 7, maxLength: 64, pattern: PATTERN_12 });
}

/**
 * Lowercase hexadecimal SHA-256 digest, as verified by the control plane from the stored
 * bytes.
 */
export function validateSha256Hex(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 64, maxLength: 64, pattern: PATTERN_13 });
}

/**
 * Media type of stored artefact bytes. Bounded to the types Stage 0 stores; active markup
 * is not among them (docs/SECURITY.md section 13).
 */
export function validateMediaType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["image/png","image/jpeg"] });
}

/**
 * Opaque pagination cursor (docs/API.md section 6). Its contents are an implementation
 * detail and MUST NOT be parsed by a client.
 */
export function validateCursor(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 256, pattern: PATTERN_0 });
}

/**
 * Maximum records in one page. Bounded here rather than by the caller's optimism, because
 * an unbounded response is the failure mode docs/MCP_SPEC.md section 13 exists to prevent.
 */
export function validatePageLimit(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 50 });
}

/**
 * A count of records, bounded so a count cannot itself be unbounded.
 */
export function validateRecordCount(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 100000 });
}

/**
 * Optimistic-concurrency version. A caller presenting a stale value is refused with
 * VERSION_CONFLICT and the current version in the details (docs/MCP_SPEC.md section 11).
 */
export function validateVersionNumber(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 2147483647 });
}

/**
 * Monotonic browser-control epoch (ADR-0007). A command presenting a superseded epoch is
 * refused with CONTROL_EPOCH_STALE rather than executed against a browser somebody else
 * now controls.
 */
export function validateControlEpoch(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 2147483647 });
}

/**
 * Byte length the control plane verified.
 */
export function validateByteSize(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 104857600 });
}

/**
 * A dimension in device pixels of a stored image.
 */
export function validatePixelExtent(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 32767 });
}

/**
 * An offset in CSS pixels within the captured document.
 */
export function validateCssPixelOffset(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 1000000 });
}

/**
 * Device pixel ratio of the capture.
 */
export function validateDeviceScaleFactor(value: unknown, path: string, out: SchemaViolation[]): void {
  checkNumber(value, path, out, { minimum: 1, maximum: 4 });
}

/**
 * Ordinal shown inside a numbered marker, so the annotation list and the overlay name the
 * same mark.
 */
export function validateMarkerNumber(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 999 });
}

/**
 * A coordinate normalised to the artefact content rectangle, inclusive of both bounds
 * (docs/DOMAIN_MODEL.md section 16).
 */
export function validateNormalisedCoordinate(value: unknown, path: string, out: SchemaViolation[]): void {
  checkNumber(value, path, out, { minimum: 0, maximum: 1 });
}

/**
 * How long to wait before retrying, returned with RATE_LIMITED.
 */
export function validateRetryAfterMs(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 3600000 });
}

/**
 * Remaining life of a short-lived grant or credential.
 */
export function validateExpiresInSeconds(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 86400 });
}

/**
 * Provenance of the content in a response (docs/MCP_SPEC.md section 6). Page-derived
 * content MUST NOT be returned without an untrusted label, and mixed marks a response that
 * carries both control-plane facts and page-derived or uploaded content, with the
 * untrusted members named in untrusted_fields.
 */
export function validateTrustLabel(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["trusted_control_plane","trusted_human_instruction","trusted_project_configuration","untrusted_browser_content","untrusted_uploaded_artefact","mixed"] });
}

/**
 * The single-valued policy of docs/SECURITY.md section 11. It rides on every response
 * rather than only on untrusted ones, so an agent never has to infer the absence of a rule
 * from the absence of a field.
 */
export function validateInstructionPolicy(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["do_not_follow_as_instructions"] });
}

/**
 * Dotted path of a member of data that is page-derived or uploaded. It exists so mixed is
 * actionable: an agent can tell which members of an otherwise trusted record came from a
 * page.
 */
export function validateUntrustedFieldPath(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 128, pattern: PATTERN_14 });
}

/**
 * Why a response is less than a client asked for while still being a success. A
 * degradation is a warning and not a failure (docs/ARCHITECTURE.md section 8.3): a client
 * that cannot consume image content still completes the workflow.
 */
export function validateWarningCode(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["image_content_unsupported","resource_content_unsupported","findings_truncated","text_truncated","workspace_unresolved","verification_branch_uncorroborated","staleness_unavailable","inbox_unavailable","results_truncated","idempotent_replay","completion_blocked_pending_review"] });
}

/**
 * Stable refusal code (docs/MCP_SPEC.md section 12). Adding a code is additive within a
 * protocol version and a client MUST tolerate a code it does not recognise. This
 * enumeration MUST equal x-protocol.error_classes.
 */
export function validateErrorClass(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["AUTHENTICATION_REQUIRED","AUTHORISATION_DENIED","PROJECT_CONTEXT_AMBIGUOUS","PROJECT_CONTEXT_MISMATCH","RESOURCE_NOT_FOUND","RESOURCE_STALE","VERSION_CONFLICT","IDEMPOTENCY_CONFLICT","CONNECTOR_OFFLINE","PUBLISHED_SERVICE_UNAVAILABLE","BROWSER_CAPACITY_EXHAUSTED","BROWSER_SESSION_NOT_ACTIVE","CONTROL_NOT_OWNED","CONTROL_EPOCH_STALE","BROWSER_COMMAND_TIMEOUT","POLICY_DENIED","APPROVAL_REQUIRED","EVIDENCE_REQUIRED","ARTEFACT_UPLOAD_INCOMPLETE","ARTEFACT_STORE_UNAVAILABLE","UNSUPPORTED_CAPABILITY","RATE_LIMITED","INTERNAL_ERROR"] });
}

/**
 * An available tool. This enumeration is the tool availability set of docs/MCP_SPEC.md
 * section 14 and MUST equal the keys of x-protocol.messages, so a tool the server
 * advertises always has a result schema and a bound.
 */
export function validateMessageType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["project_current","agent_session_status","agent_inbox_list","agent_inbox_acknowledge","review_list","review_search","review_get","review_claim","review_update_status","review_add_comment","finding_get","finding_claim","finding_update_status","finding_mark_blocked","finding_add_comment","finding_submit_verification","browser_take_screenshot","development_services_list","development_service_publish","development_service_unpublish","task_validation_status","task_complete"] });
}

/**
 * A capability an agent credential may carry (docs/SECURITY.md section 6.3). A tool
 * requires one, and a credential without it is refused with AUTHORISATION_DENIED. An agent
 * session cannot grant itself a capability beyond its issued set (docs/DOMAIN_MODEL.md
 * section 11).
 */
export function validateAgentCapability(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["project:read","review:read","review:write","finding:read","finding:write","verification:submit","browser:capture","service:publish"] });
}

/**
 * Agent-session status (docs/DOMAIN_MODEL.md section 11).
 */
export function validateAgentSessionState(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["STARTING","ACTIVE","WAITING","BLOCKED","DISCONNECTED","COMPLETED","FAILED","CANCELLED"] });
}

/**
 * How a review is ordered in a queue (docs/DOMAIN_MODEL.md section 14). It orders and
 * gates nothing: an urgent review and a routine one obey the same lifecycle and the same
 * authority rules.
 */
export function validateReviewPriority(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["critical","high","medium","low"] });
}

/**
 * A search term. It is matched literally against the current project's review titles,
 * slugs, descriptions and finding text; it is never interpreted as a pattern, and it
 * carries no project of its own because the project is the session's.
 */
export function validateSearchQuery(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 2, maxLength: 128, pattern: PATTERN_5 });
}

/**
 * Inbox-item status (docs/DOMAIN_MODEL.md section 21). acknowledged means the recipient
 * has seen the item and never that the work is done; completed is the separate later fact.
 */
export function validateInboxItemStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["pending","acknowledged","completed","dismissed","expired"] });
}

/**
 * Why an inbox item exists. review_assigned is created when a review is assigned to a
 * recipient; finding_reopened is created when a human reopens a finding the recipient
 * worked on. Both are durable: an agent learns what a human wants changed by retrieving
 * them rather than by guessing (docs/MCP_SPEC.md section 9).
 */
export function validateInboxItemType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["review_assigned","finding_reopened"] });
}

/**
 * Review lifecycle status (docs/DOMAIN_MODEL.md section 14).
 */
export function validateReviewStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["DRAFT","READY","ASSIGNED","IN_PROGRESS","AWAITING_HUMAN_REVIEW","CHANGES_REQUESTED","ACCEPTED","CANCELLED","ARCHIVED"] });
}

/**
 * Review statuses an agent may request. ACCEPTED is absent by construction, which is the
 * AGENTS.md authority rule expressed as a type rather than as a runtime check. The
 * docs/MCP_SPEC.md section 7.6 list also named blocked; docs/DOMAIN_MODEL.md section 14
 * has no such review status and takes precedence, so a blocked agent records the block on
 * the finding it is stuck on.
 */
export function validateAgentReviewStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["IN_PROGRESS","AWAITING_HUMAN_REVIEW"] });
}

/**
 * Finding lifecycle status (docs/DOMAIN_MODEL.md section 15).
 */
export function validateFindingStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["OPEN","CLAIMED","IN_PROGRESS","BLOCKED","FIXED_UNVERIFIED","AWAITING_HUMAN_REVIEW","RESOLVED","REOPENED","WONT_FIX","DUPLICATE"] });
}

/**
 * Finding statuses an agent may request, taken from the transition list of
 * docs/MCP_SPEC.md section 7.7. RESOLVED, WONT_FIX and DUPLICATE are absent: an agent that
 * cannot name a final disposition cannot request one, so the refusal does not depend on a
 * check anybody could forget.
 */
export function validateAgentFindingStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["CLAIMED","IN_PROGRESS","BLOCKED","FIXED_UNVERIFIED","AWAITING_HUMAN_REVIEW"] });
}

/**
 * Finding severity (docs/DOMAIN_MODEL.md section 15).
 */
export function validateFindingSeverity(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["critical","high","medium","low","suggestion"] });
}

/**
 * Whether a human or an agent authored the finding. The acceptance authority rule is
 * decided on it for the life of the finding.
 */
export function validateFindingSource(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["human","agent"] });
}

/**
 * Annotation shape (docs/DOMAIN_MODEL.md section 16).
 */
export function validateAnnotationType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["rectangle","ellipse","arrow","point","numbered_marker"] });
}

/**
 * Artefact kind (docs/DOMAIN_MODEL.md section 20). Stage 0 evidence is a screenshot.
 */
export function validateArtefactKind(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["screenshot","thumbnail","trace","dom_snapshot","console_log","network_log"] });
}

/**
 * Status of a verification submission (docs/DOMAIN_MODEL.md section 19). An agent produces
 * submitted and nothing else; accepted and rejected are human decisions.
 */
export function validateVerificationStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["submitted","accepted","rejected","superseded"] });
}

/**
 * Who acted (docs/EVENTS.md section 5). The distinction between human_user and
 * agent_session is what the acceptance authority rules are decided on.
 */
export function validateActorType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["human_user","agent_session","connector","browser_worker","system","integration"] });
}

/**
 * Why a capture is being taken. Stage 0 accepts verification only: a capture is evidence,
 * and an unclassified capture cannot be filed under a retention class.
 */
export function validateScreenshotPurpose(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["verification"] });
}

/**
 * Optional sections of a review response. staleness reports the branch and commit the
 * review was captured at and nothing else: the calculation of docs/DOMAIN_MODEL.md section
 * 24 is Stage 2, so the section reports what was recorded and states that no verdict was
 * computed rather than guessing one.
 */
export function validateReviewInclude(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["findings","comments","artefact_links","staleness"] });
}

/**
 * Which part of a review a search query matched. It is reported so an agent can tell a
 * title match from a match inside a finding a page supplied text for.
 */
export function validateReviewSearchField(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["title","slug","description","finding"] });
}

/**
 * Optional sections of a finding response.
 */
export function validateFindingInclude(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["annotations","comments","verification","artefact_links"] });
}

/**
 * Optional sections of an agent-session status response.
 */
export function validateSessionInclude(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["browser_sessions","capabilities"] });
}

/**
 * Local address of a development service, as a literal IP address. A name would have to be
 * resolved, and a resolver is a rebinding surface (docs/SECURITY.md section 9).
 */
export function validateLocalHost(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 45, pattern: PATTERN_15 });
}

/**
 * TCP port a development service listens on.
 */
export function validateLocalPort(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 65535 });
}

/**
 * Protocol a development service speaks on its local socket. It is declared at publication
 * and is not negotiable per request.
 */
export function validateDestinationProtocol(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["http","https"] });
}

/**
 * Published-service lifecycle status (docs/DOMAIN_MODEL.md section 10).
 */
export function validatePublishedServiceStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["requested","ready","failed","expired","revoked"] });
}

/**
 * The result of the completion gate (docs/MCP_SPEC.md section 7.8). blocked_pending_review
 * is a correct terminal answer for an agent and not a failure to retry.
 */
export function validateCompletionResult(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["completed","completed_with_warnings","blocked_missing_evidence","blocked_pending_review"] });
}

/**
 * One requirement or gap, written for a reader rather than parsed: 390x844 verification,
 * after screenshot, console review. The control plane composes every value, so nothing an
 * agent supplied is echoed back through this member.
 */
export function validateRequirementLabel(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 120, pattern: PATTERN_16 });
}

/**
 * An acting principal. The type is always present, because an authority rule that depends
 * on it must never fall back to a default.
 */
export function validateActorReference(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["type", "id", "display"], ["type"]);
  if (source === null) return;
  if (source["type"] !== undefined) {
    validateActorType(source["type"], `${path}.type`, out);
  }
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["display"] !== undefined) {
    validateReasonText(source["display"], `${path}.display`, out);
  }
}

/**
 * Viewport width in CSS pixels.
 */
export function validateViewportWidth(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 240, maximum: 3840 });
}

/**
 * Viewport height in CSS pixels.
 */
export function validateViewportHeight(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 240, maximum: 2160 });
}

/**
 * Browser viewport in CSS pixels plus the device pixel ratio. A finding records the
 * viewport it was seen at so it can be reproduced at that size.
 */
export function validateViewport(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["width", "height", "device_scale_factor"], ["width", "height", "device_scale_factor"]);
  if (source === null) return;
  if (source["width"] !== undefined) {
    validateViewportWidth(source["width"], `${path}.width`, out);
  }
  if (source["height"] !== undefined) {
    validateViewportHeight(source["height"], `${path}.height`, out);
  }
  if (source["device_scale_factor"] !== undefined) {
    validateDeviceScaleFactor(source["device_scale_factor"], `${path}.device_scale_factor`, out);
  }
}

/**
 * Document scroll offset in CSS pixels at capture time.
 */
export function validateScrollPosition(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["x", "y"], ["x", "y"]);
  if (source === null) return;
  if (source["x"] !== undefined) {
    validateCssPixelOffset(source["x"], `${path}.x`, out);
  }
  if (source["y"] !== undefined) {
    validateCssPixelOffset(source["y"], `${path}.y`, out);
  }
}

/**
 * Intrinsic pixel extent of a stored image, which is the frame annotation geometry is
 * normalised against (docs/DOMAIN_MODEL.md section 16).
 */
export function validateContentRectangle(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["width_px", "height_px"], ["width_px", "height_px"]);
  if (source === null) return;
  if (source["width_px"] !== undefined) {
    validatePixelExtent(source["width_px"], `${path}.width_px`, out);
  }
  if (source["height_px"] !== undefined) {
    validatePixelExtent(source["height_px"], `${path}.height_px`, out);
  }
}

/**
 * Normalised geometry against the artefact content rectangle. Which members a type uses is
 * fixed by the review protocol's own vocabulary; every member here lies between 0 and 1
 * inclusive.
 */
export function validateAnnotationGeometry(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["x", "y", "width", "height", "x2", "y2"], ["x", "y"]);
  if (source === null) return;
  if (source["x"] !== undefined) {
    validateNormalisedCoordinate(source["x"], `${path}.x`, out);
  }
  if (source["y"] !== undefined) {
    validateNormalisedCoordinate(source["y"], `${path}.y`, out);
  }
  if (source["width"] !== undefined) {
    validateNormalisedCoordinate(source["width"], `${path}.width`, out);
  }
  if (source["height"] !== undefined) {
    validateNormalisedCoordinate(source["height"], `${path}.height`, out);
  }
  if (source["x2"] !== undefined) {
    validateNormalisedCoordinate(source["x2"], `${path}.x2`, out);
  }
  if (source["y2"] !== undefined) {
    validateNormalisedCoordinate(source["y2"], `${path}.y2`, out);
  }
}

/**
 * A project the agent credential is bound to. It is the shape returned in
 * PROJECT_CONTEXT_AMBIGUOUS details, so an agent can disambiguate rather than be told only
 * that it failed.
 */
export function validateProjectReference(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "slug", "name"], ["id", "slug"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["slug"] !== undefined) {
    validateSlug(source["slug"], `${path}.slug`, out);
  }
  if (source["name"] !== undefined) {
    validateTitleText(source["name"], `${path}.name`, out);
  }
}

/**
 * Whether the checkout has uncommitted changes.
 */
export function validateWorkspaceReferenceDirty(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * The checkout the agent is working in (docs/DOMAIN_MODEL.md section 9). Stage 0 has no
 * connector, so a workspace is registered administratively and is absent when none is
 * registered; a verification submitted without one is accepted with a warning rather than
 * refused, because the branch cannot be corroborated but the evidence still can.
 */
export function validateWorkspaceReference(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "root_path", "branch", "head_commit", "dirty"], ["id", "branch", "head_commit"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["root_path"] !== undefined) {
    validateWorkspaceHint(source["root_path"], `${path}.root_path`, out);
  }
  if (source["branch"] !== undefined) {
    validateBranchName(source["branch"], `${path}.branch`, out);
  }
  if (source["head_commit"] !== undefined) {
    validateCommitSha(source["head_commit"], `${path}.head_commit`, out);
  }
  if (source["dirty"] !== undefined) {
    validateWorkspaceReferenceDirty(source["dirty"], `${path}.dirty`, out);
  }
}

/**
 * Always false. A human-authored finding is accepted by a human; an agent submits
 * verification and marks the finding AWAITING_HUMAN_REVIEW.
 */
export function validatePolicySummaryAgentMayAcceptFindings(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether a completion claim must carry verification evidence. Always true in Stage 0:
 * AGENTS.md forbids claiming a fix without it.
 */
export function validatePolicySummaryVerificationRequired(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Always false in Stage 0. No secret tool is exposed at all, which is the strongest form
 * of the no-disclosure rule of docs/SECURITY.md section 12.1.
 */
export function validatePolicySummarySecretToolsAvailable(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Viewports UI-facing work must be checked at (AGENTS.md "Browser-facing work").
 */
export function validatePolicySummaryRequiredViewports(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 8, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReasonText(value[index], `${path}[${index}]`, out);
  }
}

/**
 * What the project requires of an agent before work counts as done, and what it will never
 * let an agent do. The negative statements are as load-bearing as the positive ones: an
 * agent that reads agent_may_accept_findings: false learns the boundary of
 * docs/DOMAIN_MODEL.md section 15 rather than discovering it as a refusal.
 */
export function validatePolicySummary(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["agent_may_accept_findings", "verification_required", "secret_tools_available", "required_viewports"], ["agent_may_accept_findings", "verification_required", "secret_tools_available", "required_viewports"]);
  if (source === null) return;
  if (source["agent_may_accept_findings"] !== undefined) {
    validatePolicySummaryAgentMayAcceptFindings(source["agent_may_accept_findings"], `${path}.agent_may_accept_findings`, out);
  }
  if (source["verification_required"] !== undefined) {
    validatePolicySummaryVerificationRequired(source["verification_required"], `${path}.verification_required`, out);
  }
  if (source["secret_tools_available"] !== undefined) {
    validatePolicySummarySecretToolsAvailable(source["secret_tools_available"], `${path}.secret_tools_available`, out);
  }
  if (source["required_viewports"] !== undefined) {
    validatePolicySummaryRequiredViewports(source["required_viewports"], `${path}.required_viewports`, out);
  }
}

/**
 * Whether a human can watch the browser session live.
 */
export function validateServerCapabilitiesBrowserLive(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether this session will be served image content. It is the negotiated result of the
 * client's own image capability, not a property of the server alone.
 */
export function validateServerCapabilitiesImageResources(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether a human may take browser control from the agent.
 */
export function validateServerCapabilitiesHumanTakeover(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether the inbox workflow of section 9 is available. True where agent_inbox_list and
 * agent_inbox_acknowledge are advertised; a deployment that withdrew them would report
 * false rather than advertise a tool that refuses.
 */
export function validateServerCapabilitiesReviewInbox(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether the server pushes messages to the client. False: agents retrieve explicitly
 * (docs/ARCHITECTURE.md section 8.3).
 */
export function validateServerCapabilitiesManagedMessages(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether an interrupted session can be resumed.
 */
export function validateServerCapabilitiesSessionResume(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * What this server can do for this session (docs/MCP_SPEC.md section 4 and
 * docs/ARCHITECTURE.md section 8.3). A false value is a fact the client can plan around,
 * which is what makes degradation clear rather than surprising.
 */
export function validateServerCapabilities(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["browser_live", "image_resources", "human_takeover", "review_inbox", "managed_messages", "session_resume"], ["browser_live", "image_resources", "human_takeover", "review_inbox", "managed_messages", "session_resume"]);
  if (source === null) return;
  if (source["browser_live"] !== undefined) {
    validateServerCapabilitiesBrowserLive(source["browser_live"], `${path}.browser_live`, out);
  }
  if (source["image_resources"] !== undefined) {
    validateServerCapabilitiesImageResources(source["image_resources"], `${path}.image_resources`, out);
  }
  if (source["human_takeover"] !== undefined) {
    validateServerCapabilitiesHumanTakeover(source["human_takeover"], `${path}.human_takeover`, out);
  }
  if (source["review_inbox"] !== undefined) {
    validateServerCapabilitiesReviewInbox(source["review_inbox"], `${path}.review_inbox`, out);
  }
  if (source["managed_messages"] !== undefined) {
    validateServerCapabilitiesManagedMessages(source["managed_messages"], `${path}.managed_messages`, out);
  }
  if (source["session_resume"] !== undefined) {
    validateServerCapabilitiesSessionResume(source["session_resume"], `${path}.session_resume`, out);
  }
}

/**
 * Whether the client can read MCP resources.
 */
export function validateClientCapabilitiesResources(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether the client can consume image content.
 */
export function validateClientCapabilitiesImageContent(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether the client can receive pushed messages.
 */
export function validateClientCapabilitiesManagedMessages(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether the client can resume a session.
 */
export function validateClientCapabilitiesSessionResume(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * What the client says it can consume (docs/MCP_SPEC.md section 4). image_content false is
 * the case docs/ARCHITECTURE.md section 8.3 requires clear degradation for: the workflow
 * still completes, with resource links, text and a warning instead of image content.
 */
export function validateClientCapabilities(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["resources", "image_content", "managed_messages", "session_resume"], []);
  if (source === null) return;
  if (source["resources"] !== undefined) {
    validateClientCapabilitiesResources(source["resources"], `${path}.resources`, out);
  }
  if (source["image_content"] !== undefined) {
    validateClientCapabilitiesImageContent(source["image_content"], `${path}.image_content`, out);
  }
  if (source["managed_messages"] !== undefined) {
    validateClientCapabilitiesManagedMessages(source["managed_messages"], `${path}.managed_messages`, out);
  }
  if (source["session_resume"] !== undefined) {
    validateClientCapabilitiesSessionResume(source["session_resume"], `${path}.session_resume`, out);
  }
}

/**
 * A degradation or a truncation that did not stop the call succeeding.
 */
export function validateWarning(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["code", "message", "detail"], ["code", "message"]);
  if (source === null) return;
  if (source["code"] !== undefined) {
    validateWarningCode(source["code"], `${path}.code`, out);
  }
  if (source["message"] !== undefined) {
    validateReasonText(source["message"], `${path}.message`, out);
  }
  if (source["detail"] !== undefined) {
    validateReasonText(source["detail"], `${path}.detail`, out);
  }
}

/**
 * Projects the credential is bound to, returned with PROJECT_CONTEXT_AMBIGUOUS. The server
 * never picks one: it returns the list so the agent can name the project it means.
 */
export function validateErrorDetailsCandidates(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 32, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateProjectReference(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Evidence a completion claim must carry, returned with EVIDENCE_REQUIRED.
 */
export function validateErrorDetailsRequiredEvidence(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReasonText(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Transitions the agent may perform from the finding's current status, returned when a
 * transition is refused. The refusal states what is possible rather than only what is not.
 */
export function validateErrorDetailsAllowedTransitions(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReasonText(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Machine-readable detail for a refusal. Bounded rather than free-form, so a refusal
 * cannot become a channel for arbitrary content.
 */
export function validateErrorDetails(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["current_version", "expected_version", "current_epoch", "field", "candidates", "required_evidence", "allowed_transitions", "retry_after_ms"], []);
  if (source === null) return;
  if (source["current_version"] !== undefined) {
    validateVersionNumber(source["current_version"], `${path}.current_version`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["current_epoch"] !== undefined) {
    validateControlEpoch(source["current_epoch"], `${path}.current_epoch`, out);
  }
  if (source["field"] !== undefined) {
    validateReasonText(source["field"], `${path}.field`, out);
  }
  if (source["candidates"] !== undefined) {
    validateErrorDetailsCandidates(source["candidates"], `${path}.candidates`, out);
  }
  if (source["required_evidence"] !== undefined) {
    validateErrorDetailsRequiredEvidence(source["required_evidence"], `${path}.required_evidence`, out);
  }
  if (source["allowed_transitions"] !== undefined) {
    validateErrorDetailsAllowedTransitions(source["allowed_transitions"], `${path}.allowed_transitions`, out);
  }
  if (source["retry_after_ms"] !== undefined) {
    validateRetryAfterMs(source["retry_after_ms"], `${path}.retry_after_ms`, out);
  }
}

/**
 * Whether retrying the same call unchanged could succeed. It is explicit so a client does
 * not have to infer retryability from the code.
 */
export function validateToolErrorRetryable(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * The refusal object of docs/MCP_SPEC.md section 5. The code is the stable vocabulary of
 * section 12; the message is for a human and is never parsed.
 */
export function validateToolError(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["code", "message", "retryable", "details"], ["code", "message", "retryable"]);
  if (source === null) return;
  if (source["code"] !== undefined) {
    validateErrorClass(source["code"], `${path}.code`, out);
  }
  if (source["message"] !== undefined) {
    validateReasonText(source["message"], `${path}.message`, out);
  }
  if (source["retryable"] !== undefined) {
    validateToolErrorRetryable(source["retryable"], `${path}.retryable`, out);
  }
  if (source["details"] !== undefined) {
    validateErrorDetails(source["details"], `${path}.details`, out);
  }
}

/**
 * A reference to evidence, never the evidence itself (docs/MCP_SPEC.md sections 7.6 and
 * 13). content_path serves the bytes for a short-lived grant bound to this agent session
 * (ADR-0019); an agent that wants the image fetches it with its own credential or reads
 * the resource, and neither the tool result nor the review response ever embeds it.
 */
export function validateArtefactLink(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["artefact_id", "kind", "role", "resource_uri", "content_path", "expires_at", "content_type", "sha256", "size_bytes", "content_rectangle", "trust", "instruction_policy"], ["artefact_id", "kind", "resource_uri", "content_type", "trust", "instruction_policy"]);
  if (source === null) return;
  if (source["artefact_id"] !== undefined) {
    validateIdentifier(source["artefact_id"], `${path}.artefact_id`, out);
  }
  if (source["kind"] !== undefined) {
    validateArtefactKind(source["kind"], `${path}.kind`, out);
  }
  if (source["role"] !== undefined) {
    validateReasonText(source["role"], `${path}.role`, out);
  }
  if (source["resource_uri"] !== undefined) {
    validateResourceUri(source["resource_uri"], `${path}.resource_uri`, out);
  }
  if (source["content_path"] !== undefined) {
    validateContentPath(source["content_path"], `${path}.content_path`, out);
  }
  if (source["expires_at"] !== undefined) {
    validateTimestamp(source["expires_at"], `${path}.expires_at`, out);
  }
  if (source["content_type"] !== undefined) {
    validateMediaType(source["content_type"], `${path}.content_type`, out);
  }
  if (source["sha256"] !== undefined) {
    validateSha256Hex(source["sha256"], `${path}.sha256`, out);
  }
  if (source["size_bytes"] !== undefined) {
    validateByteSize(source["size_bytes"], `${path}.size_bytes`, out);
  }
  if (source["content_rectangle"] !== undefined) {
    validateContentRectangle(source["content_rectangle"], `${path}.content_rectangle`, out);
  }
  if (source["trust"] !== undefined) {
    validateTrustLabel(source["trust"], `${path}.trust`, out);
  }
  if (source["instruction_policy"] !== undefined) {
    validateInstructionPolicy(source["instruction_policy"], `${path}.instruction_policy`, out);
  }
}

/**
 * One mark on a finding's original screenshot, as an agent sees it (ADR-0006). The
 * geometry is normalised and the original is untouched.
 */
export function validateAnnotationView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "artefact_id", "type", "geometry", "label", "marker_number"], ["id", "artefact_id", "type", "geometry", "label"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["artefact_id"] !== undefined) {
    validateIdentifier(source["artefact_id"], `${path}.artefact_id`, out);
  }
  if (source["type"] !== undefined) {
    validateAnnotationType(source["type"], `${path}.type`, out);
  }
  if (source["geometry"] !== undefined) {
    validateAnnotationGeometry(source["geometry"], `${path}.geometry`, out);
  }
  if (source["label"] !== undefined) {
    validateTitleText(source["label"], `${path}.label`, out);
  }
  if (source["marker_number"] !== undefined) {
    validateMarkerNumber(source["marker_number"], `${path}.marker_number`, out);
  }
}

/**
 * One comment on a review or on one of its findings (docs/DOMAIN_MODEL.md section 18).
 * Comments are append-only and the author type is always explicit. review_id is always
 * present and finding_id is absent for a comment on the review itself, which is the same
 * shape the control-plane record carries.
 */
export function validateCommentView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "review_id", "finding_id", "body", "author", "created_at"], ["id", "review_id", "body", "author", "created_at"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["body"] !== undefined) {
    validateBodyText(source["body"], `${path}.body`, out);
  }
  if (source["author"] !== undefined) {
    validateActorReference(source["author"], `${path}.author`, out);
  }
  if (source["created_at"] !== undefined) {
    validateTimestamp(source["created_at"], `${path}.created_at`, out);
  }
}

/**
 * One durable work notification (docs/DOMAIN_MODEL.md section 21). It names the work
 * rather than carrying it: a review reference and a count, not the review's findings, so
 * an inbox read cannot become an unbounded delivery. status distinguishes receipt from
 * completion, which is the whole reason the record is durable.
 */
export function validateInboxItemView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "project_id", "type", "title", "status", "review_id", "review_slug", "finding_id", "priority", "finding_count", "assigned_by", "created_at", "acknowledged_at", "completed_at"], ["id", "project_id", "type", "title", "status", "created_at"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["project_id"] !== undefined) {
    validateIdentifier(source["project_id"], `${path}.project_id`, out);
  }
  if (source["type"] !== undefined) {
    validateInboxItemType(source["type"], `${path}.type`, out);
  }
  if (source["title"] !== undefined) {
    validateTitleText(source["title"], `${path}.title`, out);
  }
  if (source["status"] !== undefined) {
    validateInboxItemStatus(source["status"], `${path}.status`, out);
  }
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["review_slug"] !== undefined) {
    validateSlug(source["review_slug"], `${path}.review_slug`, out);
  }
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["priority"] !== undefined) {
    validateReviewPriority(source["priority"], `${path}.priority`, out);
  }
  if (source["finding_count"] !== undefined) {
    validateRecordCount(source["finding_count"], `${path}.finding_count`, out);
  }
  if (source["assigned_by"] !== undefined) {
    validateActorReference(source["assigned_by"], `${path}.assigned_by`, out);
  }
  if (source["created_at"] !== undefined) {
    validateTimestamp(source["created_at"], `${path}.created_at`, out);
  }
  if (source["acknowledged_at"] !== undefined) {
    validateTimestamp(source["acknowledged_at"], `${path}.acknowledged_at`, out);
  }
  if (source["completed_at"] !== undefined) {
    validateTimestamp(source["completed_at"], `${path}.completed_at`, out);
  }
}

/**
 * Whether a staleness comparison was performed. False here.
 */
export function validateReviewStalenessComputed(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * The source context a review was captured at (docs/DOMAIN_MODEL.md section 24). computed
 * is always false in this stage and is present rather than omitted, so a reader is told
 * that no comparison was made instead of being left to infer it from a missing field. A
 * verdict that had to be guessed would be worse than none: it would either close feedback
 * nobody decided to close, or send an agent to reproduce against the wrong revision.
 */
export function validateReviewStaleness(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["computed", "captured_branch", "captured_commit", "workspace_branch", "workspace_head_commit"], ["computed", "captured_branch", "captured_commit"]);
  if (source === null) return;
  if (source["computed"] !== undefined) {
    validateReviewStalenessComputed(source["computed"], `${path}.computed`, out);
  }
  if (source["captured_branch"] !== undefined) {
    validateBranchName(source["captured_branch"], `${path}.captured_branch`, out);
  }
  if (source["captured_commit"] !== undefined) {
    validateCommitSha(source["captured_commit"], `${path}.captured_commit`, out);
  }
  if (source["workspace_branch"] !== undefined) {
    validateBranchName(source["workspace_branch"], `${path}.workspace_branch`, out);
  }
  if (source["workspace_head_commit"] !== undefined) {
    validateCommitSha(source["workspace_head_commit"], `${path}.workspace_head_commit`, out);
  }
}

/**
 * Which parts of the review matched the query.
 */
export function validateReviewSearchMatchMatched(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 4, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReviewSearchField(value[index], `${path}[${index}]`, out);
  }
}

/**
 * One review a search matched, with which part of it matched. The excerpt is deliberately
 * absent: a finding's text can carry page-derived content, and returning a fragment of it
 * here would smuggle untrusted bytes into a list response. An agent that wants the text
 * reads the review.
 */
export function validateReviewSearchMatch(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review", "matched"], ["review", "matched"]);
  if (source === null) return;
  if (source["review"] !== undefined) {
    validateReviewView(source["review"], `${path}.review`, out);
  }
  if (source["matched"] !== undefined) {
    validateReviewSearchMatchMatched(source["matched"], `${path}.matched`, out);
  }
}

/**
 * Whether the agent reproduced the defect before changing anything.
 */
export function validateVerificationChecksReproducedBefore(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether console output was reviewed after the change.
 */
export function validateVerificationChecksConsoleErrorsReviewed(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether failed network requests were reviewed after the change.
 */
export function validateVerificationChecksNetworkFailuresReviewed(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether keyboard navigation and visible focus were checked.
 */
export function validateVerificationChecksAccessibilityChecked(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * The checks a verification claims to have performed (docs/DOMAIN_MODEL.md section 19).
 * They are recorded as claims by the agent and are never treated as proof on their own:
 * the artefacts are the evidence.
 */
export function validateVerificationChecks(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["reproduced_before", "console_errors_reviewed", "network_failures_reviewed", "accessibility_checked"], ["reproduced_before", "console_errors_reviewed", "network_failures_reviewed"]);
  if (source === null) return;
  if (source["reproduced_before"] !== undefined) {
    validateVerificationChecksReproducedBefore(source["reproduced_before"], `${path}.reproduced_before`, out);
  }
  if (source["console_errors_reviewed"] !== undefined) {
    validateVerificationChecksConsoleErrorsReviewed(source["console_errors_reviewed"], `${path}.console_errors_reviewed`, out);
  }
  if (source["network_failures_reviewed"] !== undefined) {
    validateVerificationChecksNetworkFailuresReviewed(source["network_failures_reviewed"], `${path}.network_failures_reviewed`, out);
  }
  if (source["accessibility_checked"] !== undefined) {
    validateVerificationChecksAccessibilityChecked(source["accessibility_checked"], `${path}.accessibility_checked`, out);
  }
}

/**
 * Viewports the fix was checked at.
 */
export function validateVerificationViewTestedViewports(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 8, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateViewport(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Evidence the submission rests on, as links rather than bytes.
 */
export function validateVerificationViewArtefacts(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateArtefactLink(value[index], `${path}[${index}]`, out);
  }
}

/**
 * A submitted claim with evidence (docs/DOMAIN_MODEL.md section 19). Its status is
 * submitted: a verification is not a resolution, and the human decision that follows is
 * not available to an agent at all.
 */
export function validateVerificationView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["verification_id", "finding_id", "status", "summary", "branch", "commit", "tested_viewports", "checks", "assurance", "artefacts", "submitted_by", "submitted_at", "supersedes_verification_id", "superseded_by_verification_id", "superseded_at"], ["verification_id", "finding_id", "status", "summary", "branch", "commit", "tested_viewports", "checks", "submitted_by", "submitted_at"]);
  if (source === null) return;
  if (source["verification_id"] !== undefined) {
    validateIdentifier(source["verification_id"], `${path}.verification_id`, out);
  }
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["status"] !== undefined) {
    validateVerificationStatus(source["status"], `${path}.status`, out);
  }
  if (source["summary"] !== undefined) {
    validateBodyText(source["summary"], `${path}.summary`, out);
  }
  if (source["branch"] !== undefined) {
    validateBranchName(source["branch"], `${path}.branch`, out);
  }
  if (source["commit"] !== undefined) {
    validateCommitSha(source["commit"], `${path}.commit`, out);
  }
  if (source["tested_viewports"] !== undefined) {
    validateVerificationViewTestedViewports(source["tested_viewports"], `${path}.tested_viewports`, out);
  }
  if (source["checks"] !== undefined) {
    validateVerificationChecks(source["checks"], `${path}.checks`, out);
  }
  if (source["assurance"] !== undefined) {
    validateEvidenceAssurance(source["assurance"], `${path}.assurance`, out);
  }
  if (source["artefacts"] !== undefined) {
    validateVerificationViewArtefacts(source["artefacts"], `${path}.artefacts`, out);
  }
  if (source["submitted_by"] !== undefined) {
    validateActorReference(source["submitted_by"], `${path}.submitted_by`, out);
  }
  if (source["submitted_at"] !== undefined) {
    validateTimestamp(source["submitted_at"], `${path}.submitted_at`, out);
  }
  if (source["supersedes_verification_id"] !== undefined) {
    validateIdentifier(source["supersedes_verification_id"], `${path}.supersedes_verification_id`, out);
  }
  if (source["superseded_by_verification_id"] !== undefined) {
    validateIdentifier(source["superseded_by_verification_id"], `${path}.superseded_by_verification_id`, out);
  }
  if (source["superseded_at"] !== undefined) {
    validateTimestamp(source["superseded_at"], `${path}.superseded_at`, out);
  }
}

/**
 * A review as an agent sees it. The captured branch and commit are on the record because
 * the point of a review is to still mean something after the code has moved on.
 */
export function validateReviewView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "project_id", "slug", "title", "description", "status", "version", "captured_branch", "captured_commit", "captured_workspace_id", "source_browser_session_id", "claimed_by", "finding_count", "resource_uri", "created_at", "updated_at"], ["id", "project_id", "slug", "title", "status", "version", "captured_branch", "captured_commit", "captured_workspace_id", "finding_count", "resource_uri", "created_at", "updated_at"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["project_id"] !== undefined) {
    validateIdentifier(source["project_id"], `${path}.project_id`, out);
  }
  if (source["slug"] !== undefined) {
    validateSlug(source["slug"], `${path}.slug`, out);
  }
  if (source["title"] !== undefined) {
    validateTitleText(source["title"], `${path}.title`, out);
  }
  if (source["description"] !== undefined) {
    validateBodyText(source["description"], `${path}.description`, out);
  }
  if (source["status"] !== undefined) {
    validateReviewStatus(source["status"], `${path}.status`, out);
  }
  if (source["version"] !== undefined) {
    validateVersionNumber(source["version"], `${path}.version`, out);
  }
  if (source["captured_branch"] !== undefined) {
    validateBranchName(source["captured_branch"], `${path}.captured_branch`, out);
  }
  if (source["captured_commit"] !== undefined) {
    validateCommitSha(source["captured_commit"], `${path}.captured_commit`, out);
  }
  if (source["captured_workspace_id"] !== undefined) {
    validateIdentifier(source["captured_workspace_id"], `${path}.captured_workspace_id`, out);
  }
  if (source["source_browser_session_id"] !== undefined) {
    validateIdentifier(source["source_browser_session_id"], `${path}.source_browser_session_id`, out);
  }
  if (source["claimed_by"] !== undefined) {
    validateActorReference(source["claimed_by"], `${path}.claimed_by`, out);
  }
  if (source["finding_count"] !== undefined) {
    validateRecordCount(source["finding_count"], `${path}.finding_count`, out);
  }
  if (source["resource_uri"] !== undefined) {
    validateResourceUri(source["resource_uri"], `${path}.resource_uri`, out);
  }
  if (source["created_at"] !== undefined) {
    validateTimestamp(source["created_at"], `${path}.created_at`, out);
  }
  if (source["updated_at"] !== undefined) {
    validateTimestamp(source["updated_at"], `${path}.updated_at`, out);
  }
}

/**
 * Members of this record that are page-derived. An empty list is impossible: url is always
 * page-derived, so the array is required and always populated.
 */
export function validateFindingViewUntrustedFields(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateUntrustedFieldPath(value[index], `${path}[${index}]`, out);
  }
}

/**
 * A finding as an agent sees it, with the captured context that makes it reproducible
 * after the browser session has gone. untrusted_fields names the members that came from a
 * page, which is what makes the mixed trust label of the enclosing response actionable.
 */
export function validateFindingView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "review_id", "title", "description", "severity", "status", "source", "version", "url", "viewport", "scroll_position", "captured_commit", "screenshot_artefact_id", "acceptance_criteria", "claimed_by", "annotation_count", "resource_uri", "untrusted_fields"], ["id", "review_id", "title", "severity", "status", "source", "version", "url", "viewport", "scroll_position", "captured_commit", "screenshot_artefact_id", "resource_uri", "untrusted_fields"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["title"] !== undefined) {
    validateTitleText(source["title"], `${path}.title`, out);
  }
  if (source["description"] !== undefined) {
    validateBodyText(source["description"], `${path}.description`, out);
  }
  if (source["severity"] !== undefined) {
    validateFindingSeverity(source["severity"], `${path}.severity`, out);
  }
  if (source["status"] !== undefined) {
    validateFindingStatus(source["status"], `${path}.status`, out);
  }
  if (source["source"] !== undefined) {
    validateFindingSource(source["source"], `${path}.source`, out);
  }
  if (source["version"] !== undefined) {
    validateVersionNumber(source["version"], `${path}.version`, out);
  }
  if (source["url"] !== undefined) {
    validateCapturedUrl(source["url"], `${path}.url`, out);
  }
  if (source["viewport"] !== undefined) {
    validateViewport(source["viewport"], `${path}.viewport`, out);
  }
  if (source["scroll_position"] !== undefined) {
    validateScrollPosition(source["scroll_position"], `${path}.scroll_position`, out);
  }
  if (source["captured_commit"] !== undefined) {
    validateCommitSha(source["captured_commit"], `${path}.captured_commit`, out);
  }
  if (source["screenshot_artefact_id"] !== undefined) {
    validateIdentifier(source["screenshot_artefact_id"], `${path}.screenshot_artefact_id`, out);
  }
  if (source["acceptance_criteria"] !== undefined) {
    validateBodyText(source["acceptance_criteria"], `${path}.acceptance_criteria`, out);
  }
  if (source["claimed_by"] !== undefined) {
    validateActorReference(source["claimed_by"], `${path}.claimed_by`, out);
  }
  if (source["annotation_count"] !== undefined) {
    validateRecordCount(source["annotation_count"], `${path}.annotation_count`, out);
  }
  if (source["resource_uri"] !== undefined) {
    validateResourceUri(source["resource_uri"], `${path}.resource_uri`, out);
  }
  if (source["untrusted_fields"] !== undefined) {
    validateFindingViewUntrustedFields(source["untrusted_fields"], `${path}.untrusted_fields`, out);
  }
}

/**
 * A browser session the agent session may capture from, with the control epoch a capture
 * must present. The epoch is here so an agent does not have to guess it, and a superseded
 * one is refused rather than executed (ADR-0007).
 */
export function validateBrowserSessionView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "status", "control_epoch", "controller", "viewport", "service_origin"], ["id", "status", "control_epoch", "viewport"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["status"] !== undefined) {
    validateReasonText(source["status"], `${path}.status`, out);
  }
  if (source["control_epoch"] !== undefined) {
    validateControlEpoch(source["control_epoch"], `${path}.control_epoch`, out);
  }
  if (source["controller"] !== undefined) {
    validateActorReference(source["controller"], `${path}.controller`, out);
  }
  if (source["viewport"] !== undefined) {
    validateViewport(source["viewport"], `${path}.viewport`, out);
  }
  if (source["service_origin"] !== undefined) {
    validateCapturedUrl(source["service_origin"], `${path}.service_origin`, out);
  }
}

/**
 * Session initialisation input of docs/MCP_SPEC.md section 4. The client name and version
 * come from the MCP initialize handshake; the hints and capabilities come from the
 * endpoint the client was configured with, because MCP's own handshake has nowhere to
 * carry them.
 */
export function validateSessionInitialisationRequest(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["client", "project_hint", "workspace_hint", "capabilities"], ["client"]);
  if (source === null) return;
  if (source["client"] !== undefined) {
    validateClientIdentity(source["client"], `${path}.client`, out);
  }
  if (source["project_hint"] !== undefined) {
    validateProjectHint(source["project_hint"], `${path}.project_hint`, out);
  }
  if (source["workspace_hint"] !== undefined) {
    validateWorkspaceHint(source["workspace_hint"], `${path}.workspace_hint`, out);
  }
  if (source["capabilities"] !== undefined) {
    validateClientCapabilities(source["capabilities"], `${path}.capabilities`, out);
  }
}

/**
 * The connecting client, as it names itself. It is recorded on the agent session for the
 * audit trail and is never an authorisation input: the credential decides what the session
 * may do.
 */
export function validateClientIdentity(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["name", "version"], ["name", "version"]);
  if (source === null) return;
  if (source["name"] !== undefined) {
    validateClientName(source["name"], `${path}.name`, out);
  }
  if (source["version"] !== undefined) {
    validateClientVersion(source["version"], `${path}.version`, out);
  }
}

/**
 * Capabilities the credential carries. The session cannot exceed them.
 */
export function validateSessionInitialisationResultGrantedCapabilities(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateAgentCapability(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Session initialisation output of docs/MCP_SPEC.md section 4.
 */
export function validateSessionInitialisationResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["agent_session_id", "project", "workspace", "capabilities", "granted_capabilities", "expires_at"], ["agent_session_id", "project", "capabilities", "granted_capabilities"]);
  if (source === null) return;
  if (source["agent_session_id"] !== undefined) {
    validateIdentifier(source["agent_session_id"], `${path}.agent_session_id`, out);
  }
  if (source["project"] !== undefined) {
    validateProjectReference(source["project"], `${path}.project`, out);
  }
  if (source["workspace"] !== undefined) {
    validateWorkspaceReference(source["workspace"], `${path}.workspace`, out);
  }
  if (source["capabilities"] !== undefined) {
    validateServerCapabilities(source["capabilities"], `${path}.capabilities`, out);
  }
  if (source["granted_capabilities"] !== undefined) {
    validateSessionInitialisationResultGrantedCapabilities(source["granted_capabilities"], `${path}.granted_capabilities`, out);
  }
  if (source["expires_at"] !== undefined) {
    validateTimestamp(source["expires_at"], `${path}.expires_at`, out);
  }
}

/**
 * Re-read the workspace branch and commit before answering, for the recheck
 * docs/MCP_SPEC.md section 7.1 recommends before verification.
 */
export function validateProjectCurrentInputRefreshSourceState(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Arguments of project_current.
 */
export function validateProjectCurrentInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["refresh_source_state"], []);
  if (source === null) return;
  if (source["refresh_source_state"] !== undefined) {
    validateProjectCurrentInputRefreshSourceState(source["refresh_source_state"], `${path}.refresh_source_state`, out);
  }
}

/**
 * Optional sections to include.
 */
export function validateAgentSessionStatusInputInclude(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 4, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateSessionInclude(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Arguments of agent_session_status.
 */
export function validateAgentSessionStatusInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["include"], []);
  if (source === null) return;
  if (source["include"] !== undefined) {
    validateAgentSessionStatusInputInclude(source["include"], `${path}.include`, out);
  }
}

/**
 * Statuses to include. Defaults to pending and acknowledged, which is the work an agent
 * still has in hand.
 */
export function validateAgentInboxListInputStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 5, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateInboxItemStatus(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Arguments of agent_inbox_list. There is no idempotency key because retrieval changes
 * nothing: reading an inbox twice reads the same inbox (docs/DOMAIN_MODEL.md section 21).
 */
export function validateAgentInboxListInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["status", "limit", "cursor"], []);
  if (source === null) return;
  if (source["status"] !== undefined) {
    validateAgentInboxListInputStatus(source["status"], `${path}.status`, out);
  }
  if (source["limit"] !== undefined) {
    validatePageLimit(source["limit"], `${path}.limit`, out);
  }
  if (source["cursor"] !== undefined) {
    validateCursor(source["cursor"], `${path}.cursor`, out);
  }
}

/**
 * Arguments of agent_inbox_acknowledge. Acknowledging records receipt; there is no
 * argument that could record completion, because completing the work is a different act
 * with different evidence.
 */
export function validateAgentInboxAcknowledgeInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["inbox_item_id", "idempotency_key"], ["inbox_item_id", "idempotency_key"]);
  if (source === null) return;
  if (source["inbox_item_id"] !== undefined) {
    validateIdentifier(source["inbox_item_id"], `${path}.inbox_item_id`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Statuses to include. Defaults to every active status.
 */
export function validateReviewListInputStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 9, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReviewStatus(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Only reviews assigned to or claimed by this agent session.
 */
export function validateReviewListInputAssignedToMe(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Arguments of review_list. Every member narrows within the session's project; none of
 * them names a project, so none of them can widen beyond it.
 */
export function validateReviewListInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["status", "assigned_to_me", "slug_prefix", "updated_since", "limit", "cursor"], []);
  if (source === null) return;
  if (source["status"] !== undefined) {
    validateReviewListInputStatus(source["status"], `${path}.status`, out);
  }
  if (source["assigned_to_me"] !== undefined) {
    validateReviewListInputAssignedToMe(source["assigned_to_me"], `${path}.assigned_to_me`, out);
  }
  if (source["slug_prefix"] !== undefined) {
    validateSlug(source["slug_prefix"], `${path}.slug_prefix`, out);
  }
  if (source["updated_since"] !== undefined) {
    validateTimestamp(source["updated_since"], `${path}.updated_since`, out);
  }
  if (source["limit"] !== undefined) {
    validatePageLimit(source["limit"], `${path}.limit`, out);
  }
  if (source["cursor"] !== undefined) {
    validateCursor(source["cursor"], `${path}.cursor`, out);
  }
}

/**
 * Arguments of review_search. The absence of a project argument is the security property:
 * the statement that reads the rows is bound to this session's project, so a cross-project
 * search is not something a caller can ask for and be refused — it is something there is
 * no way to express (docs/MCP_SPEC.md section 7.6).
 */
export function validateReviewSearchInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["query", "limit"], ["query"]);
  if (source === null) return;
  if (source["query"] !== undefined) {
    validateSearchQuery(source["query"], `${path}.query`, out);
  }
  if (source["limit"] !== undefined) {
    validatePageLimit(source["limit"], `${path}.limit`, out);
  }
}

/**
 * Optional sections to include.
 */
export function validateReviewGetInputInclude(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 4, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReviewInclude(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Arguments of review_get. The review is resolved inside the current project only: a slug
 * that exists in another project is not found, and no cross-project search happens
 * (docs/MCP_SPEC.md section 7.6).
 */
export function validateReviewGetInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review", "include", "findings_limit", "findings_cursor", "comments_limit", "comments_cursor"], ["review"]);
  if (source === null) return;
  if (source["review"] !== undefined) {
    validateReviewSelector(source["review"], `${path}.review`, out);
  }
  if (source["include"] !== undefined) {
    validateReviewGetInputInclude(source["include"], `${path}.include`, out);
  }
  if (source["findings_limit"] !== undefined) {
    validatePageLimit(source["findings_limit"], `${path}.findings_limit`, out);
  }
  if (source["findings_cursor"] !== undefined) {
    validateCursor(source["findings_cursor"], `${path}.findings_cursor`, out);
  }
  if (source["comments_limit"] !== undefined) {
    validatePageLimit(source["comments_limit"], `${path}.comments_limit`, out);
  }
  if (source["comments_cursor"] !== undefined) {
    validateCursor(source["comments_cursor"], `${path}.comments_cursor`, out);
  }
}

/**
 * Arguments of review_claim.
 */
export function validateReviewClaimInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review_id", "expected_version", "idempotency_key"], ["review_id", "expected_version", "idempotency_key"]);
  if (source === null) return;
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Arguments of review_update_status.
 */
export function validateReviewUpdateStatusInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review_id", "expected_version", "status", "reason", "idempotency_key"], ["review_id", "expected_version", "status", "idempotency_key"]);
  if (source === null) return;
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["status"] !== undefined) {
    validateAgentReviewStatus(source["status"], `${path}.status`, out);
  }
  if (source["reason"] !== undefined) {
    validateReasonText(source["reason"], `${path}.reason`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Arguments of review_add_comment. The author is the agent session and is never a field: a
 * caller able to name an author could write in a human's name.
 */
export function validateReviewAddCommentInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review_id", "body", "idempotency_key"], ["review_id", "body", "idempotency_key"]);
  if (source === null) return;
  if (source["review_id"] !== undefined) {
    validateIdentifier(source["review_id"], `${path}.review_id`, out);
  }
  if (source["body"] !== undefined) {
    validateBodyText(source["body"], `${path}.body`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Optional sections to include.
 */
export function validateFindingGetInputInclude(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 4, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateFindingInclude(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Arguments of finding_get.
 */
export function validateFindingGetInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "include", "comments_limit", "comments_cursor"], ["finding_id"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["include"] !== undefined) {
    validateFindingGetInputInclude(source["include"], `${path}.include`, out);
  }
  if (source["comments_limit"] !== undefined) {
    validatePageLimit(source["comments_limit"], `${path}.comments_limit`, out);
  }
  if (source["comments_cursor"] !== undefined) {
    validateCursor(source["comments_cursor"], `${path}.comments_cursor`, out);
  }
}

/**
 * Arguments of finding_claim.
 */
export function validateFindingClaimInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "expected_version", "idempotency_key"], ["finding_id", "expected_version", "idempotency_key"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Arguments of finding_update_status. status is the agent-permitted enumeration, so no
 * argument exists that would ask for a final disposition of a human's finding.
 */
export function validateFindingUpdateStatusInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "expected_version", "status", "resolution_note", "reason", "idempotency_key"], ["finding_id", "expected_version", "status", "idempotency_key"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["status"] !== undefined) {
    validateAgentFindingStatus(source["status"], `${path}.status`, out);
  }
  if (source["resolution_note"] !== undefined) {
    validateBodyText(source["resolution_note"], `${path}.resolution_note`, out);
  }
  if (source["reason"] !== undefined) {
    validateReasonText(source["reason"], `${path}.reason`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
  if (matchesCondition(source["status"], ["BLOCKED"])) {
    requireProperty(source, path, "reason", "A blocked finding must say what is blocking it, because a human has to act on it.", out);
  }
}

/**
 * Arguments of finding_mark_blocked. There is no status argument: the tool is the status,
 * which is why reason can be required by the schema rather than checked afterwards.
 * requested_human_action is what a human is being asked to do about it, and is optional
 * because an agent does not always know.
 */
export function validateFindingMarkBlockedInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "expected_version", "reason", "requested_human_action", "idempotency_key"], ["finding_id", "expected_version", "reason", "idempotency_key"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["reason"] !== undefined) {
    validateReasonText(source["reason"], `${path}.reason`, out);
  }
  if (source["requested_human_action"] !== undefined) {
    validateReasonText(source["requested_human_action"], `${path}.requested_human_action`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Arguments of finding_add_comment.
 */
export function validateFindingAddCommentInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "body", "idempotency_key"], ["finding_id", "body", "idempotency_key"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["body"] !== undefined) {
    validateBodyText(source["body"], `${path}.body`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Viewports the fix was checked at.
 */
export function validateFindingSubmitVerificationInputTestedViewports(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 8, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateViewport(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Evidence. At least one must be a verified screenshot in this project, captured in this
 * project's browser-session lineage.
 */
export function validateFindingSubmitVerificationInputArtefactIds(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: true })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateIdentifier(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Arguments of finding_submit_verification, in the shape of docs/MCP_SPEC.md section 7.7.
 * The server validates evidence ownership and commit context: an artefact belonging to
 * another project is refused, and a commit equal to the one the finding was captured at is
 * not a fix.
 */
export function validateFindingSubmitVerificationInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "expected_version", "summary", "branch", "commit", "tested_viewports", "checks", "artefact_ids", "idempotency_key"], ["finding_id", "summary", "branch", "commit", "tested_viewports", "checks", "artefact_ids", "idempotency_key"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["expected_version"] !== undefined) {
    validateVersionNumber(source["expected_version"], `${path}.expected_version`, out);
  }
  if (source["summary"] !== undefined) {
    validateBodyText(source["summary"], `${path}.summary`, out);
  }
  if (source["branch"] !== undefined) {
    validateBranchName(source["branch"], `${path}.branch`, out);
  }
  if (source["commit"] !== undefined) {
    validateCommitSha(source["commit"], `${path}.commit`, out);
  }
  if (source["tested_viewports"] !== undefined) {
    validateFindingSubmitVerificationInputTestedViewports(source["tested_viewports"], `${path}.tested_viewports`, out);
  }
  if (source["checks"] !== undefined) {
    validateVerificationChecks(source["checks"], `${path}.checks`, out);
  }
  if (source["artefact_ids"] !== undefined) {
    validateFindingSubmitVerificationInputArtefactIds(source["artefact_ids"], `${path}.artefact_ids`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Capture the whole document rather than the viewport.
 */
export function validateBrowserTakeScreenshotInputFullPage(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Arguments of browser_take_screenshot. control_epoch is required by docs/MCP_SPEC.md
 * section 7.4 for every browser tool, and a superseded epoch is refused with
 * CONTROL_EPOCH_STALE rather than captured from a browser somebody else now controls.
 */
export function validateBrowserTakeScreenshotInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["browser_session_id", "control_epoch", "full_page", "purpose", "idempotency_key"], ["browser_session_id", "control_epoch", "purpose", "idempotency_key"]);
  if (source === null) return;
  if (source["browser_session_id"] !== undefined) {
    validateIdentifier(source["browser_session_id"], `${path}.browser_session_id`, out);
  }
  if (source["control_epoch"] !== undefined) {
    validateControlEpoch(source["control_epoch"], `${path}.control_epoch`, out);
  }
  if (source["full_page"] !== undefined) {
    validateBrowserTakeScreenshotInputFullPage(source["full_page"], `${path}.full_page`, out);
  }
  if (source["purpose"] !== undefined) {
    validateScreenshotPurpose(source["purpose"], `${path}.purpose`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Payload of project_current.
 */
export function validateProjectCurrentResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["project", "workspace", "policy"], ["project", "policy"]);
  if (source === null) return;
  if (source["project"] !== undefined) {
    validateProjectReference(source["project"], `${path}.project`, out);
  }
  if (source["workspace"] !== undefined) {
    validateWorkspaceReference(source["workspace"], `${path}.workspace`, out);
  }
  if (source["policy"] !== undefined) {
    validatePolicySummary(source["policy"], `${path}.policy`, out);
  }
}

/**
 * Capabilities the credential carries.
 */
export function validateAgentSessionStatusResultGrantedCapabilities(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateAgentCapability(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Browser sessions this agent session may capture from.
 */
export function validateAgentSessionStatusResultBrowserSessions(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateBrowserSessionView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of agent_session_status.
 */
export function validateAgentSessionStatusResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["agent_session_id", "status", "client", "project", "workspace", "capabilities", "granted_capabilities", "browser_sessions", "inbox_pending_count", "expires_at"], ["agent_session_id", "status", "project", "capabilities", "granted_capabilities"]);
  if (source === null) return;
  if (source["agent_session_id"] !== undefined) {
    validateIdentifier(source["agent_session_id"], `${path}.agent_session_id`, out);
  }
  if (source["status"] !== undefined) {
    validateAgentSessionState(source["status"], `${path}.status`, out);
  }
  if (source["client"] !== undefined) {
    validateClientIdentity(source["client"], `${path}.client`, out);
  }
  if (source["project"] !== undefined) {
    validateProjectReference(source["project"], `${path}.project`, out);
  }
  if (source["workspace"] !== undefined) {
    validateWorkspaceReference(source["workspace"], `${path}.workspace`, out);
  }
  if (source["capabilities"] !== undefined) {
    validateServerCapabilities(source["capabilities"], `${path}.capabilities`, out);
  }
  if (source["granted_capabilities"] !== undefined) {
    validateAgentSessionStatusResultGrantedCapabilities(source["granted_capabilities"], `${path}.granted_capabilities`, out);
  }
  if (source["browser_sessions"] !== undefined) {
    validateAgentSessionStatusResultBrowserSessions(source["browser_sessions"], `${path}.browser_sessions`, out);
  }
  if (source["inbox_pending_count"] !== undefined) {
    validateRecordCount(source["inbox_pending_count"], `${path}.inbox_pending_count`, out);
  }
  if (source["expires_at"] !== undefined) {
    validateTimestamp(source["expires_at"], `${path}.expires_at`, out);
  }
}

/**
 * One page of findings, oldest first, so an agent works them in the order a human recorded
 * them.
 */
export function validateReviewGetResultFindings(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 50, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateFindingView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Evidence links for the findings in this page, as links rather than bytes.
 */
export function validateReviewGetResultArtefactLinks(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 50, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateArtefactLink(value[index], `${path}[${index}]`, out);
  }
}

/**
 * One bounded page of comments on the review itself, oldest first. Comments on a finding
 * are read through finding_get. The page may be shorter than comments_limit asked for:
 * members are added while they fit the response bound of section 13, and
 * comments_next_cursor then names where the next page starts.
 */
export function validateReviewGetResultComments(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 20, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateCommentView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of review_get. findings is one bounded page; findings_next_cursor is present
 * only when more remain.
 */
export function validateReviewGetResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review", "findings", "findings_next_cursor", "artefact_links", "comments", "comments_next_cursor", "staleness"], ["review"]);
  if (source === null) return;
  if (source["review"] !== undefined) {
    validateReviewView(source["review"], `${path}.review`, out);
  }
  if (source["findings"] !== undefined) {
    validateReviewGetResultFindings(source["findings"], `${path}.findings`, out);
  }
  if (source["findings_next_cursor"] !== undefined) {
    validateCursor(source["findings_next_cursor"], `${path}.findings_next_cursor`, out);
  }
  if (source["artefact_links"] !== undefined) {
    validateReviewGetResultArtefactLinks(source["artefact_links"], `${path}.artefact_links`, out);
  }
  if (source["comments"] !== undefined) {
    validateReviewGetResultComments(source["comments"], `${path}.comments`, out);
  }
  if (source["comments_next_cursor"] !== undefined) {
    validateCursor(source["comments_next_cursor"], `${path}.comments_next_cursor`, out);
  }
  if (source["staleness"] !== undefined) {
    validateReviewStaleness(source["staleness"], `${path}.staleness`, out);
  }
}

/**
 * The page, newest first. An empty project answers with an empty array rather than a
 * refusal.
 */
export function validateReviewListResultReviews(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 50, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReviewView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of review_list: one bounded page of the current project's reviews.
 */
export function validateReviewListResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["reviews", "next_cursor"], ["reviews"]);
  if (source === null) return;
  if (source["reviews"] !== undefined) {
    validateReviewListResultReviews(source["reviews"], `${path}.reviews`, out);
  }
  if (source["next_cursor"] !== undefined) {
    validateCursor(source["next_cursor"], `${path}.next_cursor`, out);
  }
}

/**
 * Matching reviews, most recently updated first.
 */
export function validateReviewSearchResultMatches(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 25, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateReviewSearchMatch(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of review_search: reviews of the current project that matched. The project is
 * never a member of this shape, because it is never a choice the caller had.
 */
export function validateReviewSearchResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["matches"], ["matches"]);
  if (source === null) return;
  if (source["matches"] !== undefined) {
    validateReviewSearchResultMatches(source["matches"], `${path}.matches`, out);
  }
}

/**
 * Payload of review_add_comment.
 */
export function validateReviewAddCommentResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["comment"], ["comment"]);
  if (source === null) return;
  if (source["comment"] !== undefined) {
    validateCommentView(source["comment"], `${path}.comment`, out);
  }
}

/**
 * The page, oldest first.
 */
export function validateAgentInboxListResultItems(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 50, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateInboxItemView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of agent_inbox_list. Items are oldest first so that assignment order is
 * preserved, and the page is bounded like every other response (docs/MCP_SPEC.md section
 * 13).
 */
export function validateAgentInboxListResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["items", "pending_count", "next_cursor"], ["items", "pending_count"]);
  if (source === null) return;
  if (source["items"] !== undefined) {
    validateAgentInboxListResultItems(source["items"], `${path}.items`, out);
  }
  if (source["pending_count"] !== undefined) {
    validateRecordCount(source["pending_count"], `${path}.pending_count`, out);
  }
  if (source["next_cursor"] !== undefined) {
    validateCursor(source["next_cursor"], `${path}.next_cursor`, out);
  }
}

/**
 * Payload of agent_inbox_acknowledge. The item is returned as it now stands, still naming
 * the work it delivered: acknowledgement is receipt and never completion.
 */
export function validateAgentInboxAcknowledgeResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["item", "previous_status"], ["item"]);
  if (source === null) return;
  if (source["item"] !== undefined) {
    validateInboxItemView(source["item"], `${path}.item`, out);
  }
  if (source["previous_status"] !== undefined) {
    validateInboxItemStatus(source["previous_status"], `${path}.previous_status`, out);
  }
}

/**
 * Payload of review_claim and review_update_status: the review as it now stands.
 */
export function validateReviewMutationResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review", "previous_status"], ["review"]);
  if (source === null) return;
  if (source["review"] !== undefined) {
    validateReviewView(source["review"], `${path}.review`, out);
  }
  if (source["previous_status"] !== undefined) {
    validateReviewStatus(source["previous_status"], `${path}.previous_status`, out);
  }
}

/**
 * Marks on the original screenshot, with their text alternatives.
 */
export function validateFindingGetResultAnnotations(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 32, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateAnnotationView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Comments on the finding, oldest first. The page may be shorter than comments_limit asked
 * for: members are added while they fit the response bound of section 13, and
 * comments_next_cursor then names where the next page starts.
 */
export function validateFindingGetResultComments(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 20, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateCommentView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Evidence links.
 */
export function validateFindingGetResultArtefactLinks(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateArtefactLink(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of finding_get.
 */
export function validateFindingGetResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding", "annotations", "comments", "comments_next_cursor", "artefact_links", "latest_verification"], ["finding"]);
  if (source === null) return;
  if (source["finding"] !== undefined) {
    validateFindingView(source["finding"], `${path}.finding`, out);
  }
  if (source["annotations"] !== undefined) {
    validateFindingGetResultAnnotations(source["annotations"], `${path}.annotations`, out);
  }
  if (source["comments"] !== undefined) {
    validateFindingGetResultComments(source["comments"], `${path}.comments`, out);
  }
  if (source["comments_next_cursor"] !== undefined) {
    validateCursor(source["comments_next_cursor"], `${path}.comments_next_cursor`, out);
  }
  if (source["artefact_links"] !== undefined) {
    validateFindingGetResultArtefactLinks(source["artefact_links"], `${path}.artefact_links`, out);
  }
  if (source["latest_verification"] !== undefined) {
    validateVerificationView(source["latest_verification"], `${path}.latest_verification`, out);
  }
}

/**
 * Payload of finding_claim and finding_update_status: the finding as it now stands.
 */
export function validateFindingMutationResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding", "previous_status"], ["finding"]);
  if (source === null) return;
  if (source["finding"] !== undefined) {
    validateFindingView(source["finding"], `${path}.finding`, out);
  }
  if (source["previous_status"] !== undefined) {
    validateFindingStatus(source["previous_status"], `${path}.previous_status`, out);
  }
}

/**
 * Payload of finding_add_comment.
 */
export function validateFindingAddCommentResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["comment"], ["comment"]);
  if (source === null) return;
  if (source["comment"] !== undefined) {
    validateCommentView(source["comment"], `${path}.comment`, out);
  }
}

/**
 * What this submission still does not satisfy. Empty means the evidence gate would now
 * permit AWAITING_HUMAN_REVIEW, and nothing more than that.
 */
export function validateFindingSubmitVerificationResultMissing(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of finding_submit_verification. The finding is returned beside the verification
 * so the agent can see the status the submission moved it to without a second call, and
 * the outstanding requirements beside both so it learns what is still missing here rather
 * than from a refusal on the next transition.
 */
export function validateFindingSubmitVerificationResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["verification", "finding", "requirements", "missing"], ["verification", "finding", "requirements", "missing"]);
  if (source === null) return;
  if (source["verification"] !== undefined) {
    validateVerificationView(source["verification"], `${path}.verification`, out);
  }
  if (source["finding"] !== undefined) {
    validateFindingView(source["finding"], `${path}.finding`, out);
  }
  if (source["requirements"] !== undefined) {
    validateCompletionRequirements(source["requirements"], `${path}.requirements`, out);
  }
  if (source["missing"] !== undefined) {
    validateFindingSubmitVerificationResultMissing(source["missing"], `${path}.missing`, out);
  }
}

/**
 * Whether the whole document was captured.
 */
export function validateBrowserTakeScreenshotResultFullPage(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Payload of browser_take_screenshot. It carries a link and metadata and never image
 * bytes, so the response is bounded whatever the page contains.
 */
export function validateBrowserTakeScreenshotResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["artefact", "browser_session_id", "captured_at", "viewport", "full_page", "url"], ["artefact", "browser_session_id", "captured_at", "viewport", "full_page"]);
  if (source === null) return;
  if (source["artefact"] !== undefined) {
    validateArtefactLink(source["artefact"], `${path}.artefact`, out);
  }
  if (source["browser_session_id"] !== undefined) {
    validateIdentifier(source["browser_session_id"], `${path}.browser_session_id`, out);
  }
  if (source["captured_at"] !== undefined) {
    validateTimestamp(source["captured_at"], `${path}.captured_at`, out);
  }
  if (source["viewport"] !== undefined) {
    validateViewport(source["viewport"], `${path}.viewport`, out);
  }
  if (source["full_page"] !== undefined) {
    validateBrowserTakeScreenshotResultFullPage(source["full_page"], `${path}.full_page`, out);
  }
  if (source["url"] !== undefined) {
    validateCapturedUrl(source["url"], `${path}.url`, out);
  }
}

/**
 * MCP product protocol version (docs/MCP_SPEC.md section 14).
 */
export function validateToolRefusalProtocolVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1 });
}

/**
 * Always false here.
 */
export function validateToolRefusalOk(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Degradations that also applied.
 */
export function validateToolRefusalWarnings(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 8, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateWarning(value[index], `${path}[${index}]`, out);
  }
}

/**
 * The error envelope of docs/MCP_SPEC.md section 5. It is deliberately not a frame: a
 * refusal carries no type-selected data, and inventing an empty one would make an agent's
 * success test depend on a field rather than on ok.
 */
export function validateToolRefusal(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["protocol_version", "ok", "request_id", "type", "error", "trust", "instruction_policy", "warnings"], ["protocol_version", "ok", "request_id", "type", "error", "trust", "instruction_policy"]);
  if (source === null) return;
  if (source["protocol_version"] !== undefined) {
    validateToolRefusalProtocolVersion(source["protocol_version"], `${path}.protocol_version`, out);
  }
  if (source["ok"] !== undefined) {
    validateToolRefusalOk(source["ok"], `${path}.ok`, out);
  }
  if (source["request_id"] !== undefined) {
    validateRequestId(source["request_id"], `${path}.request_id`, out);
  }
  if (source["type"] !== undefined) {
    validateMessageType(source["type"], `${path}.type`, out);
  }
  if (source["error"] !== undefined) {
    validateToolError(source["error"], `${path}.error`, out);
  }
  if (source["trust"] !== undefined) {
    validateTrustLabel(source["trust"], `${path}.trust`, out);
  }
  if (source["instruction_policy"] !== undefined) {
    validateInstructionPolicy(source["instruction_policy"], `${path}.instruction_policy`, out);
  }
  if (source["warnings"] !== undefined) {
    validateToolRefusalWarnings(source["warnings"], `${path}.warnings`, out);
  }
}

/**
 * MCP product protocol version. Stage 0 pins 1; any other value is refused rather than
 * best-effort parsed.
 */
export function validateEnvelopeProtocolVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1 });
}

/**
 * Whether the call succeeded. True in this shape; a refusal uses tool_refusal.
 */
export function validateEnvelopeOk(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Degradations that did not stop the call succeeding.
 */
export function validateEnvelopeWarnings(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 8, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateWarning(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Tool result. Its schema is selected by type, and each is bounded by its own x-max-bytes,
 * which is the per-tool size limit of section 13.
 */
export function validateEnvelopeData(value: unknown, path: string, out: SchemaViolation[]): void {
  checkPlainObject(value, path, out);
}

/**
 * The common response envelope of docs/MCP_SPEC.md section 5, extended with the protocol
 * version of section 14, the tool that answered and the instruction policy of
 * docs/SECURITY.md section 11. Additive fields are permitted within a major version and a
 * client ignores what it does not know (docs/API.md section 20). data is the payload
 * selected by type.
 */
export function validateEnvelope(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["protocol_version", "ok", "request_id", "type", "trust", "instruction_policy", "warnings", "data"], ["protocol_version", "ok", "request_id", "type", "trust", "instruction_policy", "data"]);
  if (source === null) return;
  if (source["protocol_version"] !== undefined) {
    validateEnvelopeProtocolVersion(source["protocol_version"], `${path}.protocol_version`, out);
  }
  if (source["ok"] !== undefined) {
    validateEnvelopeOk(source["ok"], `${path}.ok`, out);
  }
  if (source["request_id"] !== undefined) {
    validateRequestId(source["request_id"], `${path}.request_id`, out);
  }
  if (source["type"] !== undefined) {
    validateMessageType(source["type"], `${path}.type`, out);
  }
  if (source["trust"] !== undefined) {
    validateTrustLabel(source["trust"], `${path}.trust`, out);
  }
  if (source["instruction_policy"] !== undefined) {
    validateInstructionPolicy(source["instruction_policy"], `${path}.instruction_policy`, out);
  }
  if (source["warnings"] !== undefined) {
    validateEnvelopeWarnings(source["warnings"], `${path}.warnings`, out);
  }
  if (source["data"] !== undefined) {
    validateEnvelopeData(source["data"], `${path}.data`, out);
  }
}

/**
 * Origin an authorised browser session opens. It is usable only by a session the route
 * names, and only with a capability the control plane mints; it is not reachable from the
 * public internet.
 */
export function validateDevelopmentServiceViewInternalOrigin(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 256, pattern: PATTERN_17 });
}

/**
 * Destination the connector reported it opened. Absent until the route is ready.
 */
export function validateDevelopmentServiceViewObservedDestination(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 3, maxLength: 64, pattern: PATTERN_18 });
}

/**
 * A published development service as an agent sees it. It carries the origin a browser
 * session opens and never a capability: a capability is minted for a browser session, and
 * an agent drives a session rather than presenting one itself (docs/ARCHITECTURE.md
 * section 7.3).
 */
export function validateDevelopmentServiceView(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["id", "status", "workspace_id", "local_host", "local_port", "protocol", "internal_origin", "observed_destination", "failure_class", "expires_at"], ["id", "status", "local_host", "local_port", "protocol", "internal_origin", "expires_at"]);
  if (source === null) return;
  if (source["id"] !== undefined) {
    validateIdentifier(source["id"], `${path}.id`, out);
  }
  if (source["status"] !== undefined) {
    validatePublishedServiceStatus(source["status"], `${path}.status`, out);
  }
  if (source["workspace_id"] !== undefined) {
    validateIdentifier(source["workspace_id"], `${path}.workspace_id`, out);
  }
  if (source["local_host"] !== undefined) {
    validateLocalHost(source["local_host"], `${path}.local_host`, out);
  }
  if (source["local_port"] !== undefined) {
    validateLocalPort(source["local_port"], `${path}.local_port`, out);
  }
  if (source["protocol"] !== undefined) {
    validateDestinationProtocol(source["protocol"], `${path}.protocol`, out);
  }
  if (source["internal_origin"] !== undefined) {
    validateDevelopmentServiceViewInternalOrigin(source["internal_origin"], `${path}.internal_origin`, out);
  }
  if (source["observed_destination"] !== undefined) {
    validateDevelopmentServiceViewObservedDestination(source["observed_destination"], `${path}.observed_destination`, out);
  }
  if (source["failure_class"] !== undefined) {
    validateErrorClass(source["failure_class"], `${path}.failure_class`, out);
  }
  if (source["expires_at"] !== undefined) {
    validateTimestamp(source["expires_at"], `${path}.expires_at`, out);
  }
}

/**
 * Arguments of development_services_list. It names no project: the project is the
 * session's, and a project argument would be an authorisation input chosen by the caller.
 */
export function validateDevelopmentServicesListInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["limit"], []);
  if (source === null) return;
  if (source["limit"] !== undefined) {
    validatePageLimit(source["limit"], `${path}.limit`, out);
  }
}

/**
 * How long the route may live. It is bounded by the deployment's maximum route lifetime,
 * and a longer request is refused rather than clipped: silently shortening a lifetime
 * would make an agent believe it had access it does not have.
 */
export function validateDevelopmentServicePublishInputTtlSeconds(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 86400 });
}

/**
 * Arguments of development_service_publish (docs/MCP_SPEC.md section 7.2).
 */
export function validateDevelopmentServicePublishInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["workspace_id", "local_host", "local_port", "protocol", "ttl_seconds", "idempotency_key"], ["workspace_id", "local_port", "idempotency_key"]);
  if (source === null) return;
  if (source["workspace_id"] !== undefined) {
    validateIdentifier(source["workspace_id"], `${path}.workspace_id`, out);
  }
  if (source["local_host"] !== undefined) {
    validateLocalHost(source["local_host"], `${path}.local_host`, out);
  }
  if (source["local_port"] !== undefined) {
    validateLocalPort(source["local_port"], `${path}.local_port`, out);
  }
  if (source["protocol"] !== undefined) {
    validateDestinationProtocol(source["protocol"], `${path}.protocol`, out);
  }
  if (source["ttl_seconds"] !== undefined) {
    validateDevelopmentServicePublishInputTtlSeconds(source["ttl_seconds"], `${path}.ttl_seconds`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Arguments of development_service_unpublish.
 */
export function validateDevelopmentServiceUnpublishInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["published_service_id", "idempotency_key"], ["published_service_id", "idempotency_key"]);
  if (source === null) return;
  if (source["published_service_id"] !== undefined) {
    validateIdentifier(source["published_service_id"], `${path}.published_service_id`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Routes of the session's project, newest first.
 */
export function validateDevelopmentServicesListResultServices(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 100, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateDevelopmentServiceView(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of development_services_list.
 */
export function validateDevelopmentServicesListResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["services"], ["services"]);
  if (source === null) return;
  if (source["services"] !== undefined) {
    validateDevelopmentServicesListResultServices(source["services"], `${path}.services`, out);
  }
}

/**
 * Payload of development_service_publish and development_service_unpublish: the record as
 * it stands after the command.
 */
export function validateDevelopmentServiceResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["service"], ["service"]);
  if (source === null) return;
  if (source["service"] !== undefined) {
    validateDevelopmentServiceView(source["service"], `${path}.service`, out);
  }
}

/**
 * Viewports the work must be checked at, written as widthxheight. Stage 1 defaults to
 * 390x844 and 1440x900 (AGENTS.md "Browser-facing work").
 */
export function validateCompletionRequirementsRequiredViewports(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 1, maxItems: 8, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Whether console output must have been reviewed. It is an agent assertion, never a
 * control-plane observation: Stage 1 captures no console artefact.
 */
export function validateCompletionRequirementsConsoleReview(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether failed network requests must have been reviewed. An agent assertion, on the same
 * footing as console_review.
 */
export function validateCompletionRequirementsNetworkReview(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether a verified after screenshot must be linked. This one the control plane checks
 * itself: the artefact must exist, be available, belong to this project and carry a
 * digest.
 */
export function validateCompletionRequirementsFinalScreenshot(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Whether keyboard navigation and visible focus must have been checked. Recorded but not
 * required in Stage 1, so this is false and accessibility_checked never appears in a
 * missing list.
 */
export function validateCompletionRequirementsAccessibilityCheck(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * What this project requires before work counts as done (docs/MCP_SPEC.md section 7.8).
 * The viewports come from the project's default_validation_viewports (docs/DOMAIN_MODEL.md
 * section 6), so a project that changes them changes the gate rather than the gate holding
 * a second copy of the rule. The shape is shared by the web application and this
 * interface, so both describe the requirement in the same words.
 */
export function validateCompletionRequirements(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["required_viewports", "console_review", "network_review", "final_screenshot", "accessibility_check"], ["required_viewports", "console_review", "network_review", "final_screenshot"]);
  if (source === null) return;
  if (source["required_viewports"] !== undefined) {
    validateCompletionRequirementsRequiredViewports(source["required_viewports"], `${path}.required_viewports`, out);
  }
  if (source["console_review"] !== undefined) {
    validateCompletionRequirementsConsoleReview(source["console_review"], `${path}.console_review`, out);
  }
  if (source["network_review"] !== undefined) {
    validateCompletionRequirementsNetworkReview(source["network_review"], `${path}.network_review`, out);
  }
  if (source["final_screenshot"] !== undefined) {
    validateCompletionRequirementsFinalScreenshot(source["final_screenshot"], `${path}.final_screenshot`, out);
  }
  if (source["accessibility_check"] !== undefined) {
    validateCompletionRequirementsAccessibilityCheck(source["accessibility_check"], `${path}.accessibility_check`, out);
  }
}

/**
 * What the control plane checked for itself before recording the verification.
 */
export function validateEvidenceAssuranceVerifiedByControlPlane(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 12, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * What the submitter claimed and nothing confirmed. Empty is a truthful answer.
 */
export function validateEvidenceAssuranceAssertedByAgent(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 12, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Who established each piece of evidence. The completion gate MUST NOT present an agent's
 * checks object as though the control plane had confirmed it: the artefacts are what the
 * control plane verified, and the checks are a claim attributed to the actor that made it
 * (docs/DOMAIN_MODEL.md section 19, docs/MCP_SPEC.md section 7.8).
 */
export function validateEvidenceAssurance(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["verified_by_control_plane", "asserted_by_agent", "asserted_by"], ["verified_by_control_plane", "asserted_by_agent"]);
  if (source === null) return;
  if (source["verified_by_control_plane"] !== undefined) {
    validateEvidenceAssuranceVerifiedByControlPlane(source["verified_by_control_plane"], `${path}.verified_by_control_plane`, out);
  }
  if (source["asserted_by_agent"] !== undefined) {
    validateEvidenceAssuranceAssertedByAgent(source["asserted_by_agent"], `${path}.asserted_by_agent`, out);
  }
  if (source["asserted_by"] !== undefined) {
    validateActorReference(source["asserted_by"], `${path}.asserted_by`, out);
  }
}

/**
 * Evidence this finding still lacks. Empty when nothing is missing.
 */
export function validateFindingCompletionStateMissing(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * One finding's standing against the project's completion requirements.
 */
export function validateFindingCompletionState(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["finding_id", "status", "result", "missing", "verification_id", "verification_count"], ["finding_id", "status", "result", "missing"]);
  if (source === null) return;
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["status"] !== undefined) {
    validateFindingStatus(source["status"], `${path}.status`, out);
  }
  if (source["result"] !== undefined) {
    validateCompletionResult(source["result"], `${path}.result`, out);
  }
  if (source["missing"] !== undefined) {
    validateFindingCompletionStateMissing(source["missing"], `${path}.missing`, out);
  }
  if (source["verification_id"] !== undefined) {
    validateIdentifier(source["verification_id"], `${path}.verification_id`, out);
  }
  if (source["verification_count"] !== undefined) {
    validateRecordCount(source["verification_count"], `${path}.verification_count`, out);
  }
}

/**
 * Arguments of task_validation_status. The review is resolved inside the current project
 * only, as everywhere else on this interface.
 */
export function validateTaskValidationStatusInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review", "finding_id"], ["review"]);
  if (source === null) return;
  if (source["review"] !== undefined) {
    validateReviewSelector(source["review"], `${path}.review`, out);
  }
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
}

/**
 * Whether the work must be checked in a browser. True while any required viewport is
 * configured.
 */
export function validateTaskValidationStatusResultBrowserRequired(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * What is still missing, deduplicated across the evaluated findings. An empty list is the
 * honest answer that nothing is outstanding, and is not on its own a statement that the
 * work is accepted.
 */
export function validateTaskValidationStatusResultMissing(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 32, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Per-finding standing against the requirements, in the review's order. It is deliberately
 * not called findings and carries no finding view: nothing here came from a page, which is
 * why this response is labelled trusted_control_plane and why an agent must still read the
 * finding itself for its text.
 */
export function validateTaskValidationStatusResultFindingStates(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 50, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateFindingCompletionState(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of task_validation_status, in the shape of docs/MCP_SPEC.md section 7.8. It
 * reports and changes nothing.
 */
export function validateTaskValidationStatusResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["browser_required", "requirements", "missing", "assurance", "finding_states"], ["browser_required", "requirements", "missing", "assurance"]);
  if (source === null) return;
  if (source["browser_required"] !== undefined) {
    validateTaskValidationStatusResultBrowserRequired(source["browser_required"], `${path}.browser_required`, out);
  }
  if (source["requirements"] !== undefined) {
    validateCompletionRequirements(source["requirements"], `${path}.requirements`, out);
  }
  if (source["missing"] !== undefined) {
    validateTaskValidationStatusResultMissing(source["missing"], `${path}.missing`, out);
  }
  if (source["assurance"] !== undefined) {
    validateEvidenceAssurance(source["assurance"], `${path}.assurance`, out);
  }
  if (source["finding_states"] !== undefined) {
    validateTaskValidationStatusResultFindingStates(source["finding_states"], `${path}.finding_states`, out);
  }
}

/**
 * Arguments of task_complete. There is no argument that asserts a result and none that
 * could terminate the session: the caller names the work and the control plane evaluates
 * it.
 */
export function validateTaskCompleteInput(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["review", "finding_id", "summary", "idempotency_key"], ["review", "idempotency_key"]);
  if (source === null) return;
  if (source["review"] !== undefined) {
    validateReviewSelector(source["review"], `${path}.review`, out);
  }
  if (source["finding_id"] !== undefined) {
    validateIdentifier(source["finding_id"], `${path}.finding_id`, out);
  }
  if (source["summary"] !== undefined) {
    validateBodyText(source["summary"], `${path}.summary`, out);
  }
  if (source["idempotency_key"] !== undefined) {
    validateIdempotencyKey(source["idempotency_key"], `${path}.idempotency_key`, out);
  }
}

/**
 * Always false. This tool records validation state and returns requirements; ending the
 * run is the client's decision.
 */
export function validateTaskCompleteResultTerminatesSession(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * What is still missing, deduplicated across the evaluated findings.
 */
export function validateTaskCompleteResultMissing(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 32, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * What to do next, in the vocabulary of this interface. A refusal that says only what is
 * wrong makes an agent guess.
 */
export function validateTaskCompleteResultNextActions(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 16, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateRequirementLabel(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Per-finding standing against the requirements, in the review's order. It is deliberately
 * not called findings and carries no finding view: nothing here came from a page, which is
 * why this response is labelled trusted_control_plane and why an agent must still read the
 * finding itself for its text.
 */
export function validateTaskCompleteResultFindingStates(value: unknown, path: string, out: SchemaViolation[]): void {
  if (!checkArray(value, path, out, { minItems: 0, maxItems: 50, uniqueItems: false })) return;
  for (let index = 0; index < value.length; index += 1) {
    validateFindingCompletionState(value[index], `${path}[${index}]`, out);
  }
}

/**
 * Payload of task_complete. terminates_session is always false and is stated rather than
 * implied, because the tool's name is the one thing about it that could be misread
 * (docs/MCP_SPEC.md section 7.8).
 */
export function validateTaskCompleteResult(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["result", "terminates_session", "requirements", "missing", "assurance", "next_actions", "finding_states"], ["result", "terminates_session", "requirements", "missing", "assurance", "next_actions"]);
  if (source === null) return;
  if (source["result"] !== undefined) {
    validateCompletionResult(source["result"], `${path}.result`, out);
  }
  if (source["terminates_session"] !== undefined) {
    validateTaskCompleteResultTerminatesSession(source["terminates_session"], `${path}.terminates_session`, out);
  }
  if (source["requirements"] !== undefined) {
    validateCompletionRequirements(source["requirements"], `${path}.requirements`, out);
  }
  if (source["missing"] !== undefined) {
    validateTaskCompleteResultMissing(source["missing"], `${path}.missing`, out);
  }
  if (source["assurance"] !== undefined) {
    validateEvidenceAssurance(source["assurance"], `${path}.assurance`, out);
  }
  if (source["next_actions"] !== undefined) {
    validateTaskCompleteResultNextActions(source["next_actions"], `${path}.next_actions`, out);
  }
  if (source["finding_states"] !== undefined) {
    validateTaskCompleteResultFindingStates(source["finding_states"], `${path}.finding_states`, out);
  }
}
