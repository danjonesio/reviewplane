// Code generated by packages/protocol/tools/generate.ts from
// schemas/mcp/v1.schema.json. DO NOT EDIT.

/**
 * Protocol name, as it appears in ReviewPlane MCP interface, version 1.
 */
export const PROTOCOL_NAME = "mcp" as const;

/**
 * Protocol version this build accepts. Stage 0 pins a single version; an envelope carrying
 * any other value is rejected with PROTOCOL_UNSUPPORTED and is never best-effort parsed.
 */
export const PROTOCOL_VERSION = 1 as const;

/**
 * Byte bounds enforced on raw frames before deserialisation.
 */
export const LIMITS = {
  MAX_MCP_RESPONSE_BYTES: 65536,
  MAX_MCP_REQUEST_BYTES: 16384,
} as const;

/**
 * Logical channels of the mcp protocol.
 */
export const CHANNELS = [
  "session",
  "inbox",
  "review",
  "finding",
  "browser",
  "completion",
  "command",
  "development_service",
  "refusal",
] as const;

export type Channel = (typeof CHANNELS)[number];

/**
 * Human-readable purpose of each channel.
 */
export const CHANNEL_DESCRIPTIONS: Readonly<Record<Channel, string>> = {
  "session": "Project and agent-session identity (docs/MCP_SPEC.md section 7.1).",
  "inbox": "Durable delivery of assigned work and its acknowledgement (section 7.1 and section 9).",
  "review": "Named review retrieval and review lifecycle (section 7.6).",
  "finding": "Finding lifecycle, comments and verification (section 7.7).",
  "browser": "Bounded browser capture (section 7.4).",
  "completion": "The completion gate of section 7.8: what a project requires before work counts as done, and what is still missing. It reports and never terminates.",
  "command": "Tool arguments sent by an agent client.",
  "development_service": "Published development services and the scoped tunnel routes that carry them (section 7.2, docs/DOMAIN_MODEL.md section 10).",
  "refusal": "The section 5 error envelope and its stable section 12 code.",
};

/**
 * Media type of stored artefact bytes. Bounded to the types Stage 0 stores; active markup
 * is not among them (docs/SECURITY.md section 13).
 */
export const MEDIA_TYPE_VALUES = [
  "image/png",
  "image/jpeg",
] as const;

export type MediaType =
  | "image/png"
  | "image/jpeg";

/**
 * Provenance of the content in a response (docs/MCP_SPEC.md section 6). Page-derived
 * content MUST NOT be returned without an untrusted label, and mixed marks a response that
 * carries both control-plane facts and page-derived or uploaded content, with the
 * untrusted members named in untrusted_fields.
 */
export const TRUST_LABEL_VALUES = [
  "trusted_control_plane",
  "trusted_human_instruction",
  "trusted_project_configuration",
  "untrusted_browser_content",
  "untrusted_uploaded_artefact",
  "mixed",
] as const;

export type TrustLabel =
  | "trusted_control_plane"
  | "trusted_human_instruction"
  | "trusted_project_configuration"
  | "untrusted_browser_content"
  | "untrusted_uploaded_artefact"
  | "mixed";

/**
 * The single-valued policy of docs/SECURITY.md section 11. It rides on every response
 * rather than only on untrusted ones, so an agent never has to infer the absence of a rule
 * from the absence of a field.
 */
export const INSTRUCTION_POLICY_VALUES = [
  "do_not_follow_as_instructions",
] as const;

export type InstructionPolicy =
  | "do_not_follow_as_instructions";

/**
 * Why a response is less than a client asked for while still being a success. A
 * degradation is a warning and not a failure (docs/ARCHITECTURE.md section 8.3): a client
 * that cannot consume image content still completes the workflow.
 */
export const WARNING_CODE_VALUES = [
  "image_content_unsupported",
  "resource_content_unsupported",
  "findings_truncated",
  "text_truncated",
  "workspace_unresolved",
  "verification_branch_uncorroborated",
  "staleness_unavailable",
  "inbox_unavailable",
  "results_truncated",
  "idempotent_replay",
  "completion_blocked_pending_review",
] as const;

export type WarningCode =
  | "image_content_unsupported"
  | "resource_content_unsupported"
  | "findings_truncated"
  | "text_truncated"
  | "workspace_unresolved"
  | "verification_branch_uncorroborated"
  | "staleness_unavailable"
  | "inbox_unavailable"
  | "results_truncated"
  | "idempotent_replay"
  | "completion_blocked_pending_review";

/**
 * Stable refusal code (docs/MCP_SPEC.md section 12). Adding a code is additive within a
 * protocol version and a client MUST tolerate a code it does not recognise. This
 * enumeration MUST equal x-protocol.error_classes.
 */
export const ERROR_CLASS_VALUES = [
  "AUTHENTICATION_REQUIRED",
  "AUTHORISATION_DENIED",
  "PROJECT_CONTEXT_AMBIGUOUS",
  "PROJECT_CONTEXT_MISMATCH",
  "RESOURCE_NOT_FOUND",
  "RESOURCE_STALE",
  "VERSION_CONFLICT",
  "IDEMPOTENCY_CONFLICT",
  "CONNECTOR_OFFLINE",
  "PUBLISHED_SERVICE_UNAVAILABLE",
  "BROWSER_CAPACITY_EXHAUSTED",
  "BROWSER_SESSION_NOT_ACTIVE",
  "CONTROL_NOT_OWNED",
  "CONTROL_EPOCH_STALE",
  "BROWSER_COMMAND_TIMEOUT",
  "POLICY_DENIED",
  "APPROVAL_REQUIRED",
  "EVIDENCE_REQUIRED",
  "ARTEFACT_UPLOAD_INCOMPLETE",
  "ARTEFACT_STORE_UNAVAILABLE",
  "UNSUPPORTED_CAPABILITY",
  "RATE_LIMITED",
  "INTERNAL_ERROR",
] as const;

export type ErrorClass =
  | "AUTHENTICATION_REQUIRED"
  | "AUTHORISATION_DENIED"
  | "PROJECT_CONTEXT_AMBIGUOUS"
  | "PROJECT_CONTEXT_MISMATCH"
  | "RESOURCE_NOT_FOUND"
  | "RESOURCE_STALE"
  | "VERSION_CONFLICT"
  | "IDEMPOTENCY_CONFLICT"
  | "CONNECTOR_OFFLINE"
  | "PUBLISHED_SERVICE_UNAVAILABLE"
  | "BROWSER_CAPACITY_EXHAUSTED"
  | "BROWSER_SESSION_NOT_ACTIVE"
  | "CONTROL_NOT_OWNED"
  | "CONTROL_EPOCH_STALE"
  | "BROWSER_COMMAND_TIMEOUT"
  | "POLICY_DENIED"
  | "APPROVAL_REQUIRED"
  | "EVIDENCE_REQUIRED"
  | "ARTEFACT_UPLOAD_INCOMPLETE"
  | "ARTEFACT_STORE_UNAVAILABLE"
  | "UNSUPPORTED_CAPABILITY"
  | "RATE_LIMITED"
  | "INTERNAL_ERROR";

/**
 * An available tool. This enumeration is the tool availability set of docs/MCP_SPEC.md
 * section 14 and MUST equal the keys of x-protocol.messages, so a tool the server
 * advertises always has a result schema and a bound.
 */
export const MESSAGE_TYPE_VALUES = [
  "project_current",
  "agent_session_status",
  "agent_inbox_list",
  "agent_inbox_acknowledge",
  "review_list",
  "review_search",
  "review_get",
  "review_claim",
  "review_update_status",
  "review_add_comment",
  "finding_get",
  "finding_claim",
  "finding_update_status",
  "finding_mark_blocked",
  "finding_add_comment",
  "finding_submit_verification",
  "browser_take_screenshot",
  "development_services_list",
  "development_service_publish",
  "development_service_unpublish",
  "task_validation_status",
  "task_complete",
] as const;

export type MessageType =
  | "project_current"
  | "agent_session_status"
  | "agent_inbox_list"
  | "agent_inbox_acknowledge"
  | "review_list"
  | "review_search"
  | "review_get"
  | "review_claim"
  | "review_update_status"
  | "review_add_comment"
  | "finding_get"
  | "finding_claim"
  | "finding_update_status"
  | "finding_mark_blocked"
  | "finding_add_comment"
  | "finding_submit_verification"
  | "browser_take_screenshot"
  | "development_services_list"
  | "development_service_publish"
  | "development_service_unpublish"
  | "task_validation_status"
  | "task_complete";

/**
 * A capability an agent credential may carry (docs/SECURITY.md section 6.3). A tool
 * requires one, and a credential without it is refused with AUTHORISATION_DENIED. An agent
 * session cannot grant itself a capability beyond its issued set (docs/DOMAIN_MODEL.md
 * section 11).
 */
export const AGENT_CAPABILITY_VALUES = [
  "project:read",
  "review:read",
  "review:write",
  "finding:read",
  "finding:write",
  "verification:submit",
  "browser:capture",
  "service:publish",
] as const;

export type AgentCapability =
  | "project:read"
  | "review:read"
  | "review:write"
  | "finding:read"
  | "finding:write"
  | "verification:submit"
  | "browser:capture"
  | "service:publish";

/**
 * Agent-session status (docs/DOMAIN_MODEL.md section 11).
 */
export const AGENT_SESSION_STATE_VALUES = [
  "STARTING",
  "ACTIVE",
  "WAITING",
  "BLOCKED",
  "DISCONNECTED",
  "COMPLETED",
  "FAILED",
  "CANCELLED",
] as const;

export type AgentSessionState =
  | "STARTING"
  | "ACTIVE"
  | "WAITING"
  | "BLOCKED"
  | "DISCONNECTED"
  | "COMPLETED"
  | "FAILED"
  | "CANCELLED";

/**
 * How a review is ordered in a queue (docs/DOMAIN_MODEL.md section 14). It orders and
 * gates nothing: an urgent review and a routine one obey the same lifecycle and the same
 * authority rules.
 */
export const REVIEW_PRIORITY_VALUES = [
  "critical",
  "high",
  "medium",
  "low",
] as const;

export type ReviewPriority =
  | "critical"
  | "high"
  | "medium"
  | "low";

/**
 * Inbox-item status (docs/DOMAIN_MODEL.md section 21). acknowledged means the recipient
 * has seen the item and never that the work is done; completed is the separate later fact.
 */
export const INBOX_ITEM_STATUS_VALUES = [
  "pending",
  "acknowledged",
  "completed",
  "dismissed",
  "expired",
] as const;

export type InboxItemStatus =
  | "pending"
  | "acknowledged"
  | "completed"
  | "dismissed"
  | "expired";

/**
 * Why an inbox item exists. review_assigned is created when a review is assigned to a
 * recipient; finding_reopened is created when a human reopens a finding the recipient
 * worked on. Both are durable: an agent learns what a human wants changed by retrieving
 * them rather than by guessing (docs/MCP_SPEC.md section 9).
 */
export const INBOX_ITEM_TYPE_VALUES = [
  "review_assigned",
  "finding_reopened",
] as const;

export type InboxItemType =
  | "review_assigned"
  | "finding_reopened";

/**
 * Review lifecycle status (docs/DOMAIN_MODEL.md section 14).
 */
export const REVIEW_STATUS_VALUES = [
  "DRAFT",
  "READY",
  "ASSIGNED",
  "IN_PROGRESS",
  "AWAITING_HUMAN_REVIEW",
  "CHANGES_REQUESTED",
  "ACCEPTED",
  "CANCELLED",
  "ARCHIVED",
] as const;

export type ReviewStatus =
  | "DRAFT"
  | "READY"
  | "ASSIGNED"
  | "IN_PROGRESS"
  | "AWAITING_HUMAN_REVIEW"
  | "CHANGES_REQUESTED"
  | "ACCEPTED"
  | "CANCELLED"
  | "ARCHIVED";

/**
 * Review statuses an agent may request. ACCEPTED is absent by construction, which is the
 * AGENTS.md authority rule expressed as a type rather than as a runtime check. The
 * docs/MCP_SPEC.md section 7.6 list also named blocked; docs/DOMAIN_MODEL.md section 14
 * has no such review status and takes precedence, so a blocked agent records the block on
 * the finding it is stuck on.
 */
export const AGENT_REVIEW_STATUS_VALUES = [
  "IN_PROGRESS",
  "AWAITING_HUMAN_REVIEW",
] as const;

export type AgentReviewStatus =
  | "IN_PROGRESS"
  | "AWAITING_HUMAN_REVIEW";

/**
 * Finding lifecycle status (docs/DOMAIN_MODEL.md section 15).
 */
export const FINDING_STATUS_VALUES = [
  "OPEN",
  "CLAIMED",
  "IN_PROGRESS",
  "BLOCKED",
  "FIXED_UNVERIFIED",
  "AWAITING_HUMAN_REVIEW",
  "RESOLVED",
  "REOPENED",
  "WONT_FIX",
  "DUPLICATE",
] as const;

export type FindingStatus =
  | "OPEN"
  | "CLAIMED"
  | "IN_PROGRESS"
  | "BLOCKED"
  | "FIXED_UNVERIFIED"
  | "AWAITING_HUMAN_REVIEW"
  | "RESOLVED"
  | "REOPENED"
  | "WONT_FIX"
  | "DUPLICATE";

/**
 * Finding statuses an agent may request, taken from the transition list of
 * docs/MCP_SPEC.md section 7.7. RESOLVED, WONT_FIX and DUPLICATE are absent: an agent that
 * cannot name a final disposition cannot request one, so the refusal does not depend on a
 * check anybody could forget.
 */
export const AGENT_FINDING_STATUS_VALUES = [
  "CLAIMED",
  "IN_PROGRESS",
  "BLOCKED",
  "FIXED_UNVERIFIED",
  "AWAITING_HUMAN_REVIEW",
] as const;

export type AgentFindingStatus =
  | "CLAIMED"
  | "IN_PROGRESS"
  | "BLOCKED"
  | "FIXED_UNVERIFIED"
  | "AWAITING_HUMAN_REVIEW";

/**
 * Finding severity (docs/DOMAIN_MODEL.md section 15).
 */
export const FINDING_SEVERITY_VALUES = [
  "critical",
  "high",
  "medium",
  "low",
  "suggestion",
] as const;

export type FindingSeverity =
  | "critical"
  | "high"
  | "medium"
  | "low"
  | "suggestion";

/**
 * Whether a human or an agent authored the finding. The acceptance authority rule is
 * decided on it for the life of the finding.
 */
export const FINDING_SOURCE_VALUES = [
  "human",
  "agent",
] as const;

export type FindingSource =
  | "human"
  | "agent";

/**
 * Annotation shape (docs/DOMAIN_MODEL.md section 16).
 */
export const ANNOTATION_TYPE_VALUES = [
  "rectangle",
  "ellipse",
  "arrow",
  "point",
  "numbered_marker",
] as const;

export type AnnotationType =
  | "rectangle"
  | "ellipse"
  | "arrow"
  | "point"
  | "numbered_marker";

/**
 * Artefact kind (docs/DOMAIN_MODEL.md section 20). Stage 0 evidence is a screenshot.
 */
export const ARTEFACT_KIND_VALUES = [
  "screenshot",
  "thumbnail",
  "trace",
  "dom_snapshot",
  "console_log",
  "network_log",
] as const;

export type ArtefactKind =
  | "screenshot"
  | "thumbnail"
  | "trace"
  | "dom_snapshot"
  | "console_log"
  | "network_log";

/**
 * Status of a verification submission (docs/DOMAIN_MODEL.md section 19). An agent produces
 * submitted and nothing else; accepted and rejected are human decisions.
 */
export const VERIFICATION_STATUS_VALUES = [
  "submitted",
  "accepted",
  "rejected",
  "superseded",
] as const;

export type VerificationStatus =
  | "submitted"
  | "accepted"
  | "rejected"
  | "superseded";

/**
 * Who acted (docs/EVENTS.md section 5). The distinction between human_user and
 * agent_session is what the acceptance authority rules are decided on.
 */
export const ACTOR_TYPE_VALUES = [
  "human_user",
  "agent_session",
  "connector",
  "browser_worker",
  "system",
  "integration",
] as const;

export type ActorType =
  | "human_user"
  | "agent_session"
  | "connector"
  | "browser_worker"
  | "system"
  | "integration";

/**
 * Why a capture is being taken. Stage 0 accepts verification only: a capture is evidence,
 * and an unclassified capture cannot be filed under a retention class.
 */
export const SCREENSHOT_PURPOSE_VALUES = [
  "verification",
] as const;

export type ScreenshotPurpose =
  | "verification";

/**
 * Optional sections of a review response. staleness reports the branch and commit the
 * review was captured at and nothing else: the calculation of docs/DOMAIN_MODEL.md section
 * 24 is Stage 2, so the section reports what was recorded and states that no verdict was
 * computed rather than guessing one.
 */
export const REVIEW_INCLUDE_VALUES = [
  "findings",
  "comments",
  "artefact_links",
  "staleness",
] as const;

export type ReviewInclude =
  | "findings"
  | "comments"
  | "artefact_links"
  | "staleness";

/**
 * Which part of a review a search query matched. It is reported so an agent can tell a
 * title match from a match inside a finding a page supplied text for.
 */
export const REVIEW_SEARCH_FIELD_VALUES = [
  "title",
  "slug",
  "description",
  "finding",
] as const;

export type ReviewSearchField =
  | "title"
  | "slug"
  | "description"
  | "finding";

/**
 * Optional sections of a finding response.
 */
export const FINDING_INCLUDE_VALUES = [
  "annotations",
  "comments",
  "verification",
  "artefact_links",
] as const;

export type FindingInclude =
  | "annotations"
  | "comments"
  | "verification"
  | "artefact_links";

/**
 * Optional sections of an agent-session status response.
 */
export const SESSION_INCLUDE_VALUES = [
  "browser_sessions",
  "capabilities",
] as const;

export type SessionInclude =
  | "browser_sessions"
  | "capabilities";

/**
 * Protocol a development service speaks on its local socket. It is declared at publication
 * and is not negotiable per request.
 */
export const DESTINATION_PROTOCOL_VALUES = [
  "http",
  "https",
] as const;

export type DestinationProtocol =
  | "http"
  | "https";

/**
 * Published-service lifecycle status (docs/DOMAIN_MODEL.md section 10).
 */
export const PUBLISHED_SERVICE_STATUS_VALUES = [
  "requested",
  "ready",
  "failed",
  "expired",
  "revoked",
] as const;

export type PublishedServiceStatus =
  | "requested"
  | "ready"
  | "failed"
  | "expired"
  | "revoked";

/**
 * The result of the completion gate (docs/MCP_SPEC.md section 7.8). blocked_pending_review
 * is a correct terminal answer for an agent and not a failure to retry.
 */
export const COMPLETION_RESULT_VALUES = [
  "completed",
  "completed_with_warnings",
  "blocked_missing_evidence",
  "blocked_pending_review",
] as const;

export type CompletionResult =
  | "completed"
  | "completed_with_warnings"
  | "blocked_missing_evidence"
  | "blocked_pending_review";

/**
 * Which side of the trust boundary sends each message type.
 */
export const MESSAGE_DIRECTIONS: Readonly<Record<MessageType, "client_to_server" | "server_to_client">> = {
  "project_current": "server_to_client",
  "agent_session_status": "server_to_client",
  "agent_inbox_list": "server_to_client",
  "agent_inbox_acknowledge": "server_to_client",
  "review_list": "server_to_client",
  "review_search": "server_to_client",
  "review_get": "server_to_client",
  "review_claim": "server_to_client",
  "review_update_status": "server_to_client",
  "review_add_comment": "server_to_client",
  "finding_get": "server_to_client",
  "finding_claim": "server_to_client",
  "finding_update_status": "server_to_client",
  "finding_mark_blocked": "server_to_client",
  "finding_add_comment": "server_to_client",
  "finding_submit_verification": "server_to_client",
  "browser_take_screenshot": "server_to_client",
  "development_services_list": "server_to_client",
  "development_service_publish": "server_to_client",
  "development_service_unpublish": "server_to_client",
  "task_validation_status": "server_to_client",
  "task_complete": "server_to_client",
};

/**
 * Channel each message type travels on.
 */
export const MESSAGE_CHANNELS: Readonly<Record<MessageType, Channel>> = {
  "project_current": "session",
  "agent_session_status": "session",
  "agent_inbox_list": "inbox",
  "agent_inbox_acknowledge": "inbox",
  "review_list": "review",
  "review_search": "review",
  "review_get": "review",
  "review_claim": "review",
  "review_update_status": "review",
  "review_add_comment": "review",
  "finding_get": "finding",
  "finding_claim": "finding",
  "finding_update_status": "finding",
  "finding_mark_blocked": "finding",
  "finding_add_comment": "finding",
  "finding_submit_verification": "finding",
  "browser_take_screenshot": "browser",
  "development_services_list": "development_service",
  "development_service_publish": "development_service",
  "development_service_unpublish": "development_service",
  "task_validation_status": "completion",
  "task_complete": "completion",
};

/**
 * Maximum canonical payload size for each message type. A payload larger than its bound is
 * rejected.
 */
export const PAYLOAD_MAX_BYTES: Readonly<Record<MessageType, number>> = {
  "project_current": 8192,
  "agent_session_status": 16384,
  "agent_inbox_list": 32768,
  "agent_inbox_acknowledge": 8192,
  "review_list": 32768,
  "review_search": 32768,
  "review_get": 65536,
  "review_claim": 8192,
  "review_update_status": 8192,
  "review_add_comment": 8192,
  "finding_get": 32768,
  "finding_claim": 16384,
  "finding_update_status": 16384,
  "finding_mark_blocked": 16384,
  "finding_add_comment": 8192,
  "finding_submit_verification": 32768,
  "browser_take_screenshot": 8192,
  "development_services_list": 32768,
  "development_service_publish": 4096,
  "development_service_unpublish": 4096,
  "task_validation_status": 32768,
  "task_complete": 32768,
};

/**
 * Reasons a frame is refused. These classify local protocol failures; only some map to a
 * wire error class.
 */
export const PROTOCOL_VIOLATION_REASONS = [
  "frame_too_large",
  "malformed_json",
  "unsupported_protocol_version",
  "unknown_message_type",
  "payload_too_large",
  "schema_violation",
  "untrusted_content_mislabelled",
] as const;

export type ProtocolViolationReason = (typeof PROTOCOL_VIOLATION_REASONS)[number];

/**
 * Mcp error class reported for each rejection reason, where the protocol defines one.
 */
export const PROTOCOL_VIOLATION_ERROR_CLASS: Readonly<Record<ProtocolViolationReason, ErrorClass | null>> = {
  "frame_too_large": null,
  "malformed_json": null,
  "unsupported_protocol_version": "UNSUPPORTED_CAPABILITY",
  "unknown_message_type": "UNSUPPORTED_CAPABILITY",
  "payload_too_large": null,
  "schema_violation": null,
  "untrusted_content_mislabelled": "POLICY_DENIED",
};

/**
 * Codes attached to individual schema violations.
 */
export const SCHEMA_VIOLATION_CODES = [
  "type",
  "required",
  "forbidden",
  "unknown_property",
  "enum",
  "const",
  "pattern",
  "too_short",
  "too_long",
  "too_small",
  "too_large",
  "too_few_items",
  "too_many_items",
  "duplicate_items",
  "not_integer",
] as const;

export type SchemaViolationCode = (typeof SCHEMA_VIOLATION_CODES)[number];

/**
 * One schema failure, located by JSON path.
 */
export interface SchemaViolation {
  readonly path: string;
  readonly code: SchemaViolationCode;
  readonly message: string;
}

/**
 * The resource URI forms of docs/MCP_SPEC.md section 8 that Stage 0 serves. A resource
 * enforces exactly the authorisation its equivalent tool does, so a URI belonging to
 * another project is unreachable rather than merely undocumented. trace:// is listed in
 * section 8 and is deliberately absent from Stage 0, which persists no traces.
 */
export const RESOURCE_URI_FORMS = [
  "review://<project-slug>/<review-slug>",
  "review://<project-id>/<review-id>",
  "finding://<finding-id>",
  "artefact://<artefact-id>",
  "screenshot://<artefact-id>",
] as const;

/**
 * The tool availability set docs/MCP_SPEC.md section 14 requires a client to be able to
 * rely on. It equals x-protocol.messages, so a tool cannot be advertised without a result
 * schema. Everything else in the section 7 catalogue is unavailable and is absent from
 * tools/list rather than present and failing: no browser lifecycle or interaction tools,
 * no visual inspection and — the row that matters most — no secret tool of any kind, which
 * is the strongest available form of the rule that no raw secret reaches an agent
 * (docs/SECURITY.md section 12.1). The inbox tools joined it with RVP-49, the
 * development-service tools with RVP-24 and the two completion tools of section 7.8 with
 * RVP-53.
 */
export const TOOL_AVAILABILITY = [
  "project_current",
  "agent_session_status",
  "agent_inbox_list",
  "agent_inbox_acknowledge",
  "review_list",
  "review_search",
  "review_get",
  "review_claim",
  "review_update_status",
  "review_add_comment",
  "finding_get",
  "finding_claim",
  "finding_update_status",
  "finding_mark_blocked",
  "finding_add_comment",
  "finding_submit_verification",
  "browser_take_screenshot",
  "development_services_list",
  "development_service_publish",
  "development_service_unpublish",
  "task_validation_status",
  "task_complete",
] as const;

/**
 * The four results task_complete may return (docs/MCP_SPEC.md section 7.8). None of them
 * terminates the agent: the tool records the evaluation and hands back requirements.
 * blocked_pending_review is not a failure and MUST NOT be retried as though it were — it
 * is the answer when everything available to an agent is done and the decision has passed
 * to a human (docs/DOMAIN_MODEL.md section 15).
 */
export const COMPLETION_RESULTS = [
  "completed",
  "completed_with_warnings",
  "blocked_missing_evidence",
  "blocked_pending_review",
] as const;

/**
 * What the control plane checks for itself before a verification is recorded, as opposed
 * to what an agent asserts. The distinction is the point of the completion gate: an
 * agent's checks object is a claim attributed to the agent, and presenting it as though
 * the control plane had confirmed it would make the gate a mirror rather than a control
 * (docs/DOMAIN_MODEL.md section 19).
 */
export const CONTROL_PLANE_VERIFIED_EVIDENCE = [
  "artefact_project_ownership",
  "artefact_browser_session_lineage",
  "artefact_upload_completed",
  "artefact_integrity_digest",
  "after_screenshot_present",
  "commit_differs_from_capture",
  "branch_matches_workspace",
] as const;

/**
 * The members of the checks object, which are recorded as assertions by the submitting
 * actor and never as control-plane verification (docs/MCP_SPEC.md section 7.8,
 * docs/DOMAIN_MODEL.md section 19). Stage 1 captures no console or network artefact, so
 * there is nothing for the control plane to confirm them against; naming them here is what
 * keeps the gate honest about that.
 */
export const AGENT_ASSERTED_EVIDENCE = [
  "reproduced_before",
  "console_errors_reviewed",
  "network_failures_reviewed",
  "accessibility_checked",
] as const;

/**
 * The finding transitions docs/MCP_SPEC.md section 7.7 permits an agent, written as
 * from:to. The agent-facing status enumeration cannot express a final disposition at all,
 * which is why the denial of AGENTS.md is structural here and a runtime check only in the
 * domain layer beneath it.
 */
export const AGENT_TRANSITIONS = [
  "OPEN:CLAIMED",
  "CLAIMED:IN_PROGRESS",
  "IN_PROGRESS:BLOCKED",
  "IN_PROGRESS:FIXED_UNVERIFIED",
  "FIXED_UNVERIFIED:AWAITING_HUMAN_REVIEW",
  "REOPENED:IN_PROGRESS",
] as const;

/**
 * Conventional prefixes of the opaque identifiers carried by this protocol. They are
 * documentation only: consumers MUST treat identifiers as opaque and validators MUST NOT
 * require a prefix.
 */
export const IDENTIFIER_PREFIXES: Readonly<Record<string, string>> = {
  "agent_session_id": "ags_",
  "project_id": "prj_",
  "organisation_id": "org_",
  "workspace_id": "wsp_",
  "review_id": "rev_",
  "finding_id": "fin_",
  "annotation_id": "ann_",
  "artefact_id": "art_",
  "verification_id": "ver_",
  "comment_id": "cmt_",
  "browser_session_id": "brs_",
  "request_id": "req_",
  "grant_id": "agr_",
};

/**
 * Opaque durable identifier (docs/DOMAIN_MODEL.md section 3). Consumers MUST treat the
 * value as opaque: the schema bounds only its length and character class and never its
 * documented prefix.
 */
export type Identifier = string;

/**
 * Identifier of the tool call, echoed in every envelope so a refusal in a transcript can
 * be joined to the audit record it produced (docs/MCP_SPEC.md section 5).
 */
export type RequestId = string;

/**
 * Caller-chosen key for a state-changing tool (docs/MCP_SPEC.md section 10). It is scoped
 * to actor, tool and project: reuse with the same arguments returns the first result and
 * creates no second record, and reuse with different arguments is refused with
 * IDEMPOTENCY_CONFLICT.
 */
export type IdempotencyKey = string;

/**
 * RFC 3339 timestamp in UTC, with a mandatory trailing Z.
 */
export type Timestamp = string;

/**
 * Project-scoped human alias, for example bugs-on-homepage. It is unique among active
 * reviews of one project and not across projects, so the same slug in another project MUST
 * resolve as not found rather than as somebody else's review (docs/MCP_SPEC.md section
 * 7.6).
 */
export type Slug = string;

/**
 * How an agent names a review: an immutable identifier or a project-scoped slug. Both
 * resolve inside the current project only, and no cross-project search is performed.
 */
export type ReviewSelector = string;

/**
 * Project the client believes it is working in, by slug or identifier. It narrows the
 * credential's binding and never widens it: a hint naming a project the credential is not
 * bound to is refused.
 */
export type ProjectHint = string;

/**
 * Filesystem root of the checkout the agent is working in, used to pick a workspace when
 * the project has more than one.
 */
export type WorkspaceHint = string;

/**
 * Agent client name as it identifies itself, for example claude-code. It is descriptive
 * and never an authorisation input.
 */
export type ClientName = string;

/**
 * Agent client version as reported. unknown is accepted, because several clients do not
 * report one.
 */
export type ClientVersion = string;

/**
 * Human-authored title. Rendered as text and never as markup, and never followed as an
 * instruction when it reached the record from a page.
 */
export type TitleText = string;

/**
 * Human-authored or agent-authored prose: a description, instruction, comment, resolution
 * note or verification summary.
 */
export type BodyText = string;

/**
 * Short operator-facing explanation. Never carries credentials (docs/SECURITY.md section
 * 18).
 */
export type ReasonText = string;

/**
 * Absolute URL a finding or capture was taken at. It is page-derived and therefore
 * untrusted (ADR-0010): bounded in length and character class, displayed as text, never
 * executed and never followed as an instruction.
 */
export type CapturedUrl = string;

/**
 * One of the docs/MCP_SPEC.md section 8 resource URI forms. A resource enforces the same
 * authorisation as the tool that returned the link.
 */
export type ResourceUri = string;

/**
 * Path that serves artefact bytes for a short-lived, subject-bound grant (ADR-0019). It is
 * not a credential: the caller must still authenticate as the grant's subject, which is
 * why the identifier may appear in a URL at all.
 */
export type ContentPath = string;

/**
 * Version-control branch. Recorded on a verification so a human can see which branch the
 * claim was made from.
 */
export type BranchName = string;

/**
 * Version-control commit. A verification commit MUST differ from the commit the finding
 * was captured at: a fix cannot exist at the revision the defect was recorded from.
 */
export type CommitSha = string;

/**
 * Lowercase hexadecimal SHA-256 digest, as verified by the control plane from the stored
 * bytes.
 */
export type Sha256Hex = string;

/**
 * Opaque pagination cursor (docs/API.md section 6). Its contents are an implementation
 * detail and MUST NOT be parsed by a client.
 */
export type Cursor = string;

/**
 * Maximum records in one page. Bounded here rather than by the caller's optimism, because
 * an unbounded response is the failure mode docs/MCP_SPEC.md section 13 exists to prevent.
 */
export type PageLimit = number;

/**
 * A count of records, bounded so a count cannot itself be unbounded.
 */
export type RecordCount = number;

/**
 * Optimistic-concurrency version. A caller presenting a stale value is refused with
 * VERSION_CONFLICT and the current version in the details (docs/MCP_SPEC.md section 11).
 */
export type VersionNumber = number;

/**
 * Monotonic browser-control epoch (ADR-0007). A command presenting a superseded epoch is
 * refused with CONTROL_EPOCH_STALE rather than executed against a browser somebody else
 * now controls.
 */
export type ControlEpoch = number;

/**
 * Byte length the control plane verified.
 */
export type ByteSize = number;

/**
 * A dimension in device pixels of a stored image.
 */
export type PixelExtent = number;

/**
 * An offset in CSS pixels within the captured document.
 */
export type CssPixelOffset = number;

/**
 * Device pixel ratio of the capture.
 */
export type DeviceScaleFactor = number;

/**
 * Ordinal shown inside a numbered marker, so the annotation list and the overlay name the
 * same mark.
 */
export type MarkerNumber = number;

/**
 * A coordinate normalised to the artefact content rectangle, inclusive of both bounds
 * (docs/DOMAIN_MODEL.md section 16).
 */
export type NormalisedCoordinate = number;

/**
 * How long to wait before retrying, returned with RATE_LIMITED.
 */
export type RetryAfterMs = number;

/**
 * Remaining life of a short-lived grant or credential.
 */
export type ExpiresInSeconds = number;

/**
 * Dotted path of a member of data that is page-derived or uploaded. It exists so mixed is
 * actionable: an agent can tell which members of an otherwise trusted record came from a
 * page.
 */
export type UntrustedFieldPath = string;

/**
 * A search term. It is matched literally against the current project's review titles,
 * slugs, descriptions and finding text; it is never interpreted as a pattern, and it
 * carries no project of its own because the project is the session's.
 */
export type SearchQuery = string;

/**
 * Local address of a development service, as a literal IP address. A name would have to be
 * resolved, and a resolver is a rebinding surface (docs/SECURITY.md section 9).
 */
export type LocalHost = string;

/**
 * TCP port a development service listens on.
 */
export type LocalPort = number;

/**
 * One requirement or gap, written for a reader rather than parsed: 390x844 verification,
 * after screenshot, console review. The control plane composes every value, so nothing an
 * agent supplied is echoed back through this member.
 */
export type RequirementLabel = string;

/**
 * An acting principal. The type is always present, because an authority rule that depends
 * on it must never fall back to a default.
 */
export interface ActorReference {
  /**
   * Actor type.
   */
  readonly type: ActorType;
  /**
   * Identifier of the acting principal, where one exists.
   */
  readonly id?: Identifier;
  /**
   * Human-readable label. Never used to establish identity.
   */
  readonly display?: ReasonText;
}

/**
 * Browser viewport in CSS pixels plus the device pixel ratio. A finding records the
 * viewport it was seen at so it can be reproduced at that size.
 */
export interface Viewport {
  /**
   * Viewport width in CSS pixels.
   */
  readonly width: number;
  /**
   * Viewport height in CSS pixels.
   */
  readonly height: number;
  /**
   * Device pixel ratio the capture was taken at.
   */
  readonly device_scale_factor: DeviceScaleFactor;
}

/**
 * Document scroll offset in CSS pixels at capture time.
 */
export interface ScrollPosition {
  /**
   * Horizontal scroll offset.
   */
  readonly x: CssPixelOffset;
  /**
   * Vertical scroll offset.
   */
  readonly y: CssPixelOffset;
}

/**
 * Intrinsic pixel extent of a stored image, which is the frame annotation geometry is
 * normalised against (docs/DOMAIN_MODEL.md section 16).
 */
export interface ContentRectangle {
  /**
   * Intrinsic width in device pixels.
   */
  readonly width_px: PixelExtent;
  /**
   * Intrinsic height in device pixels.
   */
  readonly height_px: PixelExtent;
}

/**
 * Normalised geometry against the artefact content rectangle. Which members a type uses is
 * fixed by the review protocol's own vocabulary; every member here lies between 0 and 1
 * inclusive.
 */
export interface AnnotationGeometry {
  /**
   * Left edge for a box, position for a point or marker, tail for an arrow.
   */
  readonly x: NormalisedCoordinate;
  /**
   * Top edge for a box, position for a point or marker, tail for an arrow.
   */
  readonly y: NormalisedCoordinate;
  /**
   * Box width, for a rectangle or an ellipse.
   */
  readonly width?: NormalisedCoordinate;
  /**
   * Box height, for a rectangle or an ellipse.
   */
  readonly height?: NormalisedCoordinate;
  /**
   * Arrow head, horizontal.
   */
  readonly x2?: NormalisedCoordinate;
  /**
   * Arrow head, vertical.
   */
  readonly y2?: NormalisedCoordinate;
}

/**
 * A project the agent credential is bound to. It is the shape returned in
 * PROJECT_CONTEXT_AMBIGUOUS details, so an agent can disambiguate rather than be told only
 * that it failed.
 */
export interface ProjectReference {
  /**
   * Project identifier.
   */
  readonly id: Identifier;
  /**
   * Project slug, which is what a project_hint is usually written as.
   */
  readonly slug: Slug;
  /**
   * Display name.
   */
  readonly name?: TitleText;
}

/**
 * The checkout the agent is working in (docs/DOMAIN_MODEL.md section 9). Stage 0 has no
 * connector, so a workspace is registered administratively and is absent when none is
 * registered; a verification submitted without one is accepted with a warning rather than
 * refused, because the branch cannot be corroborated but the evidence still can.
 */
export interface WorkspaceReference {
  /**
   * Workspace identifier.
   */
  readonly id: Identifier;
  /**
   * Filesystem root of the checkout, which is what workspace_hint matches on.
   */
  readonly root_path?: WorkspaceHint;
  /**
   * Branch the workspace is on.
   */
  readonly branch: BranchName;
  /**
   * Commit at the workspace head.
   */
  readonly head_commit: CommitSha;
  /**
   * Whether the checkout has uncommitted changes.
   */
  readonly dirty?: boolean;
}

/**
 * What the project requires of an agent before work counts as done, and what it will never
 * let an agent do. The negative statements are as load-bearing as the positive ones: an
 * agent that reads agent_may_accept_findings: false learns the boundary of
 * docs/DOMAIN_MODEL.md section 15 rather than discovering it as a refusal.
 */
export interface PolicySummary {
  /**
   * Always false. A human-authored finding is accepted by a human; an agent submits
   * verification and marks the finding AWAITING_HUMAN_REVIEW.
   */
  readonly agent_may_accept_findings: boolean;
  /**
   * Whether a completion claim must carry verification evidence. Always true in Stage 0:
   * AGENTS.md forbids claiming a fix without it.
   */
  readonly verification_required: boolean;
  /**
   * Always false in Stage 0. No secret tool is exposed at all, which is the strongest form
   * of the no-disclosure rule of docs/SECURITY.md section 12.1.
   */
  readonly secret_tools_available: boolean;
  /**
   * Viewports UI-facing work must be checked at (AGENTS.md "Browser-facing work").
   */
  readonly required_viewports: readonly ReasonText[];
}

/**
 * What this server can do for this session (docs/MCP_SPEC.md section 4 and
 * docs/ARCHITECTURE.md section 8.3). A false value is a fact the client can plan around,
 * which is what makes degradation clear rather than surprising.
 */
export interface ServerCapabilities {
  /**
   * Whether a human can watch the browser session live.
   */
  readonly browser_live: boolean;
  /**
   * Whether this session will be served image content. It is the negotiated result of the
   * client's own image capability, not a property of the server alone.
   */
  readonly image_resources: boolean;
  /**
   * Whether a human may take browser control from the agent.
   */
  readonly human_takeover: boolean;
  /**
   * Whether the inbox workflow of section 9 is available. True where agent_inbox_list and
   * agent_inbox_acknowledge are advertised; a deployment that withdrew them would report
   * false rather than advertise a tool that refuses.
   */
  readonly review_inbox: boolean;
  /**
   * Whether the server pushes messages to the client. False: agents retrieve explicitly
   * (docs/ARCHITECTURE.md section 8.3).
   */
  readonly managed_messages: boolean;
  /**
   * Whether an interrupted session can be resumed.
   */
  readonly session_resume: boolean;
}

/**
 * What the client says it can consume (docs/MCP_SPEC.md section 4). image_content false is
 * the case docs/ARCHITECTURE.md section 8.3 requires clear degradation for: the workflow
 * still completes, with resource links, text and a warning instead of image content.
 */
export interface ClientCapabilities {
  /**
   * Whether the client can read MCP resources.
   */
  readonly resources?: boolean;
  /**
   * Whether the client can consume image content.
   */
  readonly image_content?: boolean;
  /**
   * Whether the client can receive pushed messages.
   */
  readonly managed_messages?: boolean;
  /**
   * Whether the client can resume a session.
   */
  readonly session_resume?: boolean;
}

/**
 * A degradation or a truncation that did not stop the call succeeding.
 */
export interface Warning {
  /**
   * Stable warning code.
   */
  readonly code: WarningCode;
  /**
   * Human-readable explanation.
   */
  readonly message: ReasonText;
  /**
   * What the client can do about it, where there is something to do.
   */
  readonly detail?: ReasonText;
}

/**
 * Machine-readable detail for a refusal. Bounded rather than free-form, so a refusal
 * cannot become a channel for arbitrary content.
 */
export interface ErrorDetails {
  /**
   * Version the record actually holds, returned with VERSION_CONFLICT.
   */
  readonly current_version?: VersionNumber;
  /**
   * Version the caller presented.
   */
  readonly expected_version?: VersionNumber;
  /**
   * Epoch the browser session actually holds, returned with CONTROL_EPOCH_STALE.
   */
  readonly current_epoch?: ControlEpoch;
  /**
   * Path of the offending argument, where one is at fault.
   */
  readonly field?: ReasonText;
  /**
   * Projects the credential is bound to, returned with PROJECT_CONTEXT_AMBIGUOUS. The
   * server never picks one: it returns the list so the agent can name the project it
   * means.
   */
  readonly candidates?: readonly ProjectReference[];
  /**
   * Evidence a completion claim must carry, returned with EVIDENCE_REQUIRED.
   */
  readonly required_evidence?: readonly ReasonText[];
  /**
   * Transitions the agent may perform from the finding's current status, returned when a
   * transition is refused. The refusal states what is possible rather than only what is
   * not.
   */
  readonly allowed_transitions?: readonly ReasonText[];
  /**
   * How long to wait before retrying, returned with RATE_LIMITED.
   */
  readonly retry_after_ms?: RetryAfterMs;
}

/**
 * The refusal object of docs/MCP_SPEC.md section 5. The code is the stable vocabulary of
 * section 12; the message is for a human and is never parsed.
 */
export interface ToolError {
  /**
   * Stable refusal code.
   */
  readonly code: ErrorClass;
  /**
   * Human-readable explanation.
   */
  readonly message: ReasonText;
  /**
   * Whether retrying the same call unchanged could succeed. It is explicit so a client
   * does not have to infer retryability from the code.
   */
  readonly retryable: boolean;
  /**
   * Machine-readable detail.
   */
  readonly details?: ErrorDetails;
}

/**
 * A reference to evidence, never the evidence itself (docs/MCP_SPEC.md sections 7.6 and
 * 13). content_path serves the bytes for a short-lived grant bound to this agent session
 * (ADR-0019); an agent that wants the image fetches it with its own credential or reads
 * the resource, and neither the tool result nor the review response ever embeds it.
 */
export interface ArtefactLink {
  /**
   * Artefact identifier.
   */
  readonly artefact_id: Identifier;
  /**
   * What the bytes are.
   */
  readonly kind: ArtefactKind;
  /**
   * What the artefact shows in context, such as before or after.
   */
  readonly role?: ReasonText;
  /**
   * MCP resource URI for the artefact.
   */
  readonly resource_uri: ResourceUri;
  /**
   * Path serving the bytes for the grant minted with this response.
   */
  readonly content_path?: ContentPath;
  /**
   * When the grant stops working.
   */
  readonly expires_at?: Timestamp;
  /**
   * Media type of the stored bytes.
   */
  readonly content_type: MediaType;
  /**
   * Digest the control plane verified from the stored bytes. It is what ties a claim in a
   * transcript to bytes on disk.
   */
  readonly sha256?: Sha256Hex;
  /**
   * Byte length the control plane verified.
   */
  readonly size_bytes?: ByteSize;
  /**
   * Intrinsic pixel extent of an image artefact.
   */
  readonly content_rectangle?: ContentRectangle;
  /**
   * Provenance of the bytes. A capture or an upload is untrusted.
   */
  readonly trust: TrustLabel;
  /**
   * The policy that applies to anything legible inside the artefact.
   */
  readonly instruction_policy: InstructionPolicy;
}

/**
 * One mark on a finding's original screenshot, as an agent sees it (ADR-0006). The
 * geometry is normalised and the original is untouched.
 */
export interface AnnotationView {
  /**
   * Annotation identifier.
   */
  readonly id: Identifier;
  /**
   * Original artefact the geometry is normalised against.
   */
  readonly artefact_id: Identifier;
  /**
   * Annotation shape.
   */
  readonly type: AnnotationType;
  /**
   * Normalised geometry.
   */
  readonly geometry: AnnotationGeometry;
  /**
   * Text alternative for the mark, which is what makes the annotation legible to an agent
   * that never renders the image.
   */
  readonly label: TitleText;
  /**
   * Ordinal shown inside a numbered marker.
   */
  readonly marker_number?: MarkerNumber;
}

/**
 * One comment on a review or on one of its findings (docs/DOMAIN_MODEL.md section 18).
 * Comments are append-only and the author type is always explicit. review_id is always
 * present and finding_id is absent for a comment on the review itself, which is the same
 * shape the control-plane record carries.
 */
export interface CommentView {
  /**
   * Comment identifier.
   */
  readonly id: Identifier;
  /**
   * Review the comment belongs to, directly or through its finding.
   */
  readonly review_id: Identifier;
  /**
   * Finding the comment is on. Absent when the comment is on the review itself.
   */
  readonly finding_id?: Identifier;
  /**
   * Comment text.
   */
  readonly body: BodyText;
  /**
   * Who wrote it. An agent comment is clearly attributed to the agent session.
   */
  readonly author: ActorReference;
  /**
   * When it was written.
   */
  readonly created_at: Timestamp;
}

/**
 * One durable work notification (docs/DOMAIN_MODEL.md section 21). It names the work
 * rather than carrying it: a review reference and a count, not the review's findings, so
 * an inbox read cannot become an unbounded delivery. status distinguishes receipt from
 * completion, which is the whole reason the record is durable.
 */
export interface InboxItemView {
  /**
   * Inbox-item identifier.
   */
  readonly id: Identifier;
  /**
   * Owning project. It is always this session's project.
   */
  readonly project_id: Identifier;
  /**
   * Why the item exists.
   */
  readonly type: InboxItemType;
  /**
   * What a human called the work. It is human-authored text and never an instruction to
   * follow blindly.
   */
  readonly title: TitleText;
  /**
   * Delivery status. acknowledged means received; it never means the work is done.
   */
  readonly status: InboxItemStatus;
  /**
   * Review the item is about, where there is one.
   */
  readonly review_id?: Identifier;
  /**
   * Project-scoped name of that review, which is what review_get accepts.
   */
  readonly review_slug?: Slug;
  /**
   * Finding the item is about, for a reopened finding.
   */
  readonly finding_id?: Identifier;
  /**
   * Priority of the work. It orders a queue and gates nothing (docs/DOMAIN_MODEL.md
   * section 14).
   */
  readonly priority?: ReviewPriority;
  /**
   * Findings in the referenced review when the item was created.
   */
  readonly finding_count?: RecordCount;
  /**
   * Who caused the item. A human assignment and an automatic reopen are distinguishable
   * here.
   */
  readonly assigned_by?: ActorReference;
  /**
   * When the item was created. The list is ordered by this, oldest first.
   */
  readonly created_at: Timestamp;
  /**
   * When receipt was acknowledged, where it has been.
   */
  readonly acknowledged_at?: Timestamp;
  /**
   * When the work the item delivered was recorded complete, which is a separate later
   * fact.
   */
  readonly completed_at?: Timestamp;
}

/**
 * The source context a review was captured at (docs/DOMAIN_MODEL.md section 24). computed
 * is always false in this stage and is present rather than omitted, so a reader is told
 * that no comparison was made instead of being left to infer it from a missing field. A
 * verdict that had to be guessed would be worse than none: it would either close feedback
 * nobody decided to close, or send an agent to reproduce against the wrong revision.
 */
export interface ReviewStaleness {
  /**
   * Whether a staleness comparison was performed. False here.
   */
  readonly computed: boolean;
  /**
   * Branch the review was captured from.
   */
  readonly captured_branch: BranchName;
  /**
   * Commit the review was captured from.
   */
  readonly captured_commit: CommitSha;
  /**
   * Branch the session's workspace is on, where one is registered. It is reported, not
   * compared.
   */
  readonly workspace_branch?: BranchName;
  /**
   * Head commit of that workspace, where one is registered.
   */
  readonly workspace_head_commit?: CommitSha;
}

/**
 * One review a search matched, with which part of it matched. The excerpt is deliberately
 * absent: a finding's text can carry page-derived content, and returning a fragment of it
 * here would smuggle untrusted bytes into a list response. An agent that wants the text
 * reads the review.
 */
export interface ReviewSearchMatch {
  /**
   * The matching review.
   */
  readonly review: ReviewView;
  /**
   * Which parts of the review matched the query.
   */
  readonly matched: readonly ReviewSearchField[];
}

/**
 * The checks a verification claims to have performed (docs/DOMAIN_MODEL.md section 19).
 * They are recorded as claims by the agent and are never treated as proof on their own:
 * the artefacts are the evidence.
 */
export interface VerificationChecks {
  /**
   * Whether the agent reproduced the defect before changing anything.
   */
  readonly reproduced_before: boolean;
  /**
   * Whether console output was reviewed after the change.
   */
  readonly console_errors_reviewed: boolean;
  /**
   * Whether failed network requests were reviewed after the change.
   */
  readonly network_failures_reviewed: boolean;
  /**
   * Whether keyboard navigation and visible focus were checked.
   */
  readonly accessibility_checked?: boolean;
}

/**
 * A submitted claim with evidence (docs/DOMAIN_MODEL.md section 19). Its status is
 * submitted: a verification is not a resolution, and the human decision that follows is
 * not available to an agent at all.
 */
export interface VerificationView {
  /**
   * Verification identifier.
   */
  readonly verification_id: Identifier;
  /**
   * Finding the verification concerns.
   */
  readonly finding_id: Identifier;
  /**
   * Status of the submission.
   */
  readonly status: VerificationStatus;
  /**
   * What was changed and how it was checked.
   */
  readonly summary: BodyText;
  /**
   * Branch the fix was made on.
   */
  readonly branch: BranchName;
  /**
   * Commit the fix was made in.
   */
  readonly commit: CommitSha;
  /**
   * Viewports the fix was checked at.
   */
  readonly tested_viewports: readonly Viewport[];
  /**
   * Checks the agent claims to have performed. They are claims by submitted_by and are
   * never control-plane observations; assurance says so explicitly so a reader does not
   * have to know it.
   */
  readonly checks: VerificationChecks;
  /**
   * Who established each piece of evidence on this submission.
   */
  readonly assurance?: EvidenceAssurance;
  /**
   * Evidence the submission rests on, as links rather than bytes.
   */
  readonly artefacts?: readonly ArtefactLink[];
  /**
   * Actor that submitted it.
   */
  readonly submitted_by: ActorReference;
  /**
   * Submission time.
   */
  readonly submitted_at: Timestamp;
  /**
   * The verification this one replaced as current, where there was one. The earlier record
   * is marked superseded and kept: a reopen cycle preserves the whole history rather than
   * starting again (docs/DOMAIN_MODEL.md section 19).
   */
  readonly supersedes_verification_id?: Identifier;
  /**
   * The verification that replaced this one. Present only on a record whose status is
   * superseded.
   */
  readonly superseded_by_verification_id?: Identifier;
  /**
   * When this record stopped being the current one.
   */
  readonly superseded_at?: Timestamp;
}

/**
 * A review as an agent sees it. The captured branch and commit are on the record because
 * the point of a review is to still mean something after the code has moved on.
 */
export interface ReviewView {
  /**
   * Review identifier.
   */
  readonly id: Identifier;
  /**
   * Owning project.
   */
  readonly project_id: Identifier;
  /**
   * Project-scoped alias, which is the name a human gave the agent.
   */
  readonly slug: Slug;
  /**
   * Human-facing title.
   */
  readonly title: TitleText;
  /**
   * Instruction from the human who created the review.
   */
  readonly description?: BodyText;
  /**
   * Lifecycle status.
   */
  readonly status: ReviewStatus;
  /**
   * Version to present on the next write.
   */
  readonly version: VersionNumber;
  /**
   * Branch the findings were captured from.
   */
  readonly captured_branch: BranchName;
  /**
   * Commit the findings were captured from.
   */
  readonly captured_commit: CommitSha;
  /**
   * Workspace the captured application was running in.
   */
  readonly captured_workspace_id: Identifier;
  /**
   * Browser session the review was captured from. The review outlives it.
   */
  readonly source_browser_session_id?: Identifier;
  /**
   * Agent session currently working the review.
   */
  readonly claimed_by?: ActorReference;
  /**
   * Findings in the review, which is not the number returned in one page.
   */
  readonly finding_count: RecordCount;
  /**
   * Resource URI for the review.
   */
  readonly resource_uri: ResourceUri;
  /**
   * Creation time.
   */
  readonly created_at: Timestamp;
  /**
   * Time of the last accepted mutation.
   */
  readonly updated_at: Timestamp;
}

/**
 * A finding as an agent sees it, with the captured context that makes it reproducible
 * after the browser session has gone. untrusted_fields names the members that came from a
 * page, which is what makes the mixed trust label of the enclosing response actionable.
 */
export interface FindingView {
  /**
   * Finding identifier.
   */
  readonly id: Identifier;
  /**
   * Review the finding belongs to.
   */
  readonly review_id: Identifier;
  /**
   * Short statement of what is wrong.
   */
  readonly title: TitleText;
  /**
   * Detail the agent needs to reproduce and judge the finding.
   */
  readonly description?: BodyText;
  /**
   * Severity.
   */
  readonly severity: FindingSeverity;
  /**
   * Lifecycle status.
   */
  readonly status: FindingStatus;
  /**
   * Whether a human or an agent authored it. A human-authored finding can only be finally
   * accepted by a human.
   */
  readonly source: FindingSource;
  /**
   * Version to present on the next write.
   */
  readonly version: VersionNumber;
  /**
   * URL the finding was captured at. Page-derived and untrusted.
   */
  readonly url: CapturedUrl;
  /**
   * Viewport and device pixel ratio at capture time.
   */
  readonly viewport: Viewport;
  /**
   * Document scroll offset at capture time.
   */
  readonly scroll_position: ScrollPosition;
  /**
   * Commit the capture was taken from.
   */
  readonly captured_commit: CommitSha;
  /**
   * Original screenshot the annotations are placed on.
   */
  readonly screenshot_artefact_id: Identifier;
  /**
   * What a resolution has to demonstrate.
   */
  readonly acceptance_criteria?: BodyText;
  /**
   * Actor currently working the finding.
   */
  readonly claimed_by?: ActorReference;
  /**
   * Live annotations on the finding.
   */
  readonly annotation_count?: RecordCount;
  /**
   * Resource URI for the finding.
   */
  readonly resource_uri: ResourceUri;
  /**
   * Members of this record that are page-derived. An empty list is impossible: url is
   * always page-derived, so the array is required and always populated.
   */
  readonly untrusted_fields: readonly UntrustedFieldPath[];
}

/**
 * A browser session the agent session may capture from, with the control epoch a capture
 * must present. The epoch is here so an agent does not have to guess it, and a superseded
 * one is refused rather than executed (ADR-0007).
 */
export interface BrowserSessionView {
  /**
   * Browser session identifier.
   */
  readonly id: Identifier;
  /**
   * Lifecycle status of the session.
   */
  readonly status: ReasonText;
  /**
   * Current control epoch.
   */
  readonly control_epoch: ControlEpoch;
  /**
   * Who currently holds control.
   */
  readonly controller?: ActorReference;
  /**
   * Viewport the session is running at.
   */
  readonly viewport: Viewport;
  /**
   * Origin of the published development service the session is bound to.
   */
  readonly service_origin?: CapturedUrl;
}

/**
 * Session initialisation input of docs/MCP_SPEC.md section 4. The client name and version
 * come from the MCP initialize handshake; the hints and capabilities come from the
 * endpoint the client was configured with, because MCP's own handshake has nowhere to
 * carry them.
 */
export interface SessionInitialisationRequest {
  /**
   * Who is connecting.
   */
  readonly client: ClientIdentity;
  /**
   * Project the client believes it is working in.
   */
  readonly project_hint?: ProjectHint;
  /**
   * Checkout root the client is working in.
   */
  readonly workspace_hint?: WorkspaceHint;
  /**
   * What the client can consume.
   */
  readonly capabilities?: ClientCapabilities;
}

/**
 * The connecting client, as it names itself. It is recorded on the agent session for the
 * audit trail and is never an authorisation input: the credential decides what the session
 * may do.
 */
export interface ClientIdentity {
  /**
   * Client name.
   */
  readonly name: ClientName;
  /**
   * Client version.
   */
  readonly version: ClientVersion;
}

/**
 * Session initialisation output of docs/MCP_SPEC.md section 4.
 */
export interface SessionInitialisationResult {
  /**
   * The agent session this connection acts as.
   */
  readonly agent_session_id: Identifier;
  /**
   * Project resolved from the credential and the hints.
   */
  readonly project: ProjectReference;
  /**
   * Workspace resolved, where the project has one.
   */
  readonly workspace?: WorkspaceReference;
  /**
   * What the server will do for this session.
   */
  readonly capabilities: ServerCapabilities;
  /**
   * Capabilities the credential carries. The session cannot exceed them.
   */
  readonly granted_capabilities: readonly AgentCapability[];
  /**
   * When the credential expires. A call after it is refused with AUTHENTICATION_REQUIRED
   * rather than partly executed.
   */
  readonly expires_at?: Timestamp;
}

/**
 * Arguments of project_current.
 */
export interface ProjectCurrentInput {
  /**
   * Re-read the workspace branch and commit before answering, for the recheck
   * docs/MCP_SPEC.md section 7.1 recommends before verification.
   */
  readonly refresh_source_state?: boolean;
}

/**
 * Arguments of agent_session_status.
 */
export interface AgentSessionStatusInput {
  /**
   * Optional sections to include.
   */
  readonly include?: readonly SessionInclude[];
}

/**
 * Arguments of agent_inbox_list. There is no idempotency key because retrieval changes
 * nothing: reading an inbox twice reads the same inbox (docs/DOMAIN_MODEL.md section 21).
 */
export interface AgentInboxListInput {
  /**
   * Statuses to include. Defaults to pending and acknowledged, which is the work an agent
   * still has in hand.
   */
  readonly status?: readonly InboxItemStatus[];
  /**
   * Maximum items in this page. Defaults to 20 and is capped at the schema maximum.
   */
  readonly limit?: PageLimit;
  /**
   * Cursor from a previous page.
   */
  readonly cursor?: Cursor;
}

/**
 * Arguments of agent_inbox_acknowledge. Acknowledging records receipt; there is no
 * argument that could record completion, because completing the work is a different act
 * with different evidence.
 */
export interface AgentInboxAcknowledgeInput {
  /**
   * Item to acknowledge.
   */
  readonly inbox_item_id: Identifier;
  /**
   * Key for this acknowledgement, so a retry acknowledges once.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of review_list. Every member narrows within the session's project; none of
 * them names a project, so none of them can widen beyond it.
 */
export interface ReviewListInput {
  /**
   * Statuses to include. Defaults to every active status.
   */
  readonly status?: readonly ReviewStatus[];
  /**
   * Only reviews assigned to or claimed by this agent session.
   */
  readonly assigned_to_me?: boolean;
  /**
   * Only reviews whose slug starts with this prefix.
   */
  readonly slug_prefix?: Slug;
  /**
   * Only reviews updated at or after this time.
   */
  readonly updated_since?: Timestamp;
  /**
   * Maximum reviews in this page. Defaults to 20 and is capped at the schema maximum.
   */
  readonly limit?: PageLimit;
  /**
   * Cursor from a previous page.
   */
  readonly cursor?: Cursor;
}

/**
 * Arguments of review_search. The absence of a project argument is the security property:
 * the statement that reads the rows is bound to this session's project, so a cross-project
 * search is not something a caller can ask for and be refused — it is something there is
 * no way to express (docs/MCP_SPEC.md section 7.6).
 */
export interface ReviewSearchInput {
  /**
   * Term to match against titles, slugs, descriptions and finding text of this project.
   */
  readonly query: SearchQuery;
  /**
   * Maximum matches. Defaults to 10 and is capped at 25.
   */
  readonly limit?: PageLimit;
}

/**
 * Arguments of review_get. The review is resolved inside the current project only: a slug
 * that exists in another project is not found, and no cross-project search happens
 * (docs/MCP_SPEC.md section 7.6).
 */
export interface ReviewGetInput {
  /**
   * Immutable identifier or project-scoped slug, for example bugs-on-homepage.
   */
  readonly review: ReviewSelector;
  /**
   * Optional sections to include.
   */
  readonly include?: readonly ReviewInclude[];
  /**
   * Maximum findings in this page. Defaults to 20 and is capped at the schema maximum.
   */
  readonly findings_limit?: PageLimit;
  /**
   * Cursor from a previous page.
   */
  readonly findings_cursor?: Cursor;
  /**
   * Maximum comments on the review itself in this page. Defaults to 20.
   */
  readonly comments_limit?: PageLimit;
  /**
   * Cursor from a previous page of comments.
   */
  readonly comments_cursor?: Cursor;
}

/**
 * Arguments of review_claim.
 */
export interface ReviewClaimInput {
  /**
   * Review to claim.
   */
  readonly review_id: Identifier;
  /**
   * Version the agent last read.
   */
  readonly expected_version: VersionNumber;
  /**
   * Key for this claim.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of review_update_status.
 */
export interface ReviewUpdateStatusInput {
  /**
   * Review to move.
   */
  readonly review_id: Identifier;
  /**
   * Version the agent last read.
   */
  readonly expected_version: VersionNumber;
  /**
   * Requested status. ACCEPTED is not expressible.
   */
  readonly status: AgentReviewStatus;
  /**
   * Why, recorded on the transition.
   */
  readonly reason?: ReasonText;
  /**
   * Key for this transition.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of review_add_comment. The author is the agent session and is never a field: a
 * caller able to name an author could write in a human's name.
 */
export interface ReviewAddCommentInput {
  /**
   * Review to comment on.
   */
  readonly review_id: Identifier;
  /**
   * Comment text. It is stored and rendered as text.
   */
  readonly body: BodyText;
  /**
   * Key for this comment, so a retried call does not produce two.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of finding_get.
 */
export interface FindingGetInput {
  /**
   * Finding to read.
   */
  readonly finding_id: Identifier;
  /**
   * Optional sections to include.
   */
  readonly include?: readonly FindingInclude[];
  /**
   * Maximum comments in this page. Defaults to 20.
   */
  readonly comments_limit?: PageLimit;
  /**
   * Cursor from a previous page of comments.
   */
  readonly comments_cursor?: Cursor;
}

/**
 * Arguments of finding_claim.
 */
export interface FindingClaimInput {
  /**
   * Finding to claim.
   */
  readonly finding_id: Identifier;
  /**
   * Version the agent last read. A concurrent claim is refused with VERSION_CONFLICT and
   * the current metadata.
   */
  readonly expected_version: VersionNumber;
  /**
   * Key for this claim.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of finding_update_status. status is the agent-permitted enumeration, so no
 * argument exists that would ask for a final disposition of a human's finding.
 */
export interface FindingUpdateStatusInput {
  /**
   * Finding to move.
   */
  readonly finding_id: Identifier;
  /**
   * Version the agent last read.
   */
  readonly expected_version: VersionNumber;
  /**
   * Requested status, from the section 7.7 list.
   */
  readonly status: AgentFindingStatus;
  /**
   * What was changed and how it was checked. Required to reach FIXED_UNVERIFIED: a
   * completion claim without evidence is refused with EVIDENCE_REQUIRED.
   */
  readonly resolution_note?: BodyText;
  /**
   * Why, recorded on the transition. Required to reach BLOCKED.
   */
  readonly reason?: ReasonText;
  /**
   * Key for this transition.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of finding_mark_blocked. There is no status argument: the tool is the status,
 * which is why reason can be required by the schema rather than checked afterwards.
 * requested_human_action is what a human is being asked to do about it, and is optional
 * because an agent does not always know.
 */
export interface FindingMarkBlockedInput {
  /**
   * Finding to block.
   */
  readonly finding_id: Identifier;
  /**
   * Version the agent last read.
   */
  readonly expected_version: VersionNumber;
  /**
   * What is blocking the work. Required, because a block a human cannot act on is a status
   * change and not a request.
   */
  readonly reason: ReasonText;
  /**
   * What the agent is asking a human to do.
   */
  readonly requested_human_action?: ReasonText;
  /**
   * Key for this transition.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of finding_add_comment.
 */
export interface FindingAddCommentInput {
  /**
   * Finding to comment on.
   */
  readonly finding_id: Identifier;
  /**
   * Comment text. It is stored and rendered as text.
   */
  readonly body: BodyText;
  /**
   * Key for this comment, so a retried call does not produce two.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of finding_submit_verification, in the shape of docs/MCP_SPEC.md section 7.7.
 * The server validates evidence ownership and commit context: an artefact belonging to
 * another project is refused, and a commit equal to the one the finding was captured at is
 * not a fix.
 */
export interface FindingSubmitVerificationInput {
  /**
   * Finding the verification concerns.
   */
  readonly finding_id: Identifier;
  /**
   * Version the agent last read, where the agent wants the submission refused if the
   * finding moved.
   */
  readonly expected_version?: VersionNumber;
  /**
   * What was changed and how it was checked.
   */
  readonly summary: BodyText;
  /**
   * Branch the fix was made on. It is checked against the workspace branch where a
   * workspace is known.
   */
  readonly branch: BranchName;
  /**
   * Commit the fix was made in.
   */
  readonly commit: CommitSha;
  /**
   * Viewports the fix was checked at.
   */
  readonly tested_viewports: readonly Viewport[];
  /**
   * Checks performed.
   */
  readonly checks: VerificationChecks;
  /**
   * Evidence. At least one must be a verified screenshot in this project, captured in this
   * project's browser-session lineage.
   */
  readonly artefact_ids: readonly Identifier[];
  /**
   * Key for this submission. A duplicate produces one verification record.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of browser_take_screenshot. control_epoch is required by docs/MCP_SPEC.md
 * section 7.4 for every browser tool, and a superseded epoch is refused with
 * CONTROL_EPOCH_STALE rather than captured from a browser somebody else now controls.
 */
export interface BrowserTakeScreenshotInput {
  /**
   * Browser session to capture from.
   */
  readonly browser_session_id: Identifier;
  /**
   * Epoch the agent believes is current.
   */
  readonly control_epoch: ControlEpoch;
  /**
   * Capture the whole document rather than the viewport.
   */
  readonly full_page?: boolean;
  /**
   * Why the capture is being taken. Stage 0 accepts verification only.
   */
  readonly purpose: ScreenshotPurpose;
  /**
   * Key for this capture, so a retried call does not store two artefacts.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Payload of project_current.
 */
export interface ProjectCurrentResult {
  /**
   * The project this session is bound to.
   */
  readonly project: ProjectReference;
  /**
   * The workspace, where one is registered.
   */
  readonly workspace?: WorkspaceReference;
  /**
   * What the project requires and what it forbids.
   */
  readonly policy: PolicySummary;
}

/**
 * Payload of agent_session_status.
 */
export interface AgentSessionStatusResult {
  /**
   * This agent session.
   */
  readonly agent_session_id: Identifier;
  /**
   * Session status.
   */
  readonly status: AgentSessionState;
  /**
   * Client that opened the session.
   */
  readonly client?: ClientIdentity;
  /**
   * Project the session is bound to.
   */
  readonly project: ProjectReference;
  /**
   * Workspace the session resolved.
   */
  readonly workspace?: WorkspaceReference;
  /**
   * Negotiated server capabilities.
   */
  readonly capabilities: ServerCapabilities;
  /**
   * Capabilities the credential carries.
   */
  readonly granted_capabilities: readonly AgentCapability[];
  /**
   * Browser sessions this agent session may capture from.
   */
  readonly browser_sessions?: readonly BrowserSessionView[];
  /**
   * Inbox items still pending for this session. It is a count and never the items: an
   * agent that wants them calls agent_inbox_list, so a status call cannot become an
   * unbounded delivery.
   */
  readonly inbox_pending_count?: RecordCount;
  /**
   * When the credential expires.
   */
  readonly expires_at?: Timestamp;
}

/**
 * Payload of review_get. findings is one bounded page; findings_next_cursor is present
 * only when more remain.
 */
export interface ReviewGetResult {
  /**
   * The review.
   */
  readonly review: ReviewView;
  /**
   * One page of findings, oldest first, so an agent works them in the order a human
   * recorded them.
   */
  readonly findings?: readonly FindingView[];
  /**
   * Cursor for the next page, present only when more findings remain.
   */
  readonly findings_next_cursor?: Cursor;
  /**
   * Evidence links for the findings in this page, as links rather than bytes.
   */
  readonly artefact_links?: readonly ArtefactLink[];
  /**
   * One bounded page of comments on the review itself, oldest first. Comments on a finding
   * are read through finding_get. The page may be shorter than comments_limit asked for:
   * members are added while they fit the response bound of section 13, and
   * comments_next_cursor then names where the next page starts.
   */
  readonly comments?: readonly CommentView[];
  /**
   * Cursor for the next page of comments, present only when more remain.
   */
  readonly comments_next_cursor?: Cursor;
  /**
   * The captured context, with no verdict. Present only when asked for.
   */
  readonly staleness?: ReviewStaleness;
}

/**
 * Payload of review_list: one bounded page of the current project's reviews.
 */
export interface ReviewListResult {
  /**
   * The page, newest first. An empty project answers with an empty array rather than a
   * refusal.
   */
  readonly reviews: readonly ReviewView[];
  /**
   * Cursor for the next page, present only when more reviews remain.
   */
  readonly next_cursor?: Cursor;
}

/**
 * Payload of review_search: reviews of the current project that matched. The project is
 * never a member of this shape, because it is never a choice the caller had.
 */
export interface ReviewSearchResult {
  /**
   * Matching reviews, most recently updated first.
   */
  readonly matches: readonly ReviewSearchMatch[];
}

/**
 * Payload of review_add_comment.
 */
export interface ReviewAddCommentResult {
  /**
   * The comment as recorded, attributed to the agent session.
   */
  readonly comment: CommentView;
}

/**
 * Payload of agent_inbox_list. Items are oldest first so that assignment order is
 * preserved, and the page is bounded like every other response (docs/MCP_SPEC.md section
 * 13).
 */
export interface AgentInboxListResult {
  /**
   * The page, oldest first.
   */
  readonly items: readonly InboxItemView[];
  /**
   * Items still pending for this session, which is not the number returned in this page.
   */
  readonly pending_count: RecordCount;
  /**
   * Cursor for the next page, present only when more items remain.
   */
  readonly next_cursor?: Cursor;
}

/**
 * Payload of agent_inbox_acknowledge. The item is returned as it now stands, still naming
 * the work it delivered: acknowledgement is receipt and never completion.
 */
export interface AgentInboxAcknowledgeResult {
  /**
   * The item after acknowledgement.
   */
  readonly item: InboxItemView;
  /**
   * Status before the change, absent when nothing moved.
   */
  readonly previous_status?: InboxItemStatus;
}

/**
 * Payload of review_claim and review_update_status: the review as it now stands.
 */
export interface ReviewMutationResult {
  /**
   * The review after the change.
   */
  readonly review: ReviewView;
  /**
   * Status before the change, absent when nothing moved.
   */
  readonly previous_status?: ReviewStatus;
}

/**
 * Payload of finding_get.
 */
export interface FindingGetResult {
  /**
   * The finding.
   */
  readonly finding: FindingView;
  /**
   * Marks on the original screenshot, with their text alternatives.
   */
  readonly annotations?: readonly AnnotationView[];
  /**
   * Comments on the finding, oldest first. The page may be shorter than comments_limit
   * asked for: members are added while they fit the response bound of section 13, and
   * comments_next_cursor then names where the next page starts.
   */
  readonly comments?: readonly CommentView[];
  /**
   * Cursor for the next page of comments, present only when more remain.
   */
  readonly comments_next_cursor?: Cursor;
  /**
   * Evidence links.
   */
  readonly artefact_links?: readonly ArtefactLink[];
  /**
   * Most recent verification submission, where one exists.
   */
  readonly latest_verification?: VerificationView;
}

/**
 * Payload of finding_claim and finding_update_status: the finding as it now stands.
 */
export interface FindingMutationResult {
  /**
   * The finding after the change.
   */
  readonly finding: FindingView;
  /**
   * Status before the change, absent when nothing moved.
   */
  readonly previous_status?: FindingStatus;
}

/**
 * Payload of finding_add_comment.
 */
export interface FindingAddCommentResult {
  /**
   * The comment as recorded.
   */
  readonly comment: CommentView;
}

/**
 * Payload of finding_submit_verification. The finding is returned beside the verification
 * so the agent can see the status the submission moved it to without a second call, and
 * the outstanding requirements beside both so it learns what is still missing here rather
 * than from a refusal on the next transition.
 */
export interface FindingSubmitVerificationResult {
  /**
   * The verification record, with status submitted.
   */
  readonly verification: VerificationView;
  /**
   * The finding as it now stands.
   */
  readonly finding: FindingView;
  /**
   * What the project requires of this work.
   */
  readonly requirements: CompletionRequirements;
  /**
   * What this submission still does not satisfy. Empty means the evidence gate would now
   * permit AWAITING_HUMAN_REVIEW, and nothing more than that.
   */
  readonly missing: readonly RequirementLabel[];
}

/**
 * Payload of browser_take_screenshot. It carries a link and metadata and never image
 * bytes, so the response is bounded whatever the page contains.
 */
export interface BrowserTakeScreenshotResult {
  /**
   * The verified capture.
   */
  readonly artefact: ArtefactLink;
  /**
   * Session the capture came from.
   */
  readonly browser_session_id: Identifier;
  /**
   * When the capture was taken.
   */
  readonly captured_at: Timestamp;
  /**
   * Viewport the capture was taken at.
   */
  readonly viewport: Viewport;
  /**
   * Whether the whole document was captured.
   */
  readonly full_page: boolean;
  /**
   * URL the browser was on. Page-derived and untrusted.
   */
  readonly url?: CapturedUrl;
}

/**
 * The error envelope of docs/MCP_SPEC.md section 5. It is deliberately not a frame: a
 * refusal carries no type-selected data, and inventing an empty one would make an agent's
 * success test depend on a field rather than on ok.
 */
export interface ToolRefusal {
  /**
   * MCP product protocol version (docs/MCP_SPEC.md section 14).
   */
  readonly protocol_version: number;
  /**
   * Always false here.
   */
  readonly ok: boolean;
  /**
   * Identifier of the refused call.
   */
  readonly request_id: RequestId;
  /**
   * Tool that refused.
   */
  readonly type: MessageType;
  /**
   * The refusal.
   */
  readonly error: ToolError;
  /**
   * Provenance of the refusal, which is the control plane.
   */
  readonly trust: TrustLabel;
  /**
   * The policy that applies to any quoted content.
   */
  readonly instruction_policy: InstructionPolicy;
  /**
   * Degradations that also applied.
   */
  readonly warnings?: readonly Warning[];
}

/**
 * The common response envelope of docs/MCP_SPEC.md section 5, extended with the protocol
 * version of section 14, the tool that answered and the instruction policy of
 * docs/SECURITY.md section 11. Additive fields are permitted within a major version and a
 * client ignores what it does not know (docs/API.md section 20). data is the payload
 * selected by type.
 */
export interface Envelope {
  /**
   * MCP product protocol version. Stage 0 pins 1; any other value is refused rather than
   * best-effort parsed.
   */
  readonly protocol_version: number;
  /**
   * Whether the call succeeded. True in this shape; a refusal uses tool_refusal.
   */
  readonly ok: boolean;
  /**
   * Identifier of the call, which joins a transcript line to its audit record.
   */
  readonly request_id: RequestId;
  /**
   * Tool that answered, which selects the schema of data.
   */
  readonly type: MessageType;
  /**
   * Provenance of the content in data. Page-derived content is never returned without an
   * untrusted label.
   */
  readonly trust: TrustLabel;
  /**
   * The policy that applies to every string in data.
   */
  readonly instruction_policy: InstructionPolicy;
  /**
   * Degradations that did not stop the call succeeding.
   */
  readonly warnings?: readonly Warning[];
}

/**
 * A published development service as an agent sees it. It carries the origin a browser
 * session opens and never a capability: a capability is minted for a browser session, and
 * an agent drives a session rather than presenting one itself (docs/ARCHITECTURE.md
 * section 7.3).
 */
export interface DevelopmentServiceView {
  /**
   * Published-service identity.
   */
  readonly id: Identifier;
  /**
   * Lifecycle status.
   */
  readonly status: PublishedServiceStatus;
  /**
   * Workspace the service was started from.
   */
  readonly workspace_id?: Identifier;
  /**
   * Local address the connector opens.
   */
  readonly local_host: LocalHost;
  /**
   * Local port the connector opens.
   */
  readonly local_port: LocalPort;
  /**
   * Protocol declared at publication.
   */
  readonly protocol: DestinationProtocol;
  /**
   * Origin an authorised browser session opens. It is usable only by a session the route
   * names, and only with a capability the control plane mints; it is not reachable from
   * the public internet.
   */
  readonly internal_origin: string;
  /**
   * Destination the connector reported it opened. Absent until the route is ready.
   */
  readonly observed_destination?: string;
  /**
   * Stable class that refused the publication. Present only when the status is failed.
   */
  readonly failure_class?: ErrorClass;
  /**
   * When the route expires. Publication always expires.
   */
  readonly expires_at: Timestamp;
}

/**
 * Arguments of development_services_list. It names no project: the project is the
 * session's, and a project argument would be an authorisation input chosen by the caller.
 */
export interface DevelopmentServicesListInput {
  /**
   * Maximum routes in this page, newest first. Bounded here rather than by the caller's
   * optimism, because an unbounded response is the failure mode docs/MCP_SPEC.md section
   * 13 exists to prevent.
   */
  readonly limit?: PageLimit;
}

/**
 * Arguments of development_service_publish (docs/MCP_SPEC.md section 7.2).
 */
export interface DevelopmentServicePublishInput {
  /**
   * Workspace the development service was started from. It MUST belong to the session's
   * project.
   */
  readonly workspace_id: Identifier;
  /**
   * Local address to publish. Defaults to 127.0.0.1, and the connector's own policy still
   * applies (docs/CONNECTOR_PROTOCOL.md section 11).
   */
  readonly local_host?: LocalHost;
  /**
   * Local port to publish.
   */
  readonly local_port: LocalPort;
  /**
   * Protocol the development service speaks. Defaults to http.
   */
  readonly protocol?: DestinationProtocol;
  /**
   * How long the route may live. It is bounded by the deployment's maximum route lifetime,
   * and a longer request is refused rather than clipped: silently shortening a lifetime
   * would make an agent believe it had access it does not have.
   */
  readonly ttl_seconds?: number;
  /**
   * Key for this publication. Section 10 requires one on every state-changing tool, and it
   * matters particularly here: a retry without one would open a second tunnel into the
   * development machine for a request the agent believes it made once.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Arguments of development_service_unpublish.
 */
export interface DevelopmentServiceUnpublishInput {
  /**
   * Route to revoke. A route outside the session's project is reported absent rather than
   * forbidden (docs/API.md section 5).
   */
  readonly published_service_id: Identifier;
  /**
   * Key for this revocation. Section 10 requires one on every state-changing tool;
   * revocation is idempotent in the domain as well, so a retry produces one event either
   * way.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Payload of development_services_list.
 */
export interface DevelopmentServicesListResult {
  /**
   * Routes of the session's project, newest first.
   */
  readonly services: readonly DevelopmentServiceView[];
}

/**
 * Payload of development_service_publish and development_service_unpublish: the record as
 * it stands after the command.
 */
export interface DevelopmentServiceResult {
  /**
   * The published development service.
   */
  readonly service: DevelopmentServiceView;
}

/**
 * What this project requires before work counts as done (docs/MCP_SPEC.md section 7.8).
 * The viewports come from the project's default_validation_viewports (docs/DOMAIN_MODEL.md
 * section 6), so a project that changes them changes the gate rather than the gate holding
 * a second copy of the rule. The shape is shared by the web application and this
 * interface, so both describe the requirement in the same words.
 */
export interface CompletionRequirements {
  /**
   * Viewports the work must be checked at, written as widthxheight. Stage 1 defaults to
   * 390x844 and 1440x900 (AGENTS.md "Browser-facing work").
   */
  readonly required_viewports: readonly RequirementLabel[];
  /**
   * Whether console output must have been reviewed. It is an agent assertion, never a
   * control-plane observation: Stage 1 captures no console artefact.
   */
  readonly console_review: boolean;
  /**
   * Whether failed network requests must have been reviewed. An agent assertion, on the
   * same footing as console_review.
   */
  readonly network_review: boolean;
  /**
   * Whether a verified after screenshot must be linked. This one the control plane checks
   * itself: the artefact must exist, be available, belong to this project and carry a
   * digest.
   */
  readonly final_screenshot: boolean;
  /**
   * Whether keyboard navigation and visible focus must have been checked. Recorded but not
   * required in Stage 1, so this is false and accessibility_checked never appears in a
   * missing list.
   */
  readonly accessibility_check?: boolean;
}

/**
 * Who established each piece of evidence. The completion gate MUST NOT present an agent's
 * checks object as though the control plane had confirmed it: the artefacts are what the
 * control plane verified, and the checks are a claim attributed to the actor that made it
 * (docs/DOMAIN_MODEL.md section 19, docs/MCP_SPEC.md section 7.8).
 */
export interface EvidenceAssurance {
  /**
   * What the control plane checked for itself before recording the verification.
   */
  readonly verified_by_control_plane: readonly RequirementLabel[];
  /**
   * What the submitter claimed and nothing confirmed. Empty is a truthful answer.
   */
  readonly asserted_by_agent: readonly RequirementLabel[];
  /**
   * The actor whose claim the assertions are. Absent when there is no verification to
   * attribute them to.
   */
  readonly asserted_by?: ActorReference;
}

/**
 * One finding's standing against the project's completion requirements.
 */
export interface FindingCompletionState {
  /**
   * The finding.
   */
  readonly finding_id: Identifier;
  /**
   * Status the finding holds now.
   */
  readonly status: FindingStatus;
  /**
   * The gate's answer for this finding alone.
   */
  readonly result: CompletionResult;
  /**
   * Evidence this finding still lacks. Empty when nothing is missing.
   */
  readonly missing: readonly RequirementLabel[];
  /**
   * The current verification, where one has been submitted. Absent means none has, which
   * is a different statement from one that is incomplete.
   */
  readonly verification_id?: Identifier;
  /**
   * How many verifications this finding has accumulated, including superseded ones. Reopen
   * cycles preserve the history rather than replacing it.
   */
  readonly verification_count?: RecordCount;
}

/**
 * Arguments of task_validation_status. The review is resolved inside the current project
 * only, as everywhere else on this interface.
 */
export interface TaskValidationStatusInput {
  /**
   * Immutable identifier or project-scoped slug of the review whose work is being
   * evaluated.
   */
  readonly review: ReviewSelector;
  /**
   * One finding to narrow to. Absent evaluates every finding of the review.
   */
  readonly finding_id?: Identifier;
}

/**
 * Payload of task_validation_status, in the shape of docs/MCP_SPEC.md section 7.8. It
 * reports and changes nothing.
 */
export interface TaskValidationStatusResult {
  /**
   * Whether the work must be checked in a browser. True while any required viewport is
   * configured.
   */
  readonly browser_required: boolean;
  /**
   * What the project requires.
   */
  readonly requirements: CompletionRequirements;
  /**
   * What is still missing, deduplicated across the evaluated findings. An empty list is
   * the honest answer that nothing is outstanding, and is not on its own a statement that
   * the work is accepted.
   */
  readonly missing: readonly RequirementLabel[];
  /**
   * Who established what. Present even when nothing has been submitted, so a reader never
   * has to infer that an absent member means confirmed.
   */
  readonly assurance: EvidenceAssurance;
  /**
   * Per-finding standing against the requirements, in the review's order. It is
   * deliberately not called findings and carries no finding view: nothing here came from a
   * page, which is why this response is labelled trusted_control_plane and why an agent
   * must still read the finding itself for its text.
   */
  readonly finding_states?: readonly FindingCompletionState[];
}

/**
 * Arguments of task_complete. There is no argument that asserts a result and none that
 * could terminate the session: the caller names the work and the control plane evaluates
 * it.
 */
export interface TaskCompleteInput {
  /**
   * Immutable identifier or project-scoped slug of the review being reported on.
   */
  readonly review: ReviewSelector;
  /**
   * One finding to narrow to. Absent evaluates every finding of the review.
   */
  readonly finding_id?: Identifier;
  /**
   * What the agent believes it finished. Recorded with the evaluation as a claim by the
   * agent; it is not evidence and does not affect the result.
   */
  readonly summary?: BodyText;
  /**
   * Key for this evaluation. Section 10 requires one on every tool that records, and this
   * one records the evaluation.
   */
  readonly idempotency_key: IdempotencyKey;
}

/**
 * Payload of task_complete. terminates_session is always false and is stated rather than
 * implied, because the tool's name is the one thing about it that could be misread
 * (docs/MCP_SPEC.md section 7.8).
 */
export interface TaskCompleteResult {
  /**
   * The gate's answer.
   */
  readonly result: CompletionResult;
  /**
   * Always false. This tool records validation state and returns requirements; ending the
   * run is the client's decision.
   */
  readonly terminates_session: boolean;
  /**
   * What the project requires.
   */
  readonly requirements: CompletionRequirements;
  /**
   * What is still missing, deduplicated across the evaluated findings.
   */
  readonly missing: readonly RequirementLabel[];
  /**
   * Who established what.
   */
  readonly assurance: EvidenceAssurance;
  /**
   * What to do next, in the vocabulary of this interface. A refusal that says only what is
   * wrong makes an agent guess.
   */
  readonly next_actions: readonly RequirementLabel[];
  /**
   * Per-finding standing against the requirements, in the review's order. It is
   * deliberately not called findings and carries no finding view: nothing here came from a
   * page, which is why this response is labelled trusted_control_plane and why an agent
   * must still read the finding itself for its text.
   */
  readonly finding_states?: readonly FindingCompletionState[];
}

/**
 * A decoded control frame: envelope header plus the payload selected by its type.
 */
export type McpFrame =
  | {
      readonly envelope: Envelope;
      readonly type: "project_current";
      readonly payload: ProjectCurrentResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "agent_session_status";
      readonly payload: AgentSessionStatusResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "agent_inbox_list";
      readonly payload: AgentInboxListResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "agent_inbox_acknowledge";
      readonly payload: AgentInboxAcknowledgeResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "review_list";
      readonly payload: ReviewListResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "review_search";
      readonly payload: ReviewSearchResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "review_get";
      readonly payload: ReviewGetResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "review_claim";
      readonly payload: ReviewMutationResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "review_update_status";
      readonly payload: ReviewMutationResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "review_add_comment";
      readonly payload: ReviewAddCommentResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "finding_get";
      readonly payload: FindingGetResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "finding_claim";
      readonly payload: FindingMutationResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "finding_update_status";
      readonly payload: FindingMutationResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "finding_mark_blocked";
      readonly payload: FindingMutationResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "finding_add_comment";
      readonly payload: FindingAddCommentResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "finding_submit_verification";
      readonly payload: FindingSubmitVerificationResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "browser_take_screenshot";
      readonly payload: BrowserTakeScreenshotResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "development_services_list";
      readonly payload: DevelopmentServicesListResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "development_service_publish";
      readonly payload: DevelopmentServiceResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "development_service_unpublish";
      readonly payload: DevelopmentServiceResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "task_validation_status";
      readonly payload: TaskValidationStatusResult;
    }
  | {
      readonly envelope: Envelope;
      readonly type: "task_complete";
      readonly payload: TaskCompleteResult;
    }
;
