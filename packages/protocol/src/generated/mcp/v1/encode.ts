// Code generated by packages/protocol/tools/generate.ts from
// schemas/mcp/v1.schema.json. DO NOT EDIT.

/**
 * Canonical encoders. Output is deterministic: properties appear in schema order, absent
 * optional properties are omitted, and numbers and strings are formatted identically to
 * the Go encoder so that both languages produce byte-identical frames.
 */

import {
  jsonBoolean,
  jsonInteger,
  jsonNumber,
  jsonString,
} from "../../../canonical.ts";
import type {
  ActorReference,
  AgentSessionStatusInput,
  AgentSessionStatusResult,
  AnnotationGeometry,
  AnnotationView,
  ArtefactLink,
  BrowserSessionView,
  BrowserTakeScreenshotInput,
  BrowserTakeScreenshotResult,
  ClientCapabilities,
  ClientIdentity,
  CommentView,
  ContentRectangle,
  Envelope,
  ErrorDetails,
  FindingAddCommentInput,
  FindingAddCommentResult,
  FindingClaimInput,
  FindingGetInput,
  FindingGetResult,
  FindingMutationResult,
  FindingSubmitVerificationInput,
  FindingSubmitVerificationResult,
  FindingUpdateStatusInput,
  FindingView,
  PolicySummary,
  ProjectCurrentInput,
  ProjectCurrentResult,
  ProjectReference,
  ReviewClaimInput,
  ReviewGetInput,
  ReviewGetResult,
  ReviewMutationResult,
  ReviewUpdateStatusInput,
  ReviewView,
  ScrollPosition,
  ServerCapabilities,
  SessionInitialisationRequest,
  SessionInitialisationResult,
  ToolError,
  ToolRefusal,
  VerificationChecks,
  VerificationView,
  Viewport,
  Warning,
  WorkspaceReference,
} from "./types.ts";

/**
 * Canonically encodes a ActorReference.
 */
export function encodeActorReference(value: ActorReference): string {
  const fields: string[] = [];
  fields.push(`"type":${jsonString(value.type)}`);
  if (value.id !== undefined) {
    fields.push(`"id":${jsonString(value.id)}`);
  }
  if (value.display !== undefined) {
    fields.push(`"display":${jsonString(value.display)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Viewport.
 */
export function encodeViewport(value: Viewport): string {
  const fields: string[] = [];
  fields.push(`"width":${jsonInteger(value.width)}`);
  fields.push(`"height":${jsonInteger(value.height)}`);
  fields.push(`"device_scale_factor":${jsonNumber(value.device_scale_factor)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ScrollPosition.
 */
export function encodeScrollPosition(value: ScrollPosition): string {
  const fields: string[] = [];
  fields.push(`"x":${jsonInteger(value.x)}`);
  fields.push(`"y":${jsonInteger(value.y)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ContentRectangle.
 */
export function encodeContentRectangle(value: ContentRectangle): string {
  const fields: string[] = [];
  fields.push(`"width_px":${jsonInteger(value.width_px)}`);
  fields.push(`"height_px":${jsonInteger(value.height_px)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a AnnotationGeometry.
 */
export function encodeAnnotationGeometry(value: AnnotationGeometry): string {
  const fields: string[] = [];
  fields.push(`"x":${jsonNumber(value.x)}`);
  fields.push(`"y":${jsonNumber(value.y)}`);
  if (value.width !== undefined) {
    fields.push(`"width":${jsonNumber(value.width)}`);
  }
  if (value.height !== undefined) {
    fields.push(`"height":${jsonNumber(value.height)}`);
  }
  if (value.x2 !== undefined) {
    fields.push(`"x2":${jsonNumber(value.x2)}`);
  }
  if (value.y2 !== undefined) {
    fields.push(`"y2":${jsonNumber(value.y2)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectReference.
 */
export function encodeProjectReference(value: ProjectReference): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"slug":${jsonString(value.slug)}`);
  if (value.name !== undefined) {
    fields.push(`"name":${jsonString(value.name)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a WorkspaceReference.
 */
export function encodeWorkspaceReference(value: WorkspaceReference): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  if (value.root_path !== undefined) {
    fields.push(`"root_path":${jsonString(value.root_path)}`);
  }
  fields.push(`"branch":${jsonString(value.branch)}`);
  fields.push(`"head_commit":${jsonString(value.head_commit)}`);
  if (value.dirty !== undefined) {
    fields.push(`"dirty":${jsonBoolean(value.dirty)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a PolicySummary.
 */
export function encodePolicySummary(value: PolicySummary): string {
  const fields: string[] = [];
  fields.push(`"agent_may_accept_findings":${jsonBoolean(value.agent_may_accept_findings)}`);
  fields.push(`"verification_required":${jsonBoolean(value.verification_required)}`);
  fields.push(`"secret_tools_available":${jsonBoolean(value.secret_tools_available)}`);
  fields.push(`"required_viewports":${`[${value.required_viewports.map((item) => jsonString(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ServerCapabilities.
 */
export function encodeServerCapabilities(value: ServerCapabilities): string {
  const fields: string[] = [];
  fields.push(`"browser_live":${jsonBoolean(value.browser_live)}`);
  fields.push(`"image_resources":${jsonBoolean(value.image_resources)}`);
  fields.push(`"human_takeover":${jsonBoolean(value.human_takeover)}`);
  fields.push(`"review_inbox":${jsonBoolean(value.review_inbox)}`);
  fields.push(`"managed_messages":${jsonBoolean(value.managed_messages)}`);
  fields.push(`"session_resume":${jsonBoolean(value.session_resume)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ClientCapabilities.
 */
export function encodeClientCapabilities(value: ClientCapabilities): string {
  const fields: string[] = [];
  if (value.resources !== undefined) {
    fields.push(`"resources":${jsonBoolean(value.resources)}`);
  }
  if (value.image_content !== undefined) {
    fields.push(`"image_content":${jsonBoolean(value.image_content)}`);
  }
  if (value.managed_messages !== undefined) {
    fields.push(`"managed_messages":${jsonBoolean(value.managed_messages)}`);
  }
  if (value.session_resume !== undefined) {
    fields.push(`"session_resume":${jsonBoolean(value.session_resume)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Warning.
 */
export function encodeWarning(value: Warning): string {
  const fields: string[] = [];
  fields.push(`"code":${jsonString(value.code)}`);
  fields.push(`"message":${jsonString(value.message)}`);
  if (value.detail !== undefined) {
    fields.push(`"detail":${jsonString(value.detail)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ErrorDetails.
 */
export function encodeErrorDetails(value: ErrorDetails): string {
  const fields: string[] = [];
  if (value.current_version !== undefined) {
    fields.push(`"current_version":${jsonInteger(value.current_version)}`);
  }
  if (value.expected_version !== undefined) {
    fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  }
  if (value.current_epoch !== undefined) {
    fields.push(`"current_epoch":${jsonInteger(value.current_epoch)}`);
  }
  if (value.field !== undefined) {
    fields.push(`"field":${jsonString(value.field)}`);
  }
  if (value.candidates !== undefined) {
    fields.push(`"candidates":${`[${value.candidates.map((item) => encodeProjectReference(item)).join(",")}]`}`);
  }
  if (value.required_evidence !== undefined) {
    fields.push(`"required_evidence":${`[${value.required_evidence.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.allowed_transitions !== undefined) {
    fields.push(`"allowed_transitions":${`[${value.allowed_transitions.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.retry_after_ms !== undefined) {
    fields.push(`"retry_after_ms":${jsonInteger(value.retry_after_ms)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ToolError.
 */
export function encodeToolError(value: ToolError): string {
  const fields: string[] = [];
  fields.push(`"code":${jsonString(value.code)}`);
  fields.push(`"message":${jsonString(value.message)}`);
  fields.push(`"retryable":${jsonBoolean(value.retryable)}`);
  if (value.details !== undefined) {
    fields.push(`"details":${encodeErrorDetails(value.details)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ArtefactLink.
 */
export function encodeArtefactLink(value: ArtefactLink): string {
  const fields: string[] = [];
  fields.push(`"artefact_id":${jsonString(value.artefact_id)}`);
  fields.push(`"kind":${jsonString(value.kind)}`);
  if (value.role !== undefined) {
    fields.push(`"role":${jsonString(value.role)}`);
  }
  fields.push(`"resource_uri":${jsonString(value.resource_uri)}`);
  if (value.content_path !== undefined) {
    fields.push(`"content_path":${jsonString(value.content_path)}`);
  }
  if (value.expires_at !== undefined) {
    fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  }
  fields.push(`"content_type":${jsonString(value.content_type)}`);
  if (value.sha256 !== undefined) {
    fields.push(`"sha256":${jsonString(value.sha256)}`);
  }
  if (value.size_bytes !== undefined) {
    fields.push(`"size_bytes":${jsonInteger(value.size_bytes)}`);
  }
  if (value.content_rectangle !== undefined) {
    fields.push(`"content_rectangle":${encodeContentRectangle(value.content_rectangle)}`);
  }
  fields.push(`"trust":${jsonString(value.trust)}`);
  fields.push(`"instruction_policy":${jsonString(value.instruction_policy)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a AnnotationView.
 */
export function encodeAnnotationView(value: AnnotationView): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"artefact_id":${jsonString(value.artefact_id)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"geometry":${encodeAnnotationGeometry(value.geometry)}`);
  fields.push(`"label":${jsonString(value.label)}`);
  if (value.marker_number !== undefined) {
    fields.push(`"marker_number":${jsonInteger(value.marker_number)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a CommentView.
 */
export function encodeCommentView(value: CommentView): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  fields.push(`"body":${jsonString(value.body)}`);
  fields.push(`"author":${encodeActorReference(value.author)}`);
  fields.push(`"created_at":${jsonString(value.created_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a VerificationChecks.
 */
export function encodeVerificationChecks(value: VerificationChecks): string {
  const fields: string[] = [];
  fields.push(`"reproduced_before":${jsonBoolean(value.reproduced_before)}`);
  fields.push(`"console_errors_reviewed":${jsonBoolean(value.console_errors_reviewed)}`);
  fields.push(`"network_failures_reviewed":${jsonBoolean(value.network_failures_reviewed)}`);
  if (value.accessibility_checked !== undefined) {
    fields.push(`"accessibility_checked":${jsonBoolean(value.accessibility_checked)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a VerificationView.
 */
export function encodeVerificationView(value: VerificationView): string {
  const fields: string[] = [];
  fields.push(`"verification_id":${jsonString(value.verification_id)}`);
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"summary":${jsonString(value.summary)}`);
  fields.push(`"branch":${jsonString(value.branch)}`);
  fields.push(`"commit":${jsonString(value.commit)}`);
  fields.push(`"tested_viewports":${`[${value.tested_viewports.map((item) => encodeViewport(item)).join(",")}]`}`);
  fields.push(`"checks":${encodeVerificationChecks(value.checks)}`);
  if (value.artefacts !== undefined) {
    fields.push(`"artefacts":${`[${value.artefacts.map((item) => encodeArtefactLink(item)).join(",")}]`}`);
  }
  fields.push(`"submitted_by":${encodeActorReference(value.submitted_by)}`);
  fields.push(`"submitted_at":${jsonString(value.submitted_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ReviewView.
 */
export function encodeReviewView(value: ReviewView): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"project_id":${jsonString(value.project_id)}`);
  fields.push(`"slug":${jsonString(value.slug)}`);
  fields.push(`"title":${jsonString(value.title)}`);
  if (value.description !== undefined) {
    fields.push(`"description":${jsonString(value.description)}`);
  }
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"version":${jsonInteger(value.version)}`);
  fields.push(`"captured_branch":${jsonString(value.captured_branch)}`);
  fields.push(`"captured_commit":${jsonString(value.captured_commit)}`);
  fields.push(`"captured_workspace_id":${jsonString(value.captured_workspace_id)}`);
  if (value.source_browser_session_id !== undefined) {
    fields.push(`"source_browser_session_id":${jsonString(value.source_browser_session_id)}`);
  }
  if (value.claimed_by !== undefined) {
    fields.push(`"claimed_by":${encodeActorReference(value.claimed_by)}`);
  }
  fields.push(`"finding_count":${jsonInteger(value.finding_count)}`);
  fields.push(`"resource_uri":${jsonString(value.resource_uri)}`);
  fields.push(`"created_at":${jsonString(value.created_at)}`);
  fields.push(`"updated_at":${jsonString(value.updated_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingView.
 */
export function encodeFindingView(value: FindingView): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"review_id":${jsonString(value.review_id)}`);
  fields.push(`"title":${jsonString(value.title)}`);
  if (value.description !== undefined) {
    fields.push(`"description":${jsonString(value.description)}`);
  }
  fields.push(`"severity":${jsonString(value.severity)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"source":${jsonString(value.source)}`);
  fields.push(`"version":${jsonInteger(value.version)}`);
  fields.push(`"url":${jsonString(value.url)}`);
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"scroll_position":${encodeScrollPosition(value.scroll_position)}`);
  fields.push(`"captured_commit":${jsonString(value.captured_commit)}`);
  fields.push(`"screenshot_artefact_id":${jsonString(value.screenshot_artefact_id)}`);
  if (value.acceptance_criteria !== undefined) {
    fields.push(`"acceptance_criteria":${jsonString(value.acceptance_criteria)}`);
  }
  if (value.claimed_by !== undefined) {
    fields.push(`"claimed_by":${encodeActorReference(value.claimed_by)}`);
  }
  if (value.annotation_count !== undefined) {
    fields.push(`"annotation_count":${jsonInteger(value.annotation_count)}`);
  }
  fields.push(`"resource_uri":${jsonString(value.resource_uri)}`);
  fields.push(`"untrusted_fields":${`[${value.untrusted_fields.map((item) => jsonString(item)).join(",")}]`}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a BrowserSessionView.
 */
export function encodeBrowserSessionView(value: BrowserSessionView): string {
  const fields: string[] = [];
  fields.push(`"id":${jsonString(value.id)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  if (value.controller !== undefined) {
    fields.push(`"controller":${encodeActorReference(value.controller)}`);
  }
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  if (value.service_origin !== undefined) {
    fields.push(`"service_origin":${jsonString(value.service_origin)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionInitialisationRequest.
 */
export function encodeSessionInitialisationRequest(value: SessionInitialisationRequest): string {
  const fields: string[] = [];
  fields.push(`"client":${encodeClientIdentity(value.client)}`);
  if (value.project_hint !== undefined) {
    fields.push(`"project_hint":${jsonString(value.project_hint)}`);
  }
  if (value.workspace_hint !== undefined) {
    fields.push(`"workspace_hint":${jsonString(value.workspace_hint)}`);
  }
  if (value.capabilities !== undefined) {
    fields.push(`"capabilities":${encodeClientCapabilities(value.capabilities)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ClientIdentity.
 */
export function encodeClientIdentity(value: ClientIdentity): string {
  const fields: string[] = [];
  fields.push(`"name":${jsonString(value.name)}`);
  fields.push(`"version":${jsonString(value.version)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionInitialisationResult.
 */
export function encodeSessionInitialisationResult(value: SessionInitialisationResult): string {
  const fields: string[] = [];
  fields.push(`"agent_session_id":${jsonString(value.agent_session_id)}`);
  fields.push(`"project":${encodeProjectReference(value.project)}`);
  if (value.workspace !== undefined) {
    fields.push(`"workspace":${encodeWorkspaceReference(value.workspace)}`);
  }
  fields.push(`"capabilities":${encodeServerCapabilities(value.capabilities)}`);
  fields.push(`"granted_capabilities":${`[${value.granted_capabilities.map((item) => jsonString(item)).join(",")}]`}`);
  if (value.expires_at !== undefined) {
    fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectCurrentInput.
 */
export function encodeProjectCurrentInput(value: ProjectCurrentInput): string {
  const fields: string[] = [];
  if (value.refresh_source_state !== undefined) {
    fields.push(`"refresh_source_state":${jsonBoolean(value.refresh_source_state)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a AgentSessionStatusInput.
 */
export function encodeAgentSessionStatusInput(value: AgentSessionStatusInput): string {
  const fields: string[] = [];
  if (value.include !== undefined) {
    fields.push(`"include":${`[${value.include.map((item) => jsonString(item)).join(",")}]`}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ReviewGetInput.
 */
export function encodeReviewGetInput(value: ReviewGetInput): string {
  const fields: string[] = [];
  fields.push(`"review":${jsonString(value.review)}`);
  if (value.include !== undefined) {
    fields.push(`"include":${`[${value.include.map((item) => jsonString(item)).join(",")}]`}`);
  }
  if (value.findings_limit !== undefined) {
    fields.push(`"findings_limit":${jsonInteger(value.findings_limit)}`);
  }
  if (value.findings_cursor !== undefined) {
    fields.push(`"findings_cursor":${jsonString(value.findings_cursor)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ReviewClaimInput.
 */
export function encodeReviewClaimInput(value: ReviewClaimInput): string {
  const fields: string[] = [];
  fields.push(`"review_id":${jsonString(value.review_id)}`);
  fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ReviewUpdateStatusInput.
 */
export function encodeReviewUpdateStatusInput(value: ReviewUpdateStatusInput): string {
  const fields: string[] = [];
  fields.push(`"review_id":${jsonString(value.review_id)}`);
  fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.reason !== undefined) {
    fields.push(`"reason":${jsonString(value.reason)}`);
  }
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingGetInput.
 */
export function encodeFindingGetInput(value: FindingGetInput): string {
  const fields: string[] = [];
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  if (value.include !== undefined) {
    fields.push(`"include":${`[${value.include.map((item) => jsonString(item)).join(",")}]`}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingClaimInput.
 */
export function encodeFindingClaimInput(value: FindingClaimInput): string {
  const fields: string[] = [];
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingUpdateStatusInput.
 */
export function encodeFindingUpdateStatusInput(value: FindingUpdateStatusInput): string {
  const fields: string[] = [];
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.resolution_note !== undefined) {
    fields.push(`"resolution_note":${jsonString(value.resolution_note)}`);
  }
  if (value.reason !== undefined) {
    fields.push(`"reason":${jsonString(value.reason)}`);
  }
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingAddCommentInput.
 */
export function encodeFindingAddCommentInput(value: FindingAddCommentInput): string {
  const fields: string[] = [];
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  fields.push(`"body":${jsonString(value.body)}`);
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingSubmitVerificationInput.
 */
export function encodeFindingSubmitVerificationInput(value: FindingSubmitVerificationInput): string {
  const fields: string[] = [];
  fields.push(`"finding_id":${jsonString(value.finding_id)}`);
  if (value.expected_version !== undefined) {
    fields.push(`"expected_version":${jsonInteger(value.expected_version)}`);
  }
  fields.push(`"summary":${jsonString(value.summary)}`);
  fields.push(`"branch":${jsonString(value.branch)}`);
  fields.push(`"commit":${jsonString(value.commit)}`);
  fields.push(`"tested_viewports":${`[${value.tested_viewports.map((item) => encodeViewport(item)).join(",")}]`}`);
  fields.push(`"checks":${encodeVerificationChecks(value.checks)}`);
  fields.push(`"artefact_ids":${`[${value.artefact_ids.map((item) => jsonString(item)).join(",")}]`}`);
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a BrowserTakeScreenshotInput.
 */
export function encodeBrowserTakeScreenshotInput(value: BrowserTakeScreenshotInput): string {
  const fields: string[] = [];
  fields.push(`"browser_session_id":${jsonString(value.browser_session_id)}`);
  fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  if (value.full_page !== undefined) {
    fields.push(`"full_page":${jsonBoolean(value.full_page)}`);
  }
  fields.push(`"purpose":${jsonString(value.purpose)}`);
  fields.push(`"idempotency_key":${jsonString(value.idempotency_key)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ProjectCurrentResult.
 */
export function encodeProjectCurrentResult(value: ProjectCurrentResult): string {
  const fields: string[] = [];
  fields.push(`"project":${encodeProjectReference(value.project)}`);
  if (value.workspace !== undefined) {
    fields.push(`"workspace":${encodeWorkspaceReference(value.workspace)}`);
  }
  fields.push(`"policy":${encodePolicySummary(value.policy)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a AgentSessionStatusResult.
 */
export function encodeAgentSessionStatusResult(value: AgentSessionStatusResult): string {
  const fields: string[] = [];
  fields.push(`"agent_session_id":${jsonString(value.agent_session_id)}`);
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.client !== undefined) {
    fields.push(`"client":${encodeClientIdentity(value.client)}`);
  }
  fields.push(`"project":${encodeProjectReference(value.project)}`);
  if (value.workspace !== undefined) {
    fields.push(`"workspace":${encodeWorkspaceReference(value.workspace)}`);
  }
  fields.push(`"capabilities":${encodeServerCapabilities(value.capabilities)}`);
  fields.push(`"granted_capabilities":${`[${value.granted_capabilities.map((item) => jsonString(item)).join(",")}]`}`);
  if (value.browser_sessions !== undefined) {
    fields.push(`"browser_sessions":${`[${value.browser_sessions.map((item) => encodeBrowserSessionView(item)).join(",")}]`}`);
  }
  if (value.expires_at !== undefined) {
    fields.push(`"expires_at":${jsonString(value.expires_at)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ReviewGetResult.
 */
export function encodeReviewGetResult(value: ReviewGetResult): string {
  const fields: string[] = [];
  fields.push(`"review":${encodeReviewView(value.review)}`);
  if (value.findings !== undefined) {
    fields.push(`"findings":${`[${value.findings.map((item) => encodeFindingView(item)).join(",")}]`}`);
  }
  if (value.findings_next_cursor !== undefined) {
    fields.push(`"findings_next_cursor":${jsonString(value.findings_next_cursor)}`);
  }
  if (value.artefact_links !== undefined) {
    fields.push(`"artefact_links":${`[${value.artefact_links.map((item) => encodeArtefactLink(item)).join(",")}]`}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ReviewMutationResult.
 */
export function encodeReviewMutationResult(value: ReviewMutationResult): string {
  const fields: string[] = [];
  fields.push(`"review":${encodeReviewView(value.review)}`);
  if (value.previous_status !== undefined) {
    fields.push(`"previous_status":${jsonString(value.previous_status)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingGetResult.
 */
export function encodeFindingGetResult(value: FindingGetResult): string {
  const fields: string[] = [];
  fields.push(`"finding":${encodeFindingView(value.finding)}`);
  if (value.annotations !== undefined) {
    fields.push(`"annotations":${`[${value.annotations.map((item) => encodeAnnotationView(item)).join(",")}]`}`);
  }
  if (value.comments !== undefined) {
    fields.push(`"comments":${`[${value.comments.map((item) => encodeCommentView(item)).join(",")}]`}`);
  }
  if (value.artefact_links !== undefined) {
    fields.push(`"artefact_links":${`[${value.artefact_links.map((item) => encodeArtefactLink(item)).join(",")}]`}`);
  }
  if (value.latest_verification !== undefined) {
    fields.push(`"latest_verification":${encodeVerificationView(value.latest_verification)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingMutationResult.
 */
export function encodeFindingMutationResult(value: FindingMutationResult): string {
  const fields: string[] = [];
  fields.push(`"finding":${encodeFindingView(value.finding)}`);
  if (value.previous_status !== undefined) {
    fields.push(`"previous_status":${jsonString(value.previous_status)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingAddCommentResult.
 */
export function encodeFindingAddCommentResult(value: FindingAddCommentResult): string {
  const fields: string[] = [];
  fields.push(`"comment":${encodeCommentView(value.comment)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FindingSubmitVerificationResult.
 */
export function encodeFindingSubmitVerificationResult(value: FindingSubmitVerificationResult): string {
  const fields: string[] = [];
  fields.push(`"verification":${encodeVerificationView(value.verification)}`);
  fields.push(`"finding":${encodeFindingView(value.finding)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a BrowserTakeScreenshotResult.
 */
export function encodeBrowserTakeScreenshotResult(value: BrowserTakeScreenshotResult): string {
  const fields: string[] = [];
  fields.push(`"artefact":${encodeArtefactLink(value.artefact)}`);
  fields.push(`"browser_session_id":${jsonString(value.browser_session_id)}`);
  fields.push(`"captured_at":${jsonString(value.captured_at)}`);
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"full_page":${jsonBoolean(value.full_page)}`);
  if (value.url !== undefined) {
    fields.push(`"url":${jsonString(value.url)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ToolRefusal.
 */
export function encodeToolRefusal(value: ToolRefusal): string {
  const fields: string[] = [];
  fields.push(`"protocol_version":${jsonInteger(value.protocol_version)}`);
  fields.push(`"ok":${jsonBoolean(value.ok)}`);
  fields.push(`"request_id":${jsonString(value.request_id)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"error":${encodeToolError(value.error)}`);
  fields.push(`"trust":${jsonString(value.trust)}`);
  fields.push(`"instruction_policy":${jsonString(value.instruction_policy)}`);
  if (value.warnings !== undefined) {
    fields.push(`"warnings":${`[${value.warnings.map((item) => encodeWarning(item)).join(",")}]`}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Envelope.
 */
export function encodeEnvelope(value: Envelope, dataJson: string): string {
  const fields: string[] = [];
  fields.push(`"protocol_version":${jsonInteger(value.protocol_version)}`);
  fields.push(`"ok":${jsonBoolean(value.ok)}`);
  fields.push(`"request_id":${jsonString(value.request_id)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"trust":${jsonString(value.trust)}`);
  fields.push(`"instruction_policy":${jsonString(value.instruction_policy)}`);
  if (value.warnings !== undefined) {
    fields.push(`"warnings":${`[${value.warnings.map((item) => encodeWarning(item)).join(",")}]`}`);
  }
  fields.push(`"data":${dataJson}`);
  return `{${fields.join(",")}}`;
}
