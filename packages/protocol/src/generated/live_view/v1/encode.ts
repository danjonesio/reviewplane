// Code generated by packages/protocol/tools/generate.ts from
// schemas/live_view/v1.schema.json. DO NOT EDIT.

/**
 * Canonical encoders. Output is deterministic: properties appear in schema order, absent
 * optional properties are omitted, and numbers and strings are formatted identically to
 * the Go encoder so that both languages produce byte-identical frames.
 */

import {
  jsonBoolean,
  jsonInteger,
  jsonNumber,
  jsonString,
} from "../../../canonical.ts";
import type {
  Envelope,
  FrameMetadata,
  LiveError,
  QualityRequest,
  QualityState,
  SessionState,
  StreamAttached,
  StreamHeartbeat,
  ViewerHeartbeat,
  Viewport,
} from "./types.ts";

/**
 * Canonically encodes a Viewport.
 */
export function encodeViewport(value: Viewport): string {
  const fields: string[] = [];
  fields.push(`"width":${jsonInteger(value.width)}`);
  fields.push(`"height":${jsonInteger(value.height)}`);
  fields.push(`"device_scale_factor":${jsonNumber(value.device_scale_factor)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a Envelope.
 */
export function encodeEnvelope(value: Envelope, payloadJson: string): string {
  const fields: string[] = [];
  fields.push(`"protocol_version":${jsonInteger(value.protocol_version)}`);
  fields.push(`"message_id":${jsonString(value.message_id)}`);
  fields.push(`"type":${jsonString(value.type)}`);
  fields.push(`"sent_at":${jsonString(value.sent_at)}`);
  if (value.browser_session_id !== undefined) {
    fields.push(`"browser_session_id":${jsonString(value.browser_session_id)}`);
  }
  if (value.stream_id !== undefined) {
    fields.push(`"stream_id":${jsonString(value.stream_id)}`);
  }
  fields.push(`"payload":${payloadJson}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamAttached.
 */
export function encodeStreamAttached(value: StreamAttached): string {
  const fields: string[] = [];
  fields.push(`"project_id":${jsonString(value.project_id)}`);
  fields.push(`"mode":${jsonString(value.mode)}`);
  fields.push(`"format":${jsonString(value.format)}`);
  fields.push(`"retention":${jsonString(value.retention)}`);
  fields.push(`"max_frame_bytes":${jsonInteger(value.max_frame_bytes)}`);
  fields.push(`"attached_at":${jsonString(value.attached_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a FrameMetadata.
 */
export function encodeFrameMetadata(value: FrameMetadata): string {
  const fields: string[] = [];
  fields.push(`"sequence":${jsonInteger(value.sequence)}`);
  fields.push(`"captured_at":${jsonString(value.captured_at)}`);
  fields.push(`"mode":${jsonString(value.mode)}`);
  fields.push(`"format":${jsonString(value.format)}`);
  fields.push(`"width":${jsonInteger(value.width)}`);
  fields.push(`"height":${jsonInteger(value.height)}`);
  fields.push(`"quality":${jsonInteger(value.quality)}`);
  fields.push(`"byte_length":${jsonInteger(value.byte_length)}`);
  fields.push(`"dropped_before":${jsonInteger(value.dropped_before)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a QualityState.
 */
export function encodeQualityState(value: QualityState): string {
  const fields: string[] = [];
  fields.push(`"mode":${jsonString(value.mode)}`);
  fields.push(`"target_fps":${jsonInteger(value.target_fps)}`);
  fields.push(`"quality":${jsonInteger(value.quality)}`);
  fields.push(`"max_width":${jsonInteger(value.max_width)}`);
  fields.push(`"max_height":${jsonInteger(value.max_height)}`);
  fields.push(`"reason":${jsonString(value.reason)}`);
  fields.push(`"decided_at":${jsonString(value.decided_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a QualityRequest.
 */
export function encodeQualityRequest(value: QualityRequest): string {
  const fields: string[] = [];
  if (value.mode !== undefined) {
    fields.push(`"mode":${jsonString(value.mode)}`);
  }
  if (value.max_fps !== undefined) {
    fields.push(`"max_fps":${jsonInteger(value.max_fps)}`);
  }
  if (value.max_width !== undefined) {
    fields.push(`"max_width":${jsonInteger(value.max_width)}`);
  }
  if (value.max_height !== undefined) {
    fields.push(`"max_height":${jsonInteger(value.max_height)}`);
  }
  fields.push(`"requested_at":${jsonString(value.requested_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a StreamHeartbeat.
 */
export function encodeStreamHeartbeat(value: StreamHeartbeat): string {
  const fields: string[] = [];
  fields.push(`"observed_at":${jsonString(value.observed_at)}`);
  fields.push(`"frames_sent":${jsonInteger(value.frames_sent)}`);
  fields.push(`"frames_dropped":${jsonInteger(value.frames_dropped)}`);
  fields.push(`"buffer_depth":${jsonInteger(value.buffer_depth)}`);
  fields.push(`"buffer_capacity":${jsonInteger(value.buffer_capacity)}`);
  fields.push(`"measured_fps":${jsonNumber(value.measured_fps)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a ViewerHeartbeat.
 */
export function encodeViewerHeartbeat(value: ViewerHeartbeat): string {
  const fields: string[] = [];
  fields.push(`"observed_at":${jsonString(value.observed_at)}`);
  if (value.last_sequence_rendered !== undefined) {
    fields.push(`"last_sequence_rendered":${jsonInteger(value.last_sequence_rendered)}`);
  }
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a SessionState.
 */
export function encodeSessionState(value: SessionState): string {
  const fields: string[] = [];
  fields.push(`"status":${jsonString(value.status)}`);
  if (value.url !== undefined) {
    fields.push(`"url":${jsonString(value.url)}`);
  }
  fields.push(`"viewport":${encodeViewport(value.viewport)}`);
  fields.push(`"control_epoch":${jsonInteger(value.control_epoch)}`);
  if (value.live_capture !== undefined) {
    fields.push(`"live_capture":${jsonBoolean(value.live_capture)}`);
  }
  fields.push(`"observed_at":${jsonString(value.observed_at)}`);
  return `{${fields.join(",")}}`;
}

/**
 * Canonically encodes a LiveError.
 */
export function encodeLiveError(value: LiveError): string {
  const fields: string[] = [];
  fields.push(`"code":${jsonString(value.code)}`);
  fields.push(`"state":${jsonString(value.state)}`);
  fields.push(`"message":${jsonString(value.message)}`);
  fields.push(`"retryable":${jsonBoolean(value.retryable)}`);
  if (value.retry_after_ms !== undefined) {
    fields.push(`"retry_after_ms":${jsonInteger(value.retry_after_ms)}`);
  }
  return `{${fields.join(",")}}`;
}
