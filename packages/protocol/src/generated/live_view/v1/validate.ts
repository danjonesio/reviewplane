// Code generated by packages/protocol/tools/generate.ts from
// schemas/live_view/v1.schema.json. DO NOT EDIT.

import type { SchemaViolation } from "./types.ts";
import {
  checkBoolean,
  checkInteger,
  checkNumber,
  checkObject,
  checkPlainObject,
  checkString,
  matchesCondition,
  requireProperty,
} from "../../../validate-runtime.ts";

const PATTERN_0 = new RegExp("^[A-Za-z0-9_-]+$", "u");
const PATTERN_1 = new RegExp("^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$", "u");
const PATTERN_2 = new RegExp("^[!-~]+$", "u");
const PATTERN_3 = new RegExp("^[^\\u0000-\\u001f\\u007f]+$", "u");

/**
 * Opaque durable identifier (docs/DOMAIN_MODEL.md section 3). Consumers MUST treat the
 * value as opaque: the schema bounds only its length and character class and never its
 * documented prefix.
 */
export function validateIdentifier(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 64, pattern: PATTERN_0 });
}

/**
 * RFC 3339 timestamp in UTC, with a mandatory trailing Z.
 */
export function validateTimestamp(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 20, maxLength: 30, pattern: PATTERN_1 });
}

/**
 * Absolute URL observed by the browser. Page-derived and therefore untrusted (ADR-0010):
 * bounded in length and character class, rendered as text, never executed.
 */
export function validateResolvedUrl(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 2048, pattern: PATTERN_2 });
}

/**
 * Short operator-facing explanation. Never carries page content verbatim beyond its bound,
 * and never carries credentials (docs/SECURITY.md section 18).
 */
export function validateReasonText(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { minLength: 1, maxLength: 512, pattern: PATTERN_3 });
}

/**
 * Version 1 message type. Unknown types are rejected, never ignored. This enumeration MUST
 * equal the keys of x-protocol.messages.
 */
export function validateMessageType(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["live.attached","live.frame","live.quality","live.quality_request","live.heartbeat","live.viewer_heartbeat","live.session_state","live.error"] });
}

/**
 * Stable error class, drawn from the docs/MCP_SPEC.md section 12 enumeration so a refusal
 * reaches the viewer with the same code the HTTP API would report. This enumeration MUST
 * equal x-protocol.error_classes.
 */
export function validateErrorClass(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["PROTOCOL_UNSUPPORTED","AUTHENTICATION_REQUIRED","AUTHORISATION_DENIED","PROJECT_CONTEXT_MISMATCH","RESOURCE_NOT_FOUND","BROWSER_CAPACITY_EXHAUSTED","BROWSER_SESSION_NOT_ACTIVE","POLICY_DENIED","RATE_LIMITED","UNSUPPORTED_CAPABILITY","INTERNAL_ERROR"] });
}

/**
 * Which of the two rates of docs/ARCHITECTURE.md section 6.3 the stream is running at.
 * session_room adapts between 10 and 20 frames per second; thumbnail between 2 and 5.
 */
export function validateLiveMode(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["session_room","thumbnail"] });
}

/**
 * Media type of the binary frame payload. JPEG only: live frames are transient viewing
 * data, not evidence, and lossless originals are the business of an explicitly captured
 * screenshot (ADR-0009).
 */
export function validateFrameFormat(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["image/jpeg"] });
}

/**
 * Retention applied to the binary payloads of this stream. The single value is the point:
 * docs/SECURITY.md section 14 sets live_frames: never and ADR-0009 forbids a recording
 * path here, so a stream cannot advertise anything else.
 */
export function validateFrameRetention(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["never"] });
}

/**
 * Browser-session lifecycle status (docs/DOMAIN_MODEL.md section 12), reported so the page
 * states the session's condition as text.
 */
export function validateSessionStatus(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["REQUESTED","ALLOCATING","READY","ACTIVE","PAUSED","DEGRADED","TERMINATING","TERMINATED","FAILED"] });
}

/**
 * The actionable state docs/UX_FLOWS.md section 18 requires the UI to show instead of a
 * blank canvas or a generic error. Each value names a cause a human can act on.
 */
export function validateFailureState(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["not_authenticated","not_authorised_for_project","browser_session_not_found","browser_session_ended","browser_worker_failed","live_capture_unavailable","viewer_rate_limited","control_plane_unavailable"] });
}

/**
 * Why the scheduler chose the quality it did. Recorded so an operator can tell an
 * adaptation from a defect.
 */
export function validateQualityReason(value: unknown, path: string, out: SchemaViolation[]): void {
  checkString(value, path, out, { values: ["initial","viewer_requested","viewer_falling_behind","viewer_keeping_up","mode_changed","bounded_by_policy"] });
}

/**
 * Viewport width in CSS pixels.
 */
export function validateViewportWidth(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 240, maximum: 3840 });
}

/**
 * Viewport height in CSS pixels.
 */
export function validateViewportHeight(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 240, maximum: 2160 });
}

/**
 * Device pixel ratio of the browser session.
 */
export function validateViewportDeviceScaleFactor(value: unknown, path: string, out: SchemaViolation[]): void {
  checkNumber(value, path, out, { minimum: 1, maximum: 4 });
}

/**
 * Browser viewport in CSS pixels plus device pixel ratio (docs/API.md section 11). The
 * frame payload's own dimensions are reported per frame and may be smaller after an
 * adaptation.
 */
export function validateViewport(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["width", "height", "device_scale_factor"], ["width", "height", "device_scale_factor"]);
  if (source === null) return;
  if (source["width"] !== undefined) {
    validateViewportWidth(source["width"], `${path}.width`, out);
  }
  if (source["height"] !== undefined) {
    validateViewportHeight(source["height"], `${path}.height`, out);
  }
  if (source["device_scale_factor"] !== undefined) {
    validateViewportDeviceScaleFactor(source["device_scale_factor"], `${path}.device_scale_factor`, out);
  }
}

/**
 * Protocol version. Stage 0 accepts 1 only; any other value is rejected with
 * PROTOCOL_UNSUPPORTED and is never best-effort parsed.
 */
export function validateEnvelopeProtocolVersion(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 1 });
}

/**
 * Message payload. Its schema is selected by type. The message is bounded by
 * x-protocol.limits.max_live_message_bytes and each payload by its own x-max-bytes.
 */
export function validateEnvelopePayload(value: unknown, path: string, out: SchemaViolation[]): void {
  checkPlainObject(value, path, out);
}

/**
 * Live-channel message envelope. Every message names the browser session it concerns and
 * the stream instance it belongs to, so a message from a superseded stream is recognisable
 * rather than merely late.
 */
export function validateEnvelope(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["protocol_version", "message_id", "type", "sent_at", "browser_session_id", "stream_id", "payload"], ["protocol_version", "message_id", "type", "sent_at", "payload"]);
  if (source === null) return;
  if (source["protocol_version"] !== undefined) {
    validateEnvelopeProtocolVersion(source["protocol_version"], `${path}.protocol_version`, out);
  }
  if (source["message_id"] !== undefined) {
    validateIdentifier(source["message_id"], `${path}.message_id`, out);
  }
  if (source["type"] !== undefined) {
    validateMessageType(source["type"], `${path}.type`, out);
  }
  if (source["sent_at"] !== undefined) {
    validateTimestamp(source["sent_at"], `${path}.sent_at`, out);
  }
  if (source["browser_session_id"] !== undefined) {
    validateIdentifier(source["browser_session_id"], `${path}.browser_session_id`, out);
  }
  if (source["stream_id"] !== undefined) {
    validateIdentifier(source["stream_id"], `${path}.stream_id`, out);
  }
  if (source["payload"] !== undefined) {
    validateEnvelopePayload(source["payload"], `${path}.payload`, out);
  }
  if (matchesCondition(source["type"], ["live.attached", "live.frame", "live.quality", "live.heartbeat", "live.session_state"])) {
    requireProperty(source, path, "browser_session_id", "every server message that describes a running stream names both the session and the stream instance", out);
    requireProperty(source, path, "stream_id", "every server message that describes a running stream names both the session and the stream instance", out);
  }
  if (matchesCondition(source["type"], ["live.frame"])) {
    requireProperty(source, path, "browser_session_id", "frame metadata is bound to its browser session so a relayed frame cannot be attributed to another", out);
  }
}

/**
 * Largest binary payload the control plane will forward. A larger frame is dropped by the
 * producer rather than truncated.
 */
export function validateStreamAttachedMaxFrameBytes(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1024, maximum: 4194304 });
}

/**
 * Confirms an authenticated, authorised viewer attachment and states the terms of the
 * stream.
 */
export function validateStreamAttached(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["project_id", "mode", "format", "retention", "max_frame_bytes", "attached_at"], ["project_id", "mode", "format", "retention", "max_frame_bytes", "attached_at"]);
  if (source === null) return;
  if (source["project_id"] !== undefined) {
    validateIdentifier(source["project_id"], `${path}.project_id`, out);
  }
  if (source["mode"] !== undefined) {
    validateLiveMode(source["mode"], `${path}.mode`, out);
  }
  if (source["format"] !== undefined) {
    validateFrameFormat(source["format"], `${path}.format`, out);
  }
  if (source["retention"] !== undefined) {
    validateFrameRetention(source["retention"], `${path}.retention`, out);
  }
  if (source["max_frame_bytes"] !== undefined) {
    validateStreamAttachedMaxFrameBytes(source["max_frame_bytes"], `${path}.max_frame_bytes`, out);
  }
  if (source["attached_at"] !== undefined) {
    validateTimestamp(source["attached_at"], `${path}.attached_at`, out);
  }
}

/**
 * Monotonically increasing per-stream frame number. Gaps are the dropped frames
 * docs/ARCHITECTURE.md section 6.3 prefers to a growing queue, and are therefore expected
 * rather than exceptional.
 */
export function validateFrameMetadataSequence(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 4294967295 });
}

/**
 * Payload width in device pixels. It may be below the viewport width after a bandwidth
 * adaptation.
 */
export function validateFrameMetadataWidth(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 7680 });
}

/**
 * Payload height in device pixels.
 */
export function validateFrameMetadataHeight(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 4320 });
}

/**
 * Encoder quality the frame was produced at, 1 to 100.
 */
export function validateFrameMetadataQuality(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 100 });
}

/**
 * Byte length of the binary payload that follows. A receiver that reads a different length
 * MUST discard the frame rather than render a partial one.
 */
export function validateFrameMetadataByteLength(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 4194304 });
}

/**
 * Frames the producer discarded since the previous delivered frame. Reported per frame so
 * the drop rate is observable without a separate metrics path.
 */
export function validateFrameMetadataDroppedBefore(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 4294967295 });
}

/**
 * Describes the binary payload that follows it as the next transport message. Nothing here
 * is page-derived except by dimension, and the payload itself is rendered as an image and
 * never as markup (ADR-0010).
 */
export function validateFrameMetadata(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["sequence", "captured_at", "mode", "format", "width", "height", "quality", "byte_length", "dropped_before"], ["sequence", "captured_at", "mode", "format", "width", "height", "quality", "byte_length", "dropped_before"]);
  if (source === null) return;
  if (source["sequence"] !== undefined) {
    validateFrameMetadataSequence(source["sequence"], `${path}.sequence`, out);
  }
  if (source["captured_at"] !== undefined) {
    validateTimestamp(source["captured_at"], `${path}.captured_at`, out);
  }
  if (source["mode"] !== undefined) {
    validateLiveMode(source["mode"], `${path}.mode`, out);
  }
  if (source["format"] !== undefined) {
    validateFrameFormat(source["format"], `${path}.format`, out);
  }
  if (source["width"] !== undefined) {
    validateFrameMetadataWidth(source["width"], `${path}.width`, out);
  }
  if (source["height"] !== undefined) {
    validateFrameMetadataHeight(source["height"], `${path}.height`, out);
  }
  if (source["quality"] !== undefined) {
    validateFrameMetadataQuality(source["quality"], `${path}.quality`, out);
  }
  if (source["byte_length"] !== undefined) {
    validateFrameMetadataByteLength(source["byte_length"], `${path}.byte_length`, out);
  }
  if (source["dropped_before"] !== undefined) {
    validateFrameMetadataDroppedBefore(source["dropped_before"], `${path}.dropped_before`, out);
  }
}

/**
 * Frames per second the producer is now aiming for. It stays inside the band of
 * x-protocol.vocabularies.frame_rate_bands for the mode.
 */
export function validateQualityStateTargetFps(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 30 });
}

/**
 * Encoder quality now in force, 1 to 100.
 */
export function validateQualityStateQuality(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 100 });
}

/**
 * Largest payload width the producer will now emit.
 */
export function validateQualityStateMaxWidth(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 7680 });
}

/**
 * Largest payload height the producer will now emit.
 */
export function validateQualityStateMaxHeight(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 4320 });
}

/**
 * The scheduler's applied decision. The worker is authoritative over every field here
 * (docs/API.md section 18.2).
 */
export function validateQualityState(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["mode", "target_fps", "quality", "max_width", "max_height", "reason", "decided_at"], ["mode", "target_fps", "quality", "max_width", "max_height", "reason", "decided_at"]);
  if (source === null) return;
  if (source["mode"] !== undefined) {
    validateLiveMode(source["mode"], `${path}.mode`, out);
  }
  if (source["target_fps"] !== undefined) {
    validateQualityStateTargetFps(source["target_fps"], `${path}.target_fps`, out);
  }
  if (source["quality"] !== undefined) {
    validateQualityStateQuality(source["quality"], `${path}.quality`, out);
  }
  if (source["max_width"] !== undefined) {
    validateQualityStateMaxWidth(source["max_width"], `${path}.max_width`, out);
  }
  if (source["max_height"] !== undefined) {
    validateQualityStateMaxHeight(source["max_height"], `${path}.max_height`, out);
  }
  if (source["reason"] !== undefined) {
    validateQualityReason(source["reason"], `${path}.reason`, out);
  }
  if (source["decided_at"] !== undefined) {
    validateTimestamp(source["decided_at"], `${path}.decided_at`, out);
  }
}

/**
 * Upper rate the viewer can consume. A viewer may lower its own ceiling; it cannot raise
 * the band's.
 */
export function validateQualityRequestMaxFps(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 30 });
}

/**
 * Largest payload width the viewer can usefully display, for example its rendered canvas
 * width.
 */
export function validateQualityRequestMaxWidth(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 7680 });
}

/**
 * Largest payload height the viewer can usefully display.
 */
export function validateQualityRequestMaxHeight(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 4320 });
}

/**
 * A viewer's advisory request. The worker scheduler may honour it, bound it or ignore it;
 * a viewer MUST NOT assume its request was applied and MUST read the live.quality that
 * follows.
 */
export function validateQualityRequest(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["mode", "max_fps", "max_width", "max_height", "requested_at"], ["requested_at"]);
  if (source === null) return;
  if (source["mode"] !== undefined) {
    validateLiveMode(source["mode"], `${path}.mode`, out);
  }
  if (source["max_fps"] !== undefined) {
    validateQualityRequestMaxFps(source["max_fps"], `${path}.max_fps`, out);
  }
  if (source["max_width"] !== undefined) {
    validateQualityRequestMaxWidth(source["max_width"], `${path}.max_width`, out);
  }
  if (source["max_height"] !== undefined) {
    validateQualityRequestMaxHeight(source["max_height"], `${path}.max_height`, out);
  }
  if (source["requested_at"] !== undefined) {
    validateTimestamp(source["requested_at"], `${path}.requested_at`, out);
  }
}

/**
 * Frames delivered to this viewer since attachment.
 */
export function validateStreamHeartbeatFramesSent(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 4294967295 });
}

/**
 * Frames discarded rather than queued since attachment.
 */
export function validateStreamHeartbeatFramesDropped(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 4294967295 });
}

/**
 * Frames currently waiting to be written to this viewer.
 */
export function validateStreamHeartbeatBufferDepth(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 64 });
}

/**
 * Bound on that buffer. It is a fixed small number: a viewer that falls behind sees fewer,
 * newer frames rather than a backlog.
 */
export function validateStreamHeartbeatBufferCapacity(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 1, maximum: 64 });
}

/**
 * Frames per second actually delivered over the last sampling window.
 */
export function validateStreamHeartbeatMeasuredFps(value: unknown, path: string, out: SchemaViolation[]): void {
  checkNumber(value, path, out, { minimum: 0, maximum: 240 });
}

/**
 * Liveness plus the figures docs/TESTING.md section 12 requires to be measurable:
 * delivered frames, dropped frames, and the depth of the bounded buffer that produced the
 * drops.
 */
export function validateStreamHeartbeat(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["observed_at", "frames_sent", "frames_dropped", "buffer_depth", "buffer_capacity", "measured_fps"], ["observed_at", "frames_sent", "frames_dropped", "buffer_depth", "buffer_capacity", "measured_fps"]);
  if (source === null) return;
  if (source["observed_at"] !== undefined) {
    validateTimestamp(source["observed_at"], `${path}.observed_at`, out);
  }
  if (source["frames_sent"] !== undefined) {
    validateStreamHeartbeatFramesSent(source["frames_sent"], `${path}.frames_sent`, out);
  }
  if (source["frames_dropped"] !== undefined) {
    validateStreamHeartbeatFramesDropped(source["frames_dropped"], `${path}.frames_dropped`, out);
  }
  if (source["buffer_depth"] !== undefined) {
    validateStreamHeartbeatBufferDepth(source["buffer_depth"], `${path}.buffer_depth`, out);
  }
  if (source["buffer_capacity"] !== undefined) {
    validateStreamHeartbeatBufferCapacity(source["buffer_capacity"], `${path}.buffer_capacity`, out);
  }
  if (source["measured_fps"] !== undefined) {
    validateStreamHeartbeatMeasuredFps(source["measured_fps"], `${path}.measured_fps`, out);
  }
}

/**
 * Sequence of the last frame the viewer actually painted.
 */
export function validateViewerHeartbeatLastSequenceRendered(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 4294967295 });
}

/**
 * Viewer liveness and progress. The last rendered sequence is what lets the control plane
 * distinguish a viewer that is slow from a link that is idle.
 */
export function validateViewerHeartbeat(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["observed_at", "last_sequence_rendered"], ["observed_at"]);
  if (source === null) return;
  if (source["observed_at"] !== undefined) {
    validateTimestamp(source["observed_at"], `${path}.observed_at`, out);
  }
  if (source["last_sequence_rendered"] !== undefined) {
    validateViewerHeartbeatLastSequenceRendered(source["last_sequence_rendered"], `${path}.last_sequence_rendered`, out);
  }
}

/**
 * Control epoch current at the browser session (ADR-0007). Reported so a later takeover
 * surface has the value it must present.
 */
export function validateSessionStateControlEpoch(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 2147483647 });
}

/**
 * Whether frame capture is running. False with an otherwise healthy status is the degraded
 * case docs/DEVELOPMENT.md section 11 requires: the session stays usable for navigation
 * and screenshots without live frames.
 */
export function validateSessionStateLiveCapture(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Browser-session condition, sent at attachment and whenever it changes. The URL is
 * page-derived and is rendered as text only.
 */
export function validateSessionState(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["status", "url", "viewport", "control_epoch", "live_capture", "observed_at"], ["status", "viewport", "control_epoch", "observed_at"]);
  if (source === null) return;
  if (source["status"] !== undefined) {
    validateSessionStatus(source["status"], `${path}.status`, out);
  }
  if (source["url"] !== undefined) {
    validateResolvedUrl(source["url"], `${path}.url`, out);
  }
  if (source["viewport"] !== undefined) {
    validateViewport(source["viewport"], `${path}.viewport`, out);
  }
  if (source["control_epoch"] !== undefined) {
    validateSessionStateControlEpoch(source["control_epoch"], `${path}.control_epoch`, out);
  }
  if (source["live_capture"] !== undefined) {
    validateSessionStateLiveCapture(source["live_capture"], `${path}.live_capture`, out);
  }
  if (source["observed_at"] !== undefined) {
    validateTimestamp(source["observed_at"], `${path}.observed_at`, out);
  }
}

/**
 * Whether reconnecting could succeed. A viewer MUST NOT reconnect in a loop when this is
 * false.
 */
export function validateLiveErrorRetryable(value: unknown, path: string, out: SchemaViolation[]): void {
  checkBoolean(value, path, out);
}

/**
 * Hint for how long to wait before reconnecting, returned with RATE_LIMITED (docs/API.md
 * section 19).
 */
export function validateLiveErrorRetryAfterMs(value: unknown, path: string, out: SchemaViolation[]): void {
  checkInteger(value, path, out, { minimum: 0, maximum: 3600000 });
}

/**
 * A refusal or a stream failure. It carries the stable error class for logs and the
 * failure state for the UI, because docs/UX_FLOWS.md section 18 forbids a generic message
 * where a specific cause exists.
 */
export function validateLiveError(value: unknown, path: string, out: SchemaViolation[]): void {
  const source = checkObject(value, path, out, ["code", "state", "message", "retryable", "retry_after_ms"], ["code", "state", "message", "retryable"]);
  if (source === null) return;
  if (source["code"] !== undefined) {
    validateErrorClass(source["code"], `${path}.code`, out);
  }
  if (source["state"] !== undefined) {
    validateFailureState(source["state"], `${path}.state`, out);
  }
  if (source["message"] !== undefined) {
    validateReasonText(source["message"], `${path}.message`, out);
  }
  if (source["retryable"] !== undefined) {
    validateLiveErrorRetryable(source["retryable"], `${path}.retryable`, out);
  }
  if (source["retry_after_ms"] !== undefined) {
    validateLiveErrorRetryAfterMs(source["retry_after_ms"], `${path}.retry_after_ms`, out);
  }
}
