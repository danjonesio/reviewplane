go 1.26.5

// Modules developed together in this repository. A Go service is listed here so
// that it resolves packages/protocol from the working tree rather than from a
// tag. services/connector joins this list when it is created.
use (
	./packages/protocol
	./services/tunnel-gateway
)
